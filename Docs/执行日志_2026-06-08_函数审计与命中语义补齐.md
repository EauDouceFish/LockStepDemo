# 执行日志：函数审计与命中语义补齐

日期：2026-06-08

## 本轮完成

- 新增 Roslyn 审计工具：`Tools/BattleAudit`。
- 已生成函数清单：
  - `Docs/Generated/battle_logic_functions_csharp.tsv`：C# 逻辑层战斗相关函数，当前 608 个。
  - `Docs/Generated/ikemen_battle_functions_go.tsv`：Ikemen GO 核心战斗相关 Go 函数，当前 1435 个。
  - `Docs/Generated/battle_function_audit.md`：C# 函数到 Ikemen 候选引用的自动审计表。
  - `Docs/Generated/ikemen_functions_not_name_matched.md`：名称级未匹配 Ikemen 函数清单。
- 修复 Ananzi fatal preview 的纯逻辑前置条件：fatal 需要低敌方血量、满气等前置，`MMovePrerequisiteProfile.TargetLife` 已接入，并加入 `AnanziFatal_Reaches4000_WhenFatalPrerequisitesAreSatisfied`。
- HitOverride 已接入命中结算：匹配 attr 后进入 override state，`ForceAir/KeepState` 有基础处理。
- AttackDist 已接入 `inguarddist`：`AttackDistController` 写入运行态 `AttackDistX`，敌方守招距离读取该值。
- ReversalDef 已接入普通命中和 projectile 命中路径：匹配 attr 后以 ReversalDef 模板反打攻方/弹幕 owner。
- Target ownership 补齐了 HitDef id 维度：HitDef `id` 解析、命中登记 `MTargetRef`、`target(id)` redirect 和 Target* 控制器按 hit id 过滤。
- 展馆调试面板改为 LiveView 风格字段：`StateNo / AnimNo / Elem / Physics / Type / Ctrl / Pos / Vel / Facing / Life`。

## TDD 验收

- `MHitSystemTests`：HitOverride、ReversalDef、projectile ReversalDef、HitDef target id 登记。
- `TargetControllersTests`：TargetState 按 HitDef id 过滤、HitDef id 解析。
- `HelperRedirectTests`：`target(id)` 按 HitDef id redirect。
- `RoundStateGuardDistTriggerTests`：`inguarddist` 使用敌方 AttackDist。
- `MoveTestRunnerTests`：Ananzi fatal 在满足前置条件后到达 4000。

## 仍不能声明完成的真实问题

- Ikemen headless oracle trace 仍未接入。本机 `go` 不存在，当前只能生成 C# trace/schema，不能做逐帧 Ikemen 对账。
- 函数审计目前是清单与候选映射，不是 608 个 C# 函数逐行人工确认。`battle_function_audit.md` 中 `auto-candidate` 只代表名称候选，不代表等价。
- ReversalDef 仍是基础 MVP：尚未完整覆盖 guardflag.not、pausetime、spark/sound、multi-hit trade、Ikemen hit result 全分支。
- HitOverride 仍需继续精确化：ForceGuard、slot 优先级、playerNo/state owner、hitpause 修正还需要 Ikemen trace 验证。
- helper/projectile/explod/spark/sound/palfx 的 Unity 展示实体没有完整表现；逻辑层有部分 event/entity，但展馆渲染不等于 Ikemen。
- localcoord/AIR/SFF/palette/ChangeAnim2 的资源 owner 与缩放仍需继续对账。

## 下一步

1. 安装/提供 Go 工具链后，给 Ikemen 固定提交插桩并输出同 schema JSONL trace。
2. 以 `Docs/Generated/battle_function_audit.md` 为索引，优先人工确认 `char.go` 中 state/action/hit/target 相关函数。
3. 继续补表现实体生命周期：Explod tick/remove/bind、PalFX/AfterImage trace event、sound/spark event 去重。
