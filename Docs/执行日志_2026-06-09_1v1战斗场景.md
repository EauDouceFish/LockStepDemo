# 2026-06-09 1v1 战斗场景（回合 HUD + 入场鞠躬 + 双人键盘）

## 逻辑层（TDD，dotnet test 706→709）
`MRoundSystem` 增强（commit 8f05425）：
- 入场鞠躬：开局/回合间 `BeginRoundIntro()` 把双方送入入场态（候选 190/191，KFM=191 鞠躬 anim190）；`EnterFight` 把仍在入场态者回中立 0；候选存在性保护（无入场态角色不动 → 旧测试不破）。
- 胜利姿态：`EnterOver` 胜者送 win pose（候选 180/181/195，KFM=195）。
- `TimerSeconds`（倒计时秒，向上取整）。
- 测试 `MRoundIntroTests` 3 例。坑：Timer 在构造函数按默认 RoundTime 缓存，对象初始化器在构造后才跑。

## Unity 表现层（⚠️ 未实测，MCP 未连，待调试会话）
- `MugenBattleHud`：程序化 ScreenSpaceOverlay HUD（1x1 白 Sprite + Image.fillAmount + LegacyRuntime.ttf）。左右双层血条（红即时 real + 黄缓降 damage，参考 Super Nova HealthUI 的 WhiteHealthFade）、中央倒计时、左右回合胜利点、中央提示（Round N / Fight! / K.O. / Player N Wins）。
- `MugenVersusView`：P2 第二键盘（WASD + J/K/L），接入 HUD，默认三局两胜。
- 菜单 `MUGEN/Demo/创建 1v1 战斗场景 (kfm vs kfm, 含HUD)`。

## 待调试会话验证
- HUD 布局/血条方向/缓降手感、入场鞠躬动画是否播放、Fight!/K.O. 提示时机。
- 绘制态混合模式（续31 Batch E 遗留：Add/Sub 需自定义材质）。

## 下一步
接 KCP 本地跑通（ITransport/Loopback + KcpClient/ServerTransport + Plugins/Kcp 已就绪）：双进程各控 P1，交换 MInput，MRoundSystem.Tick 同步推进，ComputeHash 对账。先本地 loopback/双开，再上新加坡服务器。
