================================================================================
R-ENT 实体系统进度 + 到「兼容所有 MUGEN 角色导入 → 状态机可跑」的清单
================================================================================
基线：2026-06-06，已推进到 R-DMG-PIPELINE 两段式伤害核心，dotnet test 502/502。

--------------------------------------------------------------------------------
A. R-ENT 已完成（切片 1-2）
--------------------------------------------------------------------------------
[✅] 实体世界 MEntityWorld：共享 spawn 队列 + helper 列表 + 确定性 id 分配，入 ComputeHash。
[✅] helper 实体生命周期：RequestHelper 入队 → DrainSpawns 造实体(用 owner 角色数据，接 Parent/Root/P2/Rng/Pause/World)
     → 引擎 Tick 跑 helper 的 prepare/状态机/物理/动画(用 owner 数据) → RemoveDestroyed 帧末移除。
[✅] 触发器 ishelper(无参)/numhelper。
[✅] 控制器 Helper(stateno/id/pos/postype/facing/keyctrl) / DestroySelf，接 MugenCnsParser，端到端 CNS 测试绿。
[✅] 确定性：helper 生命周期逐帧 ComputeHash 一致。

--------------------------------------------------------------------------------
B. R-ENT 余下切片（按依赖/价值排序）
--------------------------------------------------------------------------------
[✅] 切片3 Projectile：3a(commit a7be935)motion+生命周期+numproj；3b(commit ae59d48)自带 HitDef+Clsn1+命中敌队(ApplyHit 走 owner)
     +projcontact/projhit 计数+命中移除+确定性。弹幕角色可真正打人；projcontact/projhit/proj*time 触发器已于切片8 接线。
[✅] 切片4 Explod：逻辑层 MExplod 最小追踪已完成，Explod/ModifyExplod/RemoveExplod 和 numexplod(id) 可观测；
     渲染仍归后续表现层。
[✅] 切片5 custom state 投技(commit 759d948)：MChar.StateOwner+MHitDef.P2GetP1State；ApplyHit p2stateno+p2getp1state→守方跑攻方状态表；
     引擎 StateDataFor 用归属角色数据表；SelfState 释放归属。投技机制打通(P2 进 P1 投技态、P1 状态在 P2 上跑、确定性)。
[✅] 切片6 helper 关联控制器：**ParentVarSet/Add、RootVarSet/Add 已完成（commit bc79a6c）**；
     BindToParent/Root/Target、TargetBind、ChangeAnim2 核心已完成并入 Hash。localcoord/head-mid/readplayerid 精细语义后续补。
[✅] 切片7 全实体跨队命中(commit 92a1430)：玩家+helper 跨队两两 TryHit(队伍=Root.Id)，helper 能打敌队不打同队；弹幕命中走切片3b。
[✅] 切片8 参数形触发器与 helper redirect：
     已完成 numhelper(id)/ishelper(id)/numproj(id) 参数形，以及 projcontact/projhit/projguarded 布尔别名和
     projcontacttime/projhittime/projguardedtime/projcanceltime 时间读取（本轮 491/491）。
     已补 numexplod(id)，helper(id,index) redirect 与 partner(index) redirect 表达式层已绿（partner 目前依赖显式 Partner 链）。

--------------------------------------------------------------------------------
C. R-ENT 之外、到「兼容所有角色」仍需的（审计风险表对应项）
--------------------------------------------------------------------------------
[✅] R-DMG-PIPELINE(R3) 核心：引擎命中路径改为 ghv.damage 式累计，帧内 hit/projectile 全部结算后统一应用 Life；
     同帧多段/多实体命中不再边命中边扣血。KO 排序细节仍留 R-ORACLE trace 精确化。
[⬜] juggle(R5)：juggle 点扣减/检查真正生效，防无限连。
[⬜] AnimTime 即刻刷新(R7)：ChangeAnim 后同帧读 AnimTime/AnimElem 反映新动画(消除晚一相)。
[⬜] ReversalDef：反击定义容器 + 命中检测(打断对方攻击)。
[⬜] 触发器长尾覆盖补全：const/sysvar 全字段、gethitvar 余字段、projXXX、stagevar 等(随真实角色用量驱动)。
[⬜] R-LOCALCOORD：localcoord 缩放(不同坐标系角色 clsn/pos/vel 缩放)。
[⬜] R-PAL：.act 调色板 + PalFX 着色(表现，影响 Janos 等)。
[⬜] R-SND：声音事件(PlaySnd 真播；逻辑层已 no-op)。
[⬜] R-ORACLE：Ikemen headless 帧 trace 对账(锚定 commit)，量化连续量漂移 + 离散全等回归。

--------------------------------------------------------------------------------
D. 「状态机可兼容所有角色导入」的判据与现状
--------------------------------------------------------------------------------
判据 = 任意 MUGEN 角色 .def/.cns/.cmd/.air/.sff 导入后，其状态机(含负状态/招式/受击/helper/proj)在引擎里
       不崩、不哑火、离散行为与 Ikemen 一致(连续量容差)。

现状（范围分级，对齐审计 Ikemen对照审计.txt）：
- 范围A(1v1、无 helper/proj、无超必杀冻结依赖)：离散主干已在本地测试 + KFM 类样例范围内通过。
- 范围B(真实角色泛用)：**审计 4 个条件高风险缺口已全部解决**——helper(切片1-2/3/6-7)、projectile 命中(切片3b)、
  全实体跨队命中(切片7)、投技 custom state(切片5)、Explod(numexplod) 最小追踪(切片4)、helper/partner redirect 表达式层、BindTo*/ChangeAnim2 核心。剩余为较小项：
  juggle、AnimTime 即刻刷新、触发器长尾、R-ORACLE 兜底。
  核心战斗状态机+实体机制已支持真实角色主干；声称「全角色导入兼容」仍待 R-ORACLE 差分 + 全角色冒烟矩阵确认。

下一步执行顺序：
  juggle/AnimTime → 触发器长尾 → R-ORACLE 差分兜底 → 全角色导入冒烟测试矩阵。

（本清单随 R-ENT 切片推进持续更新。）
