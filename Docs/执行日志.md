# 执行日志（人类可读 ledger）

> 与 git commit 互补：git 记"改了哪些文件"，本文记"为什么/到哪了/下次继续"。
> 条目用绝对日期。详细架构见 `架构设计.md`，步骤见 `细化计划.md`。

---

## 2026-06-02 · MUGEN 化改造启动

**方向确立**：把项目从"6 态硬编码状态机"大改为 **MUGEN 化定点帧同步格斗引擎**。方案 =
Ikemen 引擎结构（只抄设计）+ MUGEN 导入管线 + 自有定点确定性。三大取舍已定：
① 战斗模拟 0 Lua（角色=数据，定点表达式 VM 解释）；② 保留 FFloat + hash 对账；
③ 回滚先不实现、框架留出（S13 / Stretch）。

**本次完成**：
- `git init`（LockstepActDemo 现在是 git 仓库）+ 基线提交 `7ca8032`（308 文件）。补 `.gitignore`（Unity + .NET + 仓库外素材）。
- 原始素材：`../MugenSource/Terrarian/`（完整角色，仓库外，已 gitignore）。
- 参考/Oracle：clone `Ikemen-GO` 到 `../MugenSource/_reference/`（只读，12 万行 Go；核心 `bytecode.go`/`char.go`/`compiler.go`/`anim.go`/`image.go`）。
- 文档：`Docs/架构设计.md`（基准）+ `Docs/细化计划.md`（21 天 / 8 阶段 / T* 任务 / TDD + 差分测试 + 多 agent 协议）。

**关键方法论**：以 Ikemen GO 为 **Oracle 做差分测试**（离散量全等 + 连续量容差）+ **确定性黄金哈希**，把"调试"变"对答案"，解决"单人无法肉眼调手感"。

**下次继续（Phase 0 / B）**：建 .NET 测试工程 → 重写 `CLAUDE.md` 为 agent 宪法 → 数据 schema 骨架 + `IExpressionVM` 接口与最小实现 + 新增 Component → 第一个黄金哈希测试。详见 `细化计划.md` Phase 0（T0.1–T0.6）。

---

## 2026-06-02（下午）· 战斗模式拆双支路 + Phase 0 开工

**决策**：拆成两个独立战斗模式，**v1 只做 MUGEN-2D**（纯 2D 平面对拳，仿 MUGEN），火影纵深 3D 列为 v2 支路。理由：纵深改变摇杆语义（2D 上=跳/下=蹲 vs 3D 上下=纵深走位）→ 与 MUGEN 角色资源天生不匹配 → 必须分模式而非可切换。模式专属逻辑隔离在 4 个 seam（Physics/Collision/输入映射/View）。v1 坐标改用 **MUGEN 原生**（X 横, Y 高度, 上为负），importer 坐标恒等映射，便于差分对齐 Ikemen。详见 `架构设计.md` §3b。

**本次完成**：架构文档 §3b 改写为"双模式 + 隔离设计"；细化计划加 §0.0 范围锁定 v1=2D；**Phase 0 T0.1 启动**（.NET 测试工程）。

---

## 2026-06-02（晚）· Phase 0 完成（T0.1–T0.6）

**"最大框架"骨架搭完，`dotnet test` 12/12 全绿。**

- **T0.1**：`.NET 测试工程` + 冒烟测试（FFloat 可逆 / Hash64 可复现）。修 `.gitignore` 误伤手写 csproj。
- **T0.2**：重写根 `CLAUDE.md` 为 **agent 宪法**（方向/三铁律/TDD+差分+黄金哈希/DoD 五条/提交规范/多 agent 协议/关键路径）。
- **T0.3**：数据 schema 骨架 `Assets/Logic/Game/Data/`：CombatEnums(StateType/MoveType/Physics/ControllerType...)、ClsnBox、AnimData/AnimFrame、CommandData、HitDef、StateController、StateDef、CharConstants、CharacterDef。**形状对齐 MUGEN，实现留空待填**。
- **T0.4**：`Assets/Logic/Game/Expr/`：IExpr/IExpressionVM/IEvalContext + `ExpressionVM`（真 lexer+递归下降 parser+AST+定点求值；支持常量/标识符/比较/四则/一元负/括号）。**不硬编码任何 trigger（避形状捷径）**。
- **T0.5**：新增组件 `AnimC/VarsC/CommandStateC/HitDefStateC/CharacterRefC`（Clone 深拷 + WriteHash）。
- **T0.6**：第一个**黄金哈希测试**（10 帧，golden=`15945274352023983870`）+ 表达式 VM 单测 + 快照深拷测试，共 12 测试。

**验收**：`cd Tests/Lockstep.Logic.Tests && dotnet test` → 12 通过 0 失败。证明 Logic 层（含全部新代码）零 Unity 依赖、可独立编译测试。

**下次继续（Phase 1）**：导入管线 —— `IAirImporter`(.air→AnimData) 与 `ISffImporter`(.sff→Sprite) 可并行（T1.1/T1.2），再接 `SpriteAnimator`(T1.3) 让 Terrarian/KFM 在 Unity 动起来。详见 `细化计划.md` Phase 1。

---

## 2026-06-02（深夜）· Phase 1 导入管线（T1.1–T1.4）

**目标达成：T1.1–T1.4 完成，`dotnet test` 25/25 全绿（含真实 Terrarian 数据验证）。**

策略：每个任务的**可测纯逻辑**放 Unity-free 层（dotnet 可测），Unity 表现层只做薄壳。

- **T1.1 AIR 解析**：`Assets/Logic/Import/Air/AirParser.cs`。处理 [Begin Action]、Clsn1/2(Default 持久/非 Default 单帧)、帧行(含 flip)、LoopStart、注释。验收：合成样例 + 真实 Terrarian.air（Action0 dur=120、Action20 走路 13 帧、每帧有受击框）。
- **T1.2 SFF v1 + PCX**：`Assets/Logic/Import/Sff/`(SffTypes/SffV1Reader/PcxDecoder)。解析头(签名+版本+组/图数+链表节点)，PCX RLE 解码+调色板。验收：真实 Terrarian.sff → 187 组/1935 图/1935 节点全在文件内 + 首个非空精灵 PCX 解码尺寸自洽。**踩坑**：尾节点 next 指针未归零→越界 EOF，改为以 NumImages 计数封顶 + 偏移边界检查。
- **T1.3 动画推进**：`Assets/Logic/Game/Anim/AnimAdvance.cs`（纯逻辑，表现层与未来 AnimSystem 共用）。验收：帧切换/循环/永久帧/LoopStart 非零。
- **T1.4 Clsn→世界 AABB**：`Assets/Logic/Game/Data/ClsnWorld.cs`（含 facing 镜像、重叠判定）。验收：朝向镜像/原点偏移/命中落空。
- **Unity 表现层薄壳（不参与 dotnet test，待编辑器内验证）**：`Assets/Editor/Import/MugenImportMenu.cs`(SFF→PNG 菜单 + AIR 日志)、`Assets/Scripts/Game/View/MugenSpriteAnimator.cs`、`ClsnGizmo.cs`。生成精灵输出到 `Assets/Mugen/`（已 gitignore）。

**验收**：`cd Tests/Lockstep.Logic.Tests && dotnet test` → 25 通过 0 失败。

**下次继续（Phase 2）**：表达式 VM 扩 trigger + 通用数据驱动 StateMachineSystem + CNS 导入 + Physics/Anim System，从 common1.cns 把站/走/蹲/跳作为数据跑起来。**注**：表现层 3 个 Unity 脚本需在编辑器打开工程确认编译 + 跑一次 SFF 导入看图。

---

## 2026-06-02（深夜·续）· 规范自查 + Phase 2 数据驱动引擎（T2.1–T2.6）

**先做 Tencent 规范自查 + 重构**（提交 `865b57c`）：无 var；修 AirParser 单行 if→Allman；
ClsnWorld/ExpressionVM/AirParser/PcxDecoder/SffV1Reader 单字母→完整词；ExpressionVM 抽 ExprNode 基类去重；补 XML summary。

**Phase 2 完成（dotnet test 25→41 全绿，TDD 每步提交）**：
- **T2.1**（`b7ceab6`+`c28894b`）：表达式 VM 加逻辑(`|| && !`)/函数(`abs/ifelse/cond/min/max`)/带参 trigger(`var(n)`)；
  `WorldEvalContext` 从 World+Entity 解析 Time/StateNo/Ctrl/Pos/Vel/Anim/Life/var 等；新增 `MugenStateC`(int 状态号)。
- **T2.4–T2.6**（`0bd2882`）：`MugenGameData` + `StateControllerExecutor`(12 类控制器) + `MugenStateMachineSystem`
  (控制器求值 + ChangeState 同帧重入有界) + `MugenPhysicsSystem`(速度积分+Air 重力+落地) + `MugenAnimSystem`。
  端到端测试：手搓 stand↔walk 角色，状态切换/位移/确定性全过。
- **T2.2**（`07d3f21`）：`CmdParser` 解析 .cmd [Command] → CommandData(方向大写/按钮小写区分)。
- **T2.3**（`c8d1de6`）：`CnsParser` 容错结构化解析 .cns → StateDef/Controller(triggerall&&(gN||..) 合成)；
  合成 CNS 端到端驱动引擎；真实 common1.cns 容错解析不崩。

**Phase 2 里程碑达成**：数据驱动引擎能跑「CNS 文本 → 角色数据 → 状态机 + 物理 + 动画」，用受支持的 trigger/controller 子集。

**诚实边界（T2.3b，后续）**：common1.cns / Terrarian.cns 完整「活着跑」还需：运行期未知 trigger 容错
（现在 `alive`/`ishelper` 等会在 eval 抛异常）、枚举 token 翻译(StateType=S)、字符串 command="..."、p2 重定向、更多 trigger/controller。

**Unity 可见性**：① 静态精灵=Unity 里跑 SFF 导入菜单（待你那边编译）；② 动画循环=需写 demo 加载器（未做）；
③ 数据驱动角色=Phase 2 引擎已就绪，但要在 Unity 里看需接输入/渲染（Phase 3+）。Unity MCP 仍未接入本会话。

**下次继续**：要么 T2.3b（让 common1.cns 真正跑起来），要么写 Unity demo 加载器（让你先看到动画），要么进 Phase 3（搓招+碰撞+命中）。

---

## 2026-06-02（深夜·续2）· Demo 加载器 + T2.3b 容错 + Unity MCP 诊断

- **T2.3b 运行期容错**（`提交`）：`WorldEvalContext` 加 alive/ishelper/roundstate + 未知 trigger/函数宽容降级为 0。
  测试：真实 common1.cns → 建角色 → 站立跑 30 帧不崩、稳定停 0（不自杀）。dotnet test 42/42。
- **Demo 加载器**（`提交`，表现层，未经编辑器验证）：`MugenCharacterView`（运行期解 AIR+SFF→Sprite→驱动 MugenSpriteAnimator，
  角色无关）+ `MugenDemoMenu`（菜单一键建 Terrarian/kfm demo 场景）。
- **Unity MCP 诊断**：`~/.claude.json` 配了 HTTP MCP `http://127.0.0.1:8080/mcp`，但探测返回 **503**
  （HTTP 进程在、Unity bridge 未就绪）；且 MCP 工具在会话启动时加载，本会话启动时未就绪 → 本会话无 Unity 工具。
  **解法**：① Unity 里把 MCP bridge/窗口启好（探测应返回 200）；② 重启 Claude Code 会话或 /mcp 重连 → 工具注册后我才能建场景/进 Play/截图。
- **KFM**：Ikemen 源码不含 kfm（在发行版里）；需用户把素材放 `../MugenSource/kfm/`，demo 加载器即可用。

**Unity 手动看效果（无需 MCP）**：打开工程→确认编译→菜单 `MUGEN/Demo/创建动画 Demo 场景 (Terrarian)`→Play。
改 `MugenCharacterView.AnimNo`：0=站立、20=走路。

---

## 2026-06-02（晚·续3）· Unity MCP 接通 + Terrarian demo 编辑器实测通过

- **Unity MCP 接通**：会话内可见 `mcp__UnityMCP__*` 工具，实例 `LockstepActDemo@31e0d1f6995b885f`（Unity 2022.3.55f1）。
- **修编译错误**（`fix(view)`）：`MugenCharacterView.cs:164` CS0120——`static BuildSprite` 引用实例字段 `PixelsPerUnit`。
  改为把 `PixelsPerUnit` 作参数传入 `BuildSprite`。`read_console` 确认 0 error（仅剩 KcpServerTransport 一个无害 CS0067 warning）。
- **Terrarian demo 编辑器实测**（开局检查②完成）：菜单建场景 `MugenDemo_Terrarian.unity` → Play →
  console 打出 `[MUGEN] Terrarian 动画 0：4 帧 / 3 张精灵 已加载` → 截图确认精灵渲染+播放。
  **整条导入管线在 Unity 内端到端跑通**：运行期解 AIR→读 SFFv1→PCX 解码→建 Sprite→MugenSpriteAnimator。
  表现层脚本"未经编辑器验证"的标记**清除**。
- **下一步**：开局检查 ③ 写 SFFv2 读取器（KFM）/ ④ Phase 3（命令系统+碰撞+命中）。

---

## 2026-06-02（晚·续4）· MUGEN 全动作展示工具 + 素材格式盘点

- **素材盘点**：`../MugenSource/` 只有 2 个角色——**Terrarian**(`Terrarian.sff`=**SFFv1**,57MB,复杂自定义角色)、**kfm**(`kfm.sff`=**SFFv2**,200KB,规整官方角色)。
  kfm 另有 intro/ending.sff(SFFv1,过场 CG,非角色)。`_reference` 是 Ikemen 源码。**故"全是 SFFv2"为否**：能跑的 Terrarian 是 v1，想要的 KFM 才是 v2 → 印证路③(SFFv2 读取器)必要性。
- **全动作展示工具**（`feat(view)`,commit `83b2b3a`）：
  - `MugenAnimShowcase`：按动画号顺序轮播角色全部动画（Terrarian **215 个**），每个 2s 自动切，←/→ 手动切、空格暂停，OnGUI 显进度。
  - 抽 `MugenSpriteLoader`(Open/BuildForAnim/Load，懒加载避免上千精灵一次性建) + `MugenAssetPaths`，`MugenCharacterView` 改为复用，消除重复。
  - 菜单加「创建全动作展示场景 (Terrarian/kfm)」；`SetupCamera` 提取共用 + 补 `clearFlags=SolidColor`（原纯色背景不生效）+ 正交拉近。
  - **修编辑器失焦冻结**：`runInBackground=false` 时编辑器无焦点 → Play 不 tick → 动画/轮播冻结（MCP 截图必现）。`execute_code` 实测 `frameCount` 卡在 1 证实；两个 demo view 的 Start 置 `runInBackground=true` 修复。
  - 验收：read_console 0 error；Play 后 execute_code 确认 frameCount 自增、runInBackground=True；截图确认渲染+轮播；**dotnet test 42/42 无回归**。

---

## 2026-06-02/03 · SFFv2 读取器(路③) + 多角色画廊 + 选错.sff 修复

- **素材盘点更新**：`../MugenSource/` 共 **12 个角色**——10 个新角色(Ananzi/Animus/Final/Gustavo/Hashi/Janos/Maxine/Noroko/Peketo/Shar-Makai)**全 SFFv1**、Terrarian(SFFv1,55MB)、kfm(SFFv2)。核实用户说法：KFM=1.0 单 st+storyboard、Terrarian=1.1 拆 st1-4+12 act，全库无 .zss(传统包)，均属实。
- **SFFv2 读取器**（`feat(import)`,commit `a4d4a01`）：`SffV2Reader` 解 v2 头(精灵/调色板头偏移+数量、ldata/tdata)、独立调色板库(RGBA→768RGB)、28 字节定长精灵头链、linked 复用；Lz5/Rle8/Rle5/raw 解码器移植自 Ikemen image.go，PNG(头像)逻辑层跳过。`SffTypes` 加 SffV2Sprite/SffV2File；`MugenSpriteLoader` 按版本字节自动分派 v1/v2 + 抽 BuildSpriteFromImage 共用。验收：**dotnet test 45/45**(+3)；Unity 内 KFM 站立(g0n0 47x106)**像素级完美**(白道服功夫男)。kfm 实况：239 Lz5+1 PNG+41 linked。
- **多角色分页画廊**（`feat(view)`,commit `52c8f54`）：`MugenGalleryShowcase` 扫全部 12 角色，一页 4 人并排站立循环、自动统一缩放同高、OnGUI 左右按钮+←/→ 翻页、懒加载。菜单「创建角色画廊场景(全部角色)」。3 页全部角色渲染正常，v1/v2 混排无误。
- **修隐藏 bug：选错 .sff**。kfm 文件夹有 ending/intro/kfm.sff 三个，原 `FirstFile` 按字母序错选 ending.sff(SFFv1 故事板)→拿 kfm.air 定义找精灵全错位成 240x120 乱图(那个"SUAVE DUDE"文字)。这也是之前单角色 kfm showcase 花屏的根因。新增 `MugenDef` 读 .def `[Files]` 段取 sprite=/anim=(主 def 优先 <文件夹名>.def 或含 cmd= 者)，三处 view 改用之。**修后 kfm renderer=47x106 g0n0 功夫男**。
- **已知遗留**：Janos(v1) 显示纯黑剪影，疑 SFFv1 外部 .act 调色板未应用，待查。
- **（2026-06-03 查实）Janos 黑剪影=非 bug**：g0n0 标准 8bit RLE PCX、形状正确，但源数据只用 3 索引(0/254/255)、122x173 仅 1407 字节(15:1 压缩)→源美术本就是扁平剪影(对比 Maxine 13/Ananzi 22 索引)。真实次要局限=未加载外部 .act 调色板(共享板取文件首个非空=g9000 头像板)；加载 janos1.act 也只把黑剪影变品红(255→234,0,255)、救不回细节，判定不值得做。完整方案=外部.act+samePalPrev 链，Phase 3 不依赖，日后再补。
- **下一步**：③→④ 完成，进 **Phase 3**（命令系统+Clsn碰撞+命中=第一次真打中人）。

---

## 2026-06-03 · Phase 3 启动：T3.1 搓招匹配（commit `fe74e11`）

- **接手导航**：重写 `CLAUDE.md §8` 为文档地图（规约/架构/计划/日志/记忆/测试入口各在哪）+ 已完成实况 + 当前任务，供其他 agent 接手（commit `5d6f353`）。
- **T3.1 完成**：`CommandInputC`(方向+按钮 60 帧环形缓冲) + `CommandMatcher`(纯逻辑：方向按朝向转相对 + 序列贪心匹配，末符号当前帧边沿触发) + `CommandSystem`(每帧推输入+逐条匹配→写 `CommandStateC.Active`)。**dotnet test 54/54**(+9)。
- **关键事实留痕**：`FrameInput` 方向在 MoveX/MoveY 不在 Buttons；`InputBufferC`(6帧仅按钮,cancel 用)不够搓招→另建 `CommandInputC`；朝向取 `TransformC.FacingX.Raw`。
- **T3.1 后续增量（未做）**：`command="name"` 字符串 trigger 接入表达式 VM（需 VM 支持字符串字面量，见 T2.3b 遗留）；MUGEN 引擎 live pipeline 尚未组装（系统现由 dotnet 测试驱动，与 Phase2 一致），组装时 CommandSystem 须排在 MugenStateMachineSystem 前。
- **下一步**：T3.3 CollisionSystem（Clsn1×Clsn2→HitEvent，ClsnWorld 已有）/ T3.2 HitDef controller。T3.4 依赖二者。

## 2026-06-03 · Phase 3 收官（T3.2/T3.3/T3.4，dotnet test 64/64）

- **T3.3 碰撞**（commit `63ef930`）：`ClsnWorld.AnyOverlap`(Clsn1×Clsn2 按 facing 镜像求重叠) + `PendingHitC`(受击方待结算命中) + `CollisionSystem`(攻击方 HitDefStateC.Active+当前帧 Clsn1 × 受击方 Clsn2→写 PendingHit + HitTargetsBits 防一招多次命中)。+5 测试。
- **T3.4 命中**（commit `53f5788`）：`HitSystem` 消费 PendingHitC——扣血(钳0)、受击方硬直 PauseTimeDefender、击退(GroundVelocity 按攻击方朝向镜像)、切 MoveType=BeingHit+PendingStateNo=5000、攻击方 hitstop。+3 测试。
- **T3.2 HitDef 控制器**（commit `5c97b0e`）：`StateControllerExecutor` 加 `case HitDef`(激活 HitDefStateC+清目标位)；`MugenStateMachineSystem.ApplyTransition` 切状态停用 HitDef。+2 测试。
- **Phase 3 完成**。系统执行顺序(组装 live pipeline 时)：InputBuffer/Command → StateMachine → Physics → Anim → Collision → Hit。
- **诚实边界/留待**：HitDef 完整 attr/hitflag/守招判定、击退/hittime 与 Ikemen 差分对齐、`command="name"` 字符串 trigger 接入 VM、MUGEN 引擎 live pipeline 组装（现仍测试驱动）——均 Phase4+ 或后续增量。
- **下一步**：Phase 4（连段/受击/击倒/格挡 + 更多 controller），见 `Docs/细化计划.md`。

---

## 2026-06-03 · 重大方向变更：转 Ikemen GO 1:1 移植（定点化）

- **决策**：clean-room 简化子集（12 controller/贪心搓招/基线命中）**无法兼容真实 MUGEN 角色**（蓄力/负边缘/防御/hitflag/受击状态机全缺）。改为**把 Ikemen 战斗系统逻辑 1:1 忠实移植**（float→定点 FFloat，挂回滚底座），目标=直接吃别人角色文件战斗+保回滚。
- **法律核实**：Ikemen GO 源码 **MIT 许可**（LICENCE.txt），移植合法，保留版权头即可。此前担心的 GPL 传染系误判。
- **规模实测**：核心战斗 ~5 万行 Go（char.go 14k/bytecode.go 15k/compiler 15k/input 3k）。128 controller、HitDef 130 字段、200+ trigger。→ 多会话多 agent 工程。
- **最高纲领文档**：新建 `Docs/移植方案_Ikemen.md`（架构/float→定点契约/Unity 目录/M0-M11 移植顺序+进度表/卡点登记/MIT 文件头模板）。`CLAUDE.md §0/§8` 已同步指向它；旧 `细化计划.md` 降为留史。
- **不可消除约束**：定点≠浮点 → **位级 1:1 不可能**；追求**逻辑 1:1**（离散量与 Ikemen 全等、连续量容差）。
- **M0 起步**（commit 见下）：新引擎根 `Assets/Logic/Mugen/`（镜像 Ikemen 模块）。移植 `BytecodeValue`(定点版)：int 用 long、float 用 FFloat 拆存；**ToI 实现向零截断**对齐 Ikemen `int32(float)`（实测 FFloat.ToInt 是 floor，负小数差 1，已记卡点）。dotnet test **70/70**(+6)。
- **下一步**：M1 表达式字节码 VM（BytecodeExp OpCode 执行器）。退役旧 clean-room 战斗码在移植件取代后进行。

## 2026-06-03 · M1 表达式字节码 VM（dotnet test 87/87）

- `Mugen/Expr/OpCode.cs`：155 个顶层 OpCode 枚举（照搬 Ikemen 顺序，字节值一致）。
- `Mugen/Expr/BytecodeOps.cs`：值运算移植（add/sub/mul/div/mod/pow/比较/位/逻辑/区间/三角/取整/neg/abs/not）。**类型规则照搬**：Min(a.type,b.type)==Float 走浮点否则整数；除零/模零/非法对数→Undefined。
- `Mugen/Expr/BytecodeExp.cs`：栈式字节码执行器。OC_float 编码携带 8 字节 FFloat raw（偏离 Ikemen float32，已记卡点，M2 须遵此）。trigger/redirect opcode 走 `IExprContext` 钩子（M3 接）。短路跳转未做（纯表达式两侧求值结果一致；ifelse 用 OC_ifelse 无需跳转）。
- `Framework/Math/FMath.cs` 补 Acos/Atan/Asin/Ln/Pow/Exp（Asin=π/2−Acos，Exp=Pow(e,x)，e 用 Q31.32 raw 常数）。
- 测试：BytecodeValue 6 + BytecodeOps 9 + BytecodeExp 8 = M0+M1 共 +23（总 87/87）。
- **下一步**：M2 编译器（compiler.go：CNS trigger 字符串 → BytecodeExp，按 trigger 增量移植）或 M3 Char（先有 Char 才能接 trigger opcode 钩子让表达式读实际值）。建议先 **M3 Char 骨架**再回 M2，使表达式能读 Time/StateNo/Pos 等跑通端到端。

## 2026-06-03 · M3 Char 骨架（dotnet test 95/95）

- `Mugen/Char/Char.cs`：`MChar`（避开 System.Char 命名）——移植 Ikemen Char+StateState 核心字段（定点）：Time/StateNo/PrevStateNo/StateType/MoveType/Physics/Ctrl/AnimNo/Life/Power/Juggle/Hitstop/Pos/OldPos/Vel/Facing + var(n)/fvar(n) 字典。
- **实现 `IExprContext`**：把常用 trigger opcode 接到 Char 取值——time/stateno/prevstateno/statetype/movetype/ctrl/anim/pos_x|y/vel_x|y|z/facing/life/lifemax/power/powermax/alive/var/fvar。**至此 M1 表达式 VM 能读真实 Char 数据**，`time >= 2` 端到端跑通。
- **Clone()+WriteHash()**：深拷字典、哈希按 key 升序，接现有回滚/Desync 底座。
- 未接 trigger（gametime/animtime/redirect/p2…）默认 Undefined，增量补。StateType/MoveType 暂用 int32 原始码（枚举映射归 M2）。
- **下一步**：M2 编译器（compiler.go → 把 CNS trigger 字符串编译成 BytecodeExp）。有了 M2，真实角色 CNS 的触发条件就能编译执行；之后 M4 状态机 + M5 controllers。

## 2026-06-03 · M2 表达式编译器核心（dotnet test 104/104）

- `Mugen/Expr/MugenExprCompiler.cs`：tokenizer + 递归下降，**优先级链照搬 Ikemen** compiler.go（expBoolOr→BoolXor→BoolAnd→Or→Xor→And→Eqne→Grls→AddSub→MulDiv→Pow→Unary→Value，低→高）。
- 支持：int/float 字面量、括号、一元(- ! ~)、算术(+ - * / % **)、比较(= != > >= < <=)、逻辑(&& || ^^)、位(& | ^)、无参 trigger(time/stateno/life/ctrl/alive/facing/anim…)、轴 trigger(pos x/vel y…)、函数(abs/floor/ceil/exp/ln/sin/cos/tan/acos/asin/atan/log/ifelse/cond)、var(n)/fvar(n)。
- **端到端**：`"life > 0 && ctrl"`、`"ifelse(time>2,100,200)"`、`"pos x"` 编译→字节码→对 MChar 求值，优先级正确。
- 修 bug：文本小数解析原"逐位×(1/10)"累加导致 0.5 带误差(1/10 定点不精确)→改"小数分子÷10^位数"一次除法(0.5/0.25 精确)。
- **待补**(记进度表)：statetype=S 字母枚举操作数、`=[a,b]` 区间语法、字符串 command=、redirect(p2/parent)、const(...)、更多 trigger。
- **下一步**：M4 状态机（StateBytecode + state runner，跑 StateDef 的控制器；依赖 M2 trigger 求值）+ 渐进补 M2 待补项。

## 2026-06-03 · M4 状态机 MVP（dotnet test 108/108）

- `Mugen/State/MStateDef.cs`：`MStateDef`（状态：头部 type/movetype/physics/ctrl/anim + 控制器列表）+ `MStateController` 基类（Trigger BytecodeExp + Run→是否 ChangeState）+ `ChangeStateController`。
- `Mugen/State/MStateMachine.cs`：`RunFrame`——hitstop>0 冻结；跑当前状态控制器（trigger 过则 Run）；ChangeState 设 PendingStateNo→ApplyTransition（设号/time=0/应用 statedef 头）→**同帧从头重入**（有界 16 次，对应 Ikemen changeStateEx 的 for c.stchtmp）；末 Time++。
- `MChar` 加 `PendingStateNo`（入 Clone/WriteHash）。
- **全链路集成验证**：用 M2 编译 `"time >= 2"` 等真实触发条件驱动状态机，stand↔walk 按时切换、statedef 头生效、hitstop 冻结——**M1+M2+M3+M4 串通**。
- **待补**：StateBlock 的 triggerall/trigger1..n 编译合并、loop/persistent/ignorehitpause、负状态(-1/-2/-3)、SelfState、common states 加载。
- **本会话移植里程碑**：M0✅ M1✅ M2🔄核心 M3🔄骨架 M4🔄MVP。**下一步 M5 controllers**（VelSet/PosAdd/ChangeAnim/CtrlSet/VarSet/HitDef… 逐个移植，挂到 MStateController）。

---

## ▶▶ 重启后开局指引（RESUME，2026-06-02 20:05）

**一句话状态**：Phase 0/1/2 全部完成，`dotnet test` **42/42 绿**，全部已提交。下面是重启新会话后要做的。

**怎么继续验证**：
```
cd D:/Desktop/demo/LockstepActDemo/Tests/Lockstep.Logic.Tests
dotnet test            # 应 42/42 通过
cd D:/Desktop/demo/LockstepActDemo && git log --oneline | head   # 看进度
```

**已完成（都在 git）**：
- Phase 0 地基：测试工程 + schema + 表达式 VM + 组件 + 黄金哈希。
- Phase 1 导入：AirParser / SffV1Reader+PcxDecoder(**仅 SFFv1**) / AnimAdvance / ClsnWorld。
- Phase 2 引擎：表达式 VM(逻辑/函数/带参) + WorldEvalContext + MugenStateC + StateControllerExecutor(12 控制器) + MugenStateMachineSystem + MugenPhysics/AnimSystem + CmdParser + CnsParser(容错)。数据驱动引擎跑通(CNS→数据→状态机+物理+动画)。
- T2.3b 部分：运行期未知 trigger 容错；common1.cns 站立可跑 30 帧不崩。
- Demo 加载器：MugenCharacterView + MugenDemoMenu（**Unity，未在编辑器验证编译**）。
- Tencent 规范自查重构完成。

**素材**：`../MugenSource/Terrarian`(SFFv1，能用)、`../MugenSource/kfm`(**SFFv2，当前读取器不支持**)。`../MugenSource/_reference/Ikemen-GO`(只读 Oracle)。

**Unity MCP**：已 register + server 跑起来(30 工具，HTTP `127.0.0.1:8080/mcp`)。重启会话后应能看到 `mcp__*unity*` 工具。

**下次开局 4 件事（按序）**：
1. 确认 Unity MCP 工具可用 → 实测 Terrarian demo（建场景/Play/截图）+ 确认表现层脚本编译。
2. 写 **SFFv2 读取器**让 KFM 能用（参考 Ikemen `src/image.go` 977/1176/1027 行；fmt 0/2/3/4/10-12；Rle/Lz5 纯 C# 可测，PNG 走 Unity）。
3. T2.3b 余：枚举 token 翻译、字符串 command、带点号 const、p2 重定向。
4. Phase 3：命令系统(输入→搓招匹配) + Clsn 碰撞 + 命中（第一次真正打中人）。

**当前测试数 42**；新引擎与旧 PlayerStates 栈并行，旧栈接场景时再切除。






---

## 2026-06-03 续：测试按模块分目录 + M1–M4 补全到预期级别（TDD）

**测试工程治理**：发现测试文件平铺（旧 clean-room 18 个在根、新 Mugen 件平铺在 `Mugen/`）。
按"每模块一子目录镜像源码树"重组：`Mugen/Expr` `Mugen/Char` `Mugen/State`（`git mv` 保历史），
并把该约定写入 `CLAUDE.md §2`（强制条款 + 旧平铺测试"删一退一"策略）。commit `e04b716`。

**M1–M4 逐模块 TDD 补全（每块先写失败测试→实现→全绿→commit）**：
- **M1**（`3ba75b4`）短路跳转：`BytecodeExp.Run` 实现 OC_jmp/jz/jnz + 8bit + jsf8，peek(不pop)语义、
  8bit(`==0→末尾`,否则`i+off+1`)/32bit(`i+len+4`)编码照搬 Ikemen。113/113。
- **M3**（`d91ab38`）redirect 机制 + Char 扩展：VM 引入 oc/cur 双上下文 + 每迭代重置 + OC_run/nordrun/rdreset，
  实现 root/parent/p2/target 重定向；MChar 加 Id/P2/Root/Parent/Targets（Clone 浅拷结构引用待 World 重链、
  Hash 不递归被引者）；CharSystemVar 常用字段+trigger；MGetHitVar 受击容器(Clone/Hash)。121/121。
- **M2**（`6a67038`）编译器：statetype/movetype 字母枚举比较(S/C/A/L,OR,!=,发 opcode+1字节掩码)、
  `=[a,b]`/`(a,b]` 区间语法、redirect 编译(p2/root/parent 用 OC_run 包裹)。127/127。
- **M4**（`9584849`）状态机：MTriggerSet(triggerall + trigger1..n, OR-of-ANDs)、persistent(1/0/N)、
  ignorehitpause、负状态 -3/-2/-1 每帧跑(移植 actionRun 顺序)、SelfState、common states 回退。135/135。

**当前 dotnet test 135/135 绿**。M0–M4 全部 ✅（见移植方案 §4）。M5(StateControllers，依赖 M2+M4) 已解锁。

**诚实边界（已在 §4 标注待补）**：command=(M6)、const()/角色常量(M9)、gethitvar opcode 与 HitDef 130 字段(M7)、
嵌套 StateBlock if/else 树·loop·for、p2statetype/prevstatetype/target 编译、persistent=N 精确节奏。

**待 Codex 复核后开 M5。**

---

## 2026-06-03 续2：治理修复(Codex 复核阻塞项) + 开 M5 第一批

**Codex 复核发现阻塞项(已全修, commit 见下)**：
- 旧平铺测试删除当时只 stage 了新分目录路径(`git reset`+`add 新路径`漏了旧路径删除)→ HEAD 双份同名测试类，
  干净检出会编译失败。已 `git add -A` 真正提交 6 个旧文件删除；新建 worktree 干净检出验证：构建通过、0 失败、无重复。
- `GetHitVar.cs.meta` 漏跟踪 → 已提交。`BytecodeExp.cs` 顶部"M1 未实现短路跳转"过期注释 → 已修。commit 治理项一条。
- 经验教训：`git mv` 后**不要** `git reset` 再只 `add` 新路径；要么直接提交 mv 的暂存，要么 `git add -A` 把删除一起带上。

**M5 第一批 基础控制器(commit `a06ebd7`, dotnet test 146/146)**：
`Mugen/StateCtrl/BasicControllers` = Null/VelSet/VelAdd/PosSet/PosAdd/ChangeAnim/CtrlSet/VarSet/VarAdd/StateTypeSet。
朝向语义逐一对照 Ikemen char.go：PosAdd X 乘 facing(addX)、PosSet 绝对(setPosX)、VelSet 存原值(facing 积分时应用)。
VarSet/VarAdd 与表达式 VM 闭环。控制器无内部可变状态(参数=配置)。测试落 `Mugen/StateCtrl/`。

**M5 待续**：其余战斗控制器(HitBy/AssertSpecial/Width/Turn/...)、把控制器接入 CnsParser(`[State N] type=` 实例化)、HitDef 归 M7。

---

## 2026-06-03 续3：Codex 复核 M5 + 记录当前项目状态

**复核结论**：上次指出的测试治理阻塞项已修复。`dfe7f81` 真正删除旧平铺测试、补 `GetHitVar.cs.meta`、修 `BytecodeExp.cs` 过期注释；`git ls-files Tests/Lockstep.Logic.Tests/Mugen` 只剩按模块分目录的测试文件，不再有干净检出双份同名类风险。

**M5 验收**：M5 可按"核心 StateControllers + CNS 接入"验收通过，但不能表述为 ~90 个控制器全量完成。
- `a06ebd7`：基础控制器 Null/VelSet/VelAdd/PosSet/PosAdd/ChangeAnim/CtrlSet/VarSet/VarAdd/StateTypeSet。
- `431edc3`：LifeAdd/LifeSet/PowerAdd/PowerSet/Turn/AssertSpecial，并补 `MAssertFlag` 与帧首清空断言标志。
- `c736b13`：`MugenCnsParser` 将 `[State N] type=` 实例化到上述控制器，CNS 文本可端到端驱动 `MStateMachine`。
- 覆盖测试：`Mugen/StateCtrl/BasicControllersTests.cs`、`Mugen/StateCtrl/StatControllersTests.cs`、`Mugen/Parse/MugenCnsParserTests.cs`。

**验收命令**：`cd Tests/Lockstep.Logic.Tests && dotnet test`。普通权限仍会遇到 NuGet 临时锁权限问题；提升权限重跑通过：**167/167 passed**。

**当前项目状态**：
- M0-M4：完成并通过复核。
- M5：核心完成，已解锁后续；边界仍是 HitDef→M7，PlaySnd/Explod/AfterImage/HitBy/Width 等长尾控制器待后续增量。
- M6：已出现核心提交 `a8ace2c`（输入缓冲 + CMD 键解析 + 匹配测试），但当前工作树另有 `Assets/Logic/Mugen/Command/MCommandList.cs`、`Assets/Logic/Mugen/Parse/MugenCmdParser.cs` 未跟踪；本地 167/167 已包含它们，提交前不得视为完整 ledger。
- 当前工作树仍不干净：`Docs/移植方案_Ikemen.md` 有本次状态更新；另有上述 M6 未跟踪文件。

---

## 2026-06-03 续3：M5-M7 完成（TDD，dotnet test 180/180）

承 /goal「制作到 M5-M7 都完成并验收通过」，按相同标准逐模块 TDD + Ikemen 对照 + 每单元 commit。

**M5 StateControllers（核心完成）**
- b1 `a06ebd7`：BasicControllers = Null/VelSet/VelAdd/PosSet/PosAdd/ChangeAnim/CtrlSet/VarSet/VarAdd/StateTypeSet。朝向语义照搬 Ikemen char.go（PosAdd X 乘 facing、PosSet 绝对、VelSet 存原值）。146/146。
- b2 `431edc3`：StatControllers = LifeAdd(kill/clamp)/LifeSet/PowerAdd/PowerSet/Turn/AssertSpecial(MAssertFlag 每帧清)。153/153。
- b3 `c736b13`：MugenCnsParser —— [Statedef]/[State] → MStateDef + 实例化 18 类控制器 + 编译 triggerall/triggerN，未知控制器降级 Null。CNS 文本端到端驱动状态机。159/159。

**M6 命令系统（完成）**
- 核心 `a8ace2c`：MCommandModel(输入位+环形缓冲 Clone/Hash) + MCommandParser(~释放/数字蓄力//按住/$4way/+AND/>严格；方向大写 B/F/U/D+对角，按钮小写) + MCommandMatcher(末步当前帧边沿触发 + 向前贪心 + 朝向相对 B/F + 蓄力连按 N 帧 + 释放边沿 + $子集)。167/167。
- 集成 `ecb2b13`：MugenCmdParser(.cmd [Command] 段) + MCommandList(每帧 Update/IsActive/Clone/Hash) + 编译器字符串字面量词法 + command="name"/!= → OC_command+[长][ASCII名] + MChar 持 CommandList 求值。173/173。

**M7 命中系统（核心完成 `6c6e593`）**
- MHitDef(核心字段) + MClsn(Clsn1×Clsn2 AABB 朝向镜像重叠) + HitDefController(激活+清 targets+movetype=A) + MHitSystem.TryHit(hitflag 校验受击姿态 + 重叠检测 + 结算：扣血/守方转向/击退速度/双方 hitstop/GetHitVar 填值/守方进 5000+movetype H+失控/攻方 movehit·hitcount·target 登记/同招对同目标只命中一次) + MugenCnsParser 接入 type=HitDef。
- MChar 加 HitDef/Clsn1/Clsn2(帧派生浅拷) + Clone/Hash。180/180。

**M0-M7 全部 ✅（见移植方案 §4）。当前 dotnet test 180/180 绿。**
**诚实边界**：M5 更多控制器(HitBy/Width/Explod/PlaySnd…)、M6 '|'OR 逻辑、M7 完整 130 字段/守招格挡/projectile/多段命中/gethit 5000-5150 数据态、gethitvar(...) opcode 接入，均列为后续。下一步建议 M8(Anim/SFF 对接) → M9(角色加载+差分对账 Ikemen)。

## 2026-06-03 续5：修复诚实边界遗留（修复1-4，dotnet test 200/200）

承 /goal「修复到修复5为止 + 大厂面试官标准自审 + 全测试绿」，逐项补齐 M5-M7 列为后续的边界。

- 修复1 gethitvar `6aa6fec`：接入 gethitvar(...) 触发器，编译 OC_ex_ + 字段 id，MChar 从 Ghv 读。184 绿。
- 修复2 命令 OR `c4cb4bc`：命令支持 '|' 逻辑（步内任一键满足即过）。186 绿。
- 修复3 p2statetype `c10b859`：编译 p2statetype/prevstatetype 字母枚举比较。189 绿。
- 修复4 守招 + HitBy/NotHitBy（本条）：
  - Char 加 Guarding/HitByAttr/HitByTime/HitByIsNot（含 Clone/WriteHash）。
  - MHitDef 加 MAttackType 攻击类别位标志枚举 + Attr + guardflag(GuardHigh/Low/Air) + guard.hittime/ctrltime/velocity（含 Hash）。
  - MHitSystem：HitBy/NotHitBy 属性免疫过滤（HitByTime>0 时按 Attr bitmask 与 IsNot 决定放行）+ guardflag 守招结算 ApplyGuard（chip 伤害 / 守方击退用 guard.velocity / ghv.guarded / 攻方 moveguarded，不进受击 5000、不失 movetype）。
  - MStateMachine.RunFrame：HitByTime 每帧递减（hitpause 冻结，不降为负），递减在控制器执行之前。
  - StateCtrl：HitByController（HitBy/NotHitBy 共用，IsNot 区分）。HitDefController.CopyInto 补拷新字段（Attr/Guard*），否则解析值传不到运行期。
  - MugenCnsParser：BuildHitDef 解析 attr/guardflag/guard.*；BuildController 加 hitby/nothitby 分支；ParseAttr/AttackTypeCode 解析 `<statetype>, <攻击类别>` 串为 MAttackType bitmask。
  - 测试 `Mugen/Hit/GuardAndHitByTests.cs`：守招 chip/击退/不进 5000、guardflag 不匹配回落整命中、HitBy/NotHitBy 放行与拦截、HitByTime 递减与 hitpause 冻结、CNS 端到端（guardflag/guard 字段、HitBy、NotHitBy）。

**dotnet test 200/200 绿**（黄金哈希通过，MChar 新增字段已纳入 WriteHash 未破回归）。
**修复5 未开工**：const(...) 角色常量、enemy redirect、persistent=N 节奏核对（对照 Ikemen 倒计时）。

---

## 2026-06-03 续6：修复5 完成（const / enemy redirect / persistent 帧冷却，dotnet test 215/215）

承 /goal「完成修复5为止 + 大厂面试官标准自审 + 全测试绿」，把修复3-5 的诚实边界三项补齐。

- **修复5-1 const(...) 角色常量**（`06cf718`）：新增 `MConstId`(常量字段 id 枚举) + `MConstants`(MChar 不可变常量集，默认值对齐 Ikemen init) + `MugenConstParser`(解析 [Data]/[Size]/[Velocity]/[Movement] 段，jump.neu.y→velocity.jump.y 等键映射对齐 Ikemen)。编译器 `const(field)` 发 `OC_const_`+id 字节，`MChar.ReadTrigger` 从 Constants 读（无常量集容错返 0）。Constants 引用 Clone 浅拷、WriteHash 不混入（不可变配置）。
- **修复5-2 enemy/enemynear redirect**（`c5ea674`）：`MChar.Redirect` 接 `OC_enemy/OC_enemynear`，1v1 中索引 0→P2 其余→null；编译器 `EmitEnemyRedirect` 解析可选 (index) + 子值（OC_run 在敌人上下文求值），须在函数调用判断前处理以免 enemy(0) 被当函数。
- **修复5-3 persistent 改 Ikemen 帧冷却模型**（`5f03b16` + 续 `375736b`）：重写 `MStateMachine` persistent gate——每帧到达控制器先递减计数器，>0 跳过（不求值 trigger），<=0 时 trigger 过才执行并重置为 persistent 值（对齐 Ikemen StateBlock.Run + ctrlsps 倒计时）。归一对齐编译期：1 或 >128→1；<=0→锁定哨兵（进状态仅一次）；其余=N。计数器改 (状态号,控制器序号) 复合键消除负状态/当前/重入串扰，进状态只清该状态范围。续提交进一步修正：控制器触发 ChangeState 时**不**重置旧状态计数器（Ikemen 仅在控制器返回 false 时于 StateBlock.Run 末尾重置）。

**dotnet test 215/215 绿**（黄金哈希通过；新增 ConstTests/enemy/persistent 共约 15 例）。
**M0-M7 + 修复1-5 全部 ✅。诚实边界（后续）**：const 仅覆盖真实角色常用子集（非 200+ 全量）；enemy redirect 仅 1v1（多人/helper 未做）；负状态的 persistent 重置时序与当前状态略有差异（负状态在切状态前即 ResetPersist），真实角色负状态少用 persistent+ChangeState，列为待核。下一步建议 **M8（Anim/SFF 对接）→ M9（角色加载 KFM + 差分对账 Ikemen）**——把已成型的 Expr/State/Ctrl/Command/Hit 子系统组装成 live pipeline，第一个可玩里程碑。

---

## 2026-06-04 续7：M8 动画推进系统完成（移植 Ikemen Animation.Action，dotnet test 229/229）

承 /goal「先简单重构 → 推进 M8（TDD）→ 通过后汇报讨论 M9」。先做零风险微重构再推进 M8。

**前置微重构（commit 5b12904，215 绿）**：抽 `Mugen/Parse/MugenCodes` 集中 statetype/movetype/physics 字母码、攻击类别二字母码、attr 合成、hitflag/guardflag 标志解析；MugenCnsParser 删 6 个私有码表方法改委托，guardflag/hitflag 两段重复解析合并为 MugenCodes.HitFlags（501→约430 行）。纯内部、行为不变。

**M8（commit 7f80357，229 绿）**：
- `Mugen/Anim/MAnim`：引擎自有动画数据 MAnimFrame/MAnimData，Clsn 用 MClsnBox（与命中系统一致，免运行期转换）；`ComputePacing` 预算 TotalTime/LoopTime/PreLoopTime（移植 anim.go 行 337-363）。
- `Mugen/Anim/MAnimSystem`：`Action` 逐 tick 推进，**literal 移植 Ikemen Action() 的 next 闭包**——跳零时长帧、永久帧停末元素、末尾回绕 loopstart、curtime 到 totaltime 时回绕到 totaltime-looptime。派生 trigger 量 AnimElemNo(1-based) 与 AnimTime(=curtime-totaltime，惯例≤0)，每 tick 填当前帧 Clsn1/Clsn2 供命中系统。动画号变化（ChangeAnim/statedef anim=）由系统自检 AnimNo!=AnimRunningNo 重置，不需控制器介入。无静态可变态、运行态全写回 MChar 接回滚。`Play` 复位不推进。
- `Mugen/Anim/MAnimImport`：AIR(Game.Data.AnimData，AirParser 输出)→MAnimData 桥接，新引擎对共享导入层的**唯一耦合点**（单向、加载期，便于日后随 AnimData 上移）。
- MChar 增 AnimElem/AnimElemTime/AnimCurTime/AnimLoopEnd/AnimRunningNo 五个运行态字段，全部纳入 Clone/WriteHash。
- TDD：`Tests/.../Mugen/Anim/MAnimSystemTests` 14 例（节拍量/单双帧推进/循环回绕/永久帧停留/跳零时长帧/切动画重置/Clsn 填充/Play/null 容错/AIR 导入/克隆哈希独立）。先红（一处测试算错 totaltime）后绿。

**dotnet test 229/229**。**M0-M8 + 修复1-5 全部 ✅。**
**诚实边界（后续）**：AnimElemTime(n)/AnimElemNo(time) 复杂查询未做（当前只给简单情形的 AnimElemNo=curelem+1）；interpolate/scale/blend/翻转为纯表现（M11）；SFF 像素加载属表现层（M11）；本里程碑只做逻辑层动画推进与 Clsn 派生。
**下一步**：按计划暂停，向用户汇报 M8 + 讨论 M9（角色全链路加载 KFM + 黄金哈希迁到 MChar + 旧 clean-room 引擎退役）。

---

## 2026-06-04 续8：M9.1 live pipeline 装配 + 角色加载，真实 KFM 跑通（dotnet test 235/235）

承用户选择「先接 Unity 渲染看角色动起来 + M9 内做真差分对账」。但要"看到动"或"对账"，前提是先把散装子系统组装成能跑的引擎并能吃真实角色文件——故 M9 从此起步（commit 9c14d6f）。

- `Mugen/Parse/MDefParser`：解析 .def [Files] 段（cmd/cns/st*/stcommon/sprite/anim），逻辑层不做 IO。
- `Mugen/Battle/MCharData`：角色静态配置（状态/公共状态/动画表/命令/常量），加载后只读。
- `Mugen/Battle/MCharLoader`：从 CNS/CMD/AIR 文本装配 MCharData（复用既有解析器 + MAnimImport）；`SpawnChar` 建挂好配置的运行期 MChar。
- `Mugen/Battle/MBattleEngine`：每帧 pipeline = 命令→状态机→物理→动画(M8)→命中，顺序对齐 CLAUDE §8.3；ComputeHash 覆盖全角色接快照/回滚。**物理目前仅最小速度积分（pos+=vel，X 乘朝向）**，重力/摩擦/地面夹取列为 M9 物理完善 TODO。
- 命名避坑：模块命名空间用 `Lockstep.Mugen.Battle`（不能叫 System，否则遮蔽 BCL System 致 [System.Flags] 编译失败）。
- 测试基建修复：`TestAssets` 改为锚定自身文件目录（DemoDir），不再依赖 [CallerFilePath] 调用方深度——之前嵌套子目录的新测试会解析错素材路径而误跳过真实素材测试。
- TDD：`Tests/.../Mugen/Battle/MBattleEngineTests` 6 例（.def 解析/多状态文件/装配/单角色推进+物理位移/逐帧确定性哈希/**真实 KFM 加载并连跑 60 帧不崩、动画推进、未自杀**）。

**dotnet test 235/235（0 跳过）。M0-M8 + 修复1-5 + M9.1 ✅。**
**诚实边界/下一步**：
1. **M9 物理完善**：当前最小积分，缺重力/摩擦/地面 y=0 夹取/PlayerPush——"走路/跳跃手感"要靠这步。
2. **公共状态**：KFM 测试只载 kfm.cns（招式态），未载 common1.cns（站立/行走在公共态里，KFM 目录无 common1，需从引擎 data 供给）。差分对账前要把 common1 接上。
3. **M9.2 Unity 渲染**：把 MBattleEngine → MugenSpriteAnimator/View，编辑器里看 KFM 动（依赖用户启动 Unity MCP 桥接 8080 + 重启会话）。
4. **M9.3 真差分对账**：需在 Ikemen 跑出 KFM 逐帧状态/动画序列存 Golden，新引擎离散全等——须先确认 Ikemen 可 headless 抽 trace（Go 构建可行性待查）。

---

## 2026-06-04 续9：M9 物理(摩擦/重力) + 接入 common1 公共状态（dotnet test 240/240）

承用户「把物理完善 + 接 common1 两块纯逻辑做掉，然后写文档，明天重启做剩下的」。参考 Ikemen char.go（非 MCP）。

- **物理（commit 97904e3，移植 char.go posUpdate/gravity）**：新增 `Mugen/Battle/MPhysics`——位置积分（pos+=vel*facing X / vel Y）+ 按状态 physics 类型：S/C 地面摩擦（vel.x*=friction，绝对值低于阈值归零）、A 重力（vel.y+=yaccel）、N 不施加。**落地/地面夹取不在物理里**，MUGEN 由公共状态 trigger 检测，忠实照搬。`MConstants` 增 Stand/CrouchFrictionThreshold + 解析 [Movement] 对应键（MUGEN 1.0）。`MBattleEngine` 第 3 步改用 MPhysics.Step。
- **公共状态（同 commit）**：`MCharLoader.SpawnChar` 入场应用初始 [Statedef] 头部（type/movetype/physics/ctrl/anim），对齐 MUGEN 入场 changeState——否则直接置 StateNo 不经转换，physics=S 等头部不生效。公共状态加载链路打通（Load 的 commonText→CommonStates→引擎传状态机回退）。**KFM 目录无 common1.cns**（MUGEN 引擎自带；Ikemen 用 .zss 脚本格式，我们吃 CNS），借 Terrarian 的标准公共状态。
- **TDD**：`MPhysicsTests` 4 例（空中重力累加/站立摩擦衰减并归零/N 不变/朝向积分）；`MBattleEngineTests` 加真实 KFM+common1（公共态含 Statedef 0/20、入场 physics=S/type=S、stand.friction 载入、连跑 60 帧不崩）。

**dotnet test 240/240（0 跳过）。M0-M8 + 修复1-5 + M9.1 + M9 物理/公共状态 ✅。**
**文档**：新增 `Docs/M9_进度与续作.md`（M9 状态快照 + 明日续作导航：M9.2 Unity 渲染、物理/状态补全、M9.3 Ikemen 差分对账可行性、黄金哈希迁移/旧引擎退役）。
**明日重启续作**：见 `Docs/M9_进度与续作.md §2`。第一步建议 = 用户启 Unity MCP 桥接 8080 + 重启会话 → 做 M9.2 渲染（先看 KFM 站立/行走），同时调研 Ikemen headless 抽 trace 可行性（go version）。

---

## 2026-06-04 续10：M9.2 接 Unity 渲染——新引擎首次在编辑器里跑出真实 KFM

承用户「连 Unity MCP 继续昨天未完成工作」。Unity MCP 桥接已起（LockstepActDemo@31e0d1f…，2022.3.55f1），M9.2 前置满足。

- **表现层 live 入口（`Assets/Scripts/Game/View/MugenLiveView.cs`，Assembly-CSharp，非逻辑层）**：MonoBehaviour 把 `MBattleEngine` 接进 Unity。Start 做文件 IO（读 kfm.cns/.cmd/.air + 借 Terrarian/common1.cns）→ `MCharLoader.Load`/`SpawnChar` 装配纯定点引擎；`MugenSpriteLoader.Open` 开 SFF、按动画号懒构建精灵。Update 按 60 tick/s 定步累加：采键盘 → `MInput` → `engine.Tick` → 读 `MChar.AnimNo` + 当前帧 `MAnimFrame(SpriteGroup,SpriteImage)` 取精灵摆位。**定点→float 仅在此边界转换**（pos/PPU，MUGEN Y 向下取负）。OnGUI 叠 StateNo/AnimNo/Elem/Pos/Vel 调试。
- **编辑器菜单**：`MUGEN/Demo/创建 Live 引擎场景 (kfm)` 一键建场景（正交相机 + MugenLiveView）。
- **实测验证（MCP，进 Play 截图 + execute_code 反射读引擎态）**：KFM 站立动画在新引擎驱动下渲染；引擎确在逐帧推进——`frame=2140 stateNo=0 animNo=0 animElem=3(循环中) animCurTime=26 physics=1(S) alive=True`，无运行时错误。加载报告：状态 58 / 动画 117 / 命令 37。**这是 MUGEN 化新引擎第一次"能在 Unity 里看到真实角色动起来"。**

**诚实边界 / 下一步（走跳补全，原续作 §2.2）**：
- 现 `ctrl=False`：直接 spawn 进 state 0，未经 MUGEN 回合开始赋 ctrl 的流程 → 走路/出招被挡（攻击 trigger 要 `ctrl`）。
- **关键诊断**：站立→走(20)/蹲(10/11)/跳(40+) **不是 .cns 里的 ChangeState**——MUGEN 引擎硬编码了"stand+ctrl+holdfwd → 自动进保留状态 20"这套内置移动转移（Ikemen 在系统代码 actionRun/changeStateEx 实现，不在角色数据里）。kfm.cmd 的 Statedef -1 只有招式/跑/跳攻击，common1 Statedef 0 也无走路 ChangeState。
- 故"方向键能走"= 移植 Ikemen 内置移动转移（stand↔walk↔crouch↔jump 的自动进入）+ 回合开始 ctrl 初始化，是独立的逻辑层 TDD 单元（下一块）。Unity 渲染/输入/精灵链路本条已全部就绪，走跳一旦接上即可见。

**未回归**：本条仅新增表现层文件（Assembly-CSharp），逻辑层与 dotnet test 240/240 不变。

---

## 2026-06-04 续11：动作战斗系统 Ikemen 1:1 复刻（站/走/蹲/跳/落地，dotnet test 271/271）

承用户「别挤牙膏，对标 Ikemen 列 1:1 复刻清单一口气做完，每模块 TDD，沉淀文档」。先深读 Ikemen 源码定位：站立→走/蹲/跳、空中→落地的状态转移**不在角色 .cns 数据里**，是引擎硬编码（char.go actionPrepare/actionRun）。common1 公共状态只管进入保留状态后的行为。这才是"走跳缺失"的真因——整块引擎动作系统未移植。

复刻清单（6 模块，均 MIT 保留版权头）：
- **A 输入边沿缓冲**（input.go InputBuffer→`Command/MInputBuffer`）：有符号帧计数 Fb/Bb/Ub/Db，按住+N 松开-N；B/F 按朝向推导；SOCD 对消。Clone/Hash 接回滚。测 7。
- **B 动作禁制标志**（ASF_no{jump,crouch,stand,airjump,brake,hardcodedkeys,postroundinput}→`MAssertFlag`+解析）。测 3。
- **C 硬编码基础动作**（char.go actionPrepare 11435-11481→`Battle/MActionSystem.Prepare`）：jump40/airjump45/crouch10/stand12/walk20/brake(20→0)，gate=keyctrl&&!nohardcodedkeys&&ctrl，按 Ikemen else-if 顺序读边沿；刹车不需 ctrl；落地清空跳计数。
- **C2 空中落地**（actionRun 11717-11723，posUpdate 之后→`MActionSystem.LandCheck`）：physics=A&&vel.y>0&&pos.y>=0&&ss.no!=105→state52。**这条之前漏了，导致跳起后无限下落（实测 y 冲到 170 万）**——地面夹取在 MUGEN 同样是引擎硬编码转移，非 .cns。C+C2 测 16。
- **D ctrl/keyctrl**（char.go ctrl() 5255→`MChar.Control()/KeyCtrl`+`MBattleEngine.StartRound`）：回合开始授玩家 ctrl（faithful shim，无回合状态机）。否则 spawn 进 state0 ctrl=false，硬编码动作全被挡=之前"走不动"根因。
- **E 管线整合**（`MBattleEngine.Tick` 重排对齐 char.go）：输入缓冲→actionPrepare→状态机→物理+LandCheck→动画→命中。AssertFlags 时序：actionPrepare 读上帧、RunFrame 起始清零。真实 KFM 端到端测 5（走前移/走后/跳升空/完整弧线落回地面/刹车/确定性）。

验收：**dotnet test 271/271（240→271，+31，0 跳过）**。Unity MCP 实测（execute_code 注入合成输入）：walk dx=48；完整跳跃 apex=-84.4、landed=True、finalState=52、finalY=0.0（精确落地，无限穿地已修）。MugenLiveView 加 StartRound 授 ctrl。

诚实边界：守招120（需 inguarddist+敌方攻击，单角色无法触发，未入链）、inguarddist 未接（走路用无敌人 XOR 解，持后即后退）、回合状态机未做、跑100/105（属角色 cmd Statedef-1 数据，不涉）、down/getup 5110/state140 两条硬编码转移（受击系统时补）。详见新增 `Docs/动作系统_Ikemen移植.md`。

---

## 2026-06-04 续12：修 statedef 头部表达式求值（跳跃时机错乱/卡住根因，dotnet test 273/273）

用户实测：左右走 OK，按上后等一阵、Elem 慢慢涨到 10 才起跳、然后卡在空中。根因=**statedef 头部里写成表达式的字段（anim=40+var(11) / 47+var(11) 等）之前没被求值**——`ParseFirstInt` 只认字面量，遇表达式返回 -1 → 跳跃起始态 40 的 anim 没切上（角色仍停在 idle 动画），而 40 的起跳触发是 AnimTime=0（动画播完），于是要等 idle 动画循环到头才起跳（时机飘忽）；落地态 52 的 anim=47+var(11) 同理失效 → 卡住。

按 Ikemen 实现修对（char.go StateBytecode.init + bytecode.go stateDef.Run）：statedef 头部参数在 MUGEN 本就是**进入状态时按角色上下文求值的表达式**。
- `MStateDef`：anim/ctrl 由 int 改为编译后的 `BytecodeExp`；新增 velset(x/y/z)/facep2/juggle/poweradd 表达式字段；新增 `RunInit(c)` 统一应用头部（type/movetype/physics 字面量 + 其余表达式求值），对齐 Ikemen stateDef.Run 的参数集与顺序。
- `MugenCnsParser.ApplyStatedefField`：这些参数改为 `comp.Compile(val)`（type/movetype/physics 仍字面量字母码）。velset 按逗号拆分。
- `MStateMachine.ApplyTransition` 与 `MCharLoader.ApplyInitialStateHeader`：改调 `def.RunInit(c)`，去重。
- 测试：`MugenCnsParserTests` 头部断言改为求值（含 anim=40+var(11) 随 var11 变 40/41 的回归）；`MStateMachineTests` 手构 MStateDef 的 Ctrl 改 Comp.Compile；新增 `RealKfm_JumpThenRecoversToStandAndCanActAgain`（跳→落→回站立→重获 ctrl→能再走）。

验收：**dotnet test 273/273（0 跳过）**。Unity 实测：按上**第 2 帧即起跳**（不再等 idle 动画）、apex=-84.4、第 45 帧落地恢复站立、末态 state0/ctrl=true 可再行动。
诚实边界：statedef 头部 persist 标志（hitcountpersist/movehitpersist/hitdefpersist，进状态清连段计数）暂未做（与本 bug 无关，连段系统相关）；changeState 控制器的 ctrl=/anim= 参数仍按字面量（少见表达式）。

---

## 2026-06-04 续13：修跳跃浮空真因（ChangeAnim 缺 nil 守卫）+ 沉淀对战完整化路线图（dotnet test 275/275）

用户实测：按上后 anim 41 elem0-3 正常，切到 anim44 后 elem 卡死 3、精灵浮在空中、过一阵突然贴地站立。要求重新全面查 bug + 审计框架是否 1:1 + 估算复刻完整度，再修 bug。

**全面诊断（MCP headless 逐帧 dump 出铁证）**：物理一直在正确下落（pos.y -84→0），是**动画链冻结 + 表现层位置冻结**。完整根因链：① 借的 Terrarian common1 其 Statedef 50,3 `ChangeAnim value=44+var(11)` 没带 `SelfAnimExist` 守卫（标准 common1 有）；② KFM 没有 anim 44；③ **我方 ChangeAnim 控制器无条件 `c.AnimNo=44`，缺 Ikemen `changeAnimEx`（char.go:4489）的 `if a==nil return` 守卫**；④ `MAnimSystem.Action` 撞不存在动画 → 开头 return → 运行态冻结；⑤ `MugenLiveView.Render` 同样 return → 连 transform.position 都不更新 → 精灵+位置一起冻在空中。

**忠实修法（对齐 changeAnimEx：目标动画不存在则保留当前动画）**：
- `MChar` 增 `AnimTable`（不可变配置引用，同 Constants：Clone 浅拷、不哈希、SpawnChar 接入）+ `AnimExists` + `CanChangeAnimTo`（无表时放行，保持裸构造单测兼容）。
- `ChangeAnimController` / `MStateDef.RunInit` 的 anim / ChangeState·SelfState 内联 anim：赋值前过 `CanChangeAnimTo` 守卫。
- `MugenLiveView.Render`：位置永远先更新，缺动画只跳过精灵刷新（绝不冻结角色）。
- 测试：`BasicControllersTests` 增 2 例（切不存在动画 no-op 保留当前 / 存在则正常切）。

验收：**dotnet test 275/275（273→275，+2，0 跳过）**。Unity MCP 复跑真实 KFM 跳跃：animNo 全程 41（exists=Y），elem 正常推进 0→3→4→5→7，pos.y 平滑 -84→0，f40 落地 anim47、f43 站立 anim0，**指向不存在动画的帧数=0**。浮空消除。

**架构审计结论**：结构层面忠实 1:1（模块镜像 Ikemen、文件头带源行号、Tick 顺序一致），刻意偏离仅 BytecodeValue 拆 int/float + float→定点（均登记）。本 bug 是「漏移一个 nil 守卫」的完整度问题，非架构抄歪——物理算得分毫不差即为反证。**完整度 ≈10-11%（行）/≈15%（功能加权），且集中在单角色站走蹲跳；「两个角色能真打」≈0%（受击状态机未做）**。

**沉淀**：新增 `Docs/对战完整化路线图.md`——把 M9 之后长尾拆成可认领 TDD 任务队列（Tier A 受击机/回合机/双人 → B 控制器补全多 agent 并行 → C 命中触发 → D helper/proj/声音/调色板 → E 回滚 → F 差分 trace → G 决斗场 demo 最后做），含每模块封闭验收 harness（6 门）、重构评估（不做结构性重构只做保真修补）、多 agent 认领表。`移植方案_Ikemen.md §4` 已加指针。

---

## 2026-06-04 续14：R-TRG-SAE 完成（SelfAnimExist/AnimExist trigger）+ 与 Codex 双 agent 并行启动

用户决定让 Claude 与 Codex 在同一工程并行施工，按「热点文件单一所有者」分岛：**Codex 拿岛①控制器+HitDef（唯一编辑 `Parse/MugenCnsParser.cs`）**，**Claude 拿岛②触发器 + 岛③引擎核心受击机/回合机/2P + 岛④common1**。握手契约 = 控制器字符串名照 Ikemen `bytecode.go` 原名（受击机经 common1 数据按名调用，不在 C# 编译期耦合）。Codex 起 R-CTRL-hit，Claude 起 R-TRG-SAE。

**R-TRG-SAE（SelfAnimExist/AnimExist trigger，解锁 R-FIX-3 换标准 common1）**：
- 编译器 `MugenExprCompiler.UnaryFuncs` 加 `animexist`/`selfanimexist` → 求值参数 n 后发 `OC_animexist`/`OC_selfanimexist`（OpCode 枚举此前已占位）。对齐 Ikemen `compiler.go:1824/3588`。
- `MChar.ReadTrigger` 加两 case：弹参数 n、undefined 透传、否则返回 `AnimExists(n)`（查本角色 AnimTable）。对齐 Ikemen `char.go:5088 animExist` / `char.go:5102 selfAnimExist`。v1 单角色无 helper/共享动画表，二者同归本角色表（分歧待 R-ENT）。
- 测试 `Tests/.../Mugen/Expr/AnimExistTriggerTests.cs`（6 例：存在/缺失/参数为表达式/守卫常见用法）。先红后绿。

验收：**dotnet test 288/288（0 跳过）**——含本任务 6 例 + Codex R-CTRL-hit 同期落地的用例。Unity MCP 实例已连（`LockstepActDemo@31e0d1f6995b885f`，2022.3.55f1，控制台 0 error）。
诚实边界：① Codex 与本会话**共享同一工作树**（非 CLAUDE §5 规定的隔离 git worktree），分工靠文件级所有权守住、无真冲突，但跑全套测试时若对方正在改文件会出瞬时红（已复现一次、对方编辑完即恢复 288 绿）；建议后续改用独立 worktree。② commit 待办：本任务只应 stage `Char.cs`/`MugenExprCompiler.cs`/`AnimExistTriggerTests.cs` 三个我方文件，勿扫入 Codex 的 `MugenCnsParser.cs`/`HitControllers*`。

---

## 2026-06-04 续15：Codex R-CTRL-hit 第一批控制器落地（dotnet test 288/288）

按 Claude 给 Codex 的施工边界，Codex 只动控制器岛与唯一共享 parser 文件 `Assets/Logic/Mugen/Parse/MugenCnsParser.cs`，没有触碰 `MugenExprCompiler.cs`、`Char.cs`、`OpCode.cs`、`MStateMachine.cs`、`MBattleEngine.cs`、`MActionSystem.cs` 或 common1 数据。

**本批落地**：
- 新增 `Assets/Logic/Mugen/StateCtrl/HitControllers.cs`：`HitVelSetController`、`VelMulController`、`HitAddController`、`HitFallSetController`、`HitFallVelController`、`GravityController`。
- `MugenCnsParser.BuildController` 接入 Ikemen 原名：`HitVelSet`、`VelMul`、`HitAdd`、`HitFallSet`、`HitFallVel`、`Gravity`。
- 新增 `Tests/Lockstep.Logic.Tests/Mugen/StateCtrl/HitControllersTests.cs`：覆盖 gethit velocity 回灌、速度倍率、HitAdd 最近 target 计数、fall flag/velocity proxy、movetype H 下 HitFallVel、Gravity yaccel、parser 字符串名接线。

验收：`dotnet test --no-restore` **288/288 通过**。第一次普通 `dotnet test` 被 NuGet 用户目录锁阻塞；一次 sandbox 内 `--no-restore` 被 MSBuild 写用户 Temp 拒绝，提权后通过。

诚实边界：`R-CTRL-hit` 未整体标完。`DefenceMulSet`、`AttackMulSet`、`HitFallDamage`、`HitOverride`、`PosFreeze` 需要攻防倍率、fall damage、hitoverride、posfreeze 等运行态容器/伤害链路承载；在不改 `Char.cs` 的边界下本批不做伪实现。

---

## 2026-06-04 续15：Tier A 全部完成（R-GHV 受击机 + R-ROUND 回合机 + R-2P 双人）+ 10 角色受击 TDD（dotnet test 333/333）

用户指示：参考 Ikemen 把"对战完整化路线图"Tier A 全做完，再用项目里 10 个角色文件逐个跑 TDD 验收。与 Codex 并行（Codex 岛①控制器，本会话岛②③受击/引擎）。

**R-GHV 受击状态机 5000-5160（引擎侧，313 绿时）**：
- `MHitSystem.ApplyHit` 重写为忠实移植 `char.go:10790-10924` getHitVarSet：填 GetHitVar 全字段（groundtype/airtype/yaccel/fall.*/animtype 经 gethitAnimtype 派生），按守方 statetype 路由 5000(立)/5010(蹲)/5020(空)，trip→5070、down→5080（`char.go:12195-12259`）。**守方不进 hitpause**（对齐 getter.hitPauseTime=0），受击冻结改由 `ghv.HitShakeTime` 驱动。
- `MStateMachine.UpdateGetHitTimers`（`char.go:11775-11834`）：非 hitpause 帧 movetype=H 时 HitShakeTime 递减→HitShakeOver，之后 HitTime 递减→HitOver(<0)，fallflag 时 FallTime++；离开受击清残留。
- 新触发器 `HitShakeOver/HitOver/HitFall/CanRecover`（`char.go:5165/5338/5342`，OpCode 已占位）+ GetHitVar 字段 groundtype/airtype/yaccel/fall.yvel（编译器 gethitvar 改读点分名）。
- `MGetHitVar`/`MHitDef` 增 fall/air 字段（默认值，解析归 R-HITDEF）；`MChar` 增 FallTime + GetHitAnimType()；`MStateDef` movetype 默认 I（`bytecode.go:15139`，否则受击 movetype=H 滞留）。
- 守方 hitstop 语义变更，更新 `MHitSystemTests` 一处断言（def.Hitstop 0 + ghv.HitShakeTime=P2PauseTime）。
- 验收 `GetHitStateMachineTests`（13 例）：触发器/计时递减/路由/**真实标准 common1 跑完整周期 命中→5000→hitshake→5001 滑行→恢复站立 0+重获 ctrl+扣血**。

**R-ROUND 回合状态机**：新增 `Battle/MRoundSystem`（移植 `system.go` roundState/stepRoundState 骨架）。RoundState Intro(1)→Fight(2 授ctrl)→PreOver(3)→Over(4 记分)→下一回合/整场结算；KO/超时(血高者胜)判胜负、双KO平局、三局两胜、回合间满血复位、计时在 Fight 起算。验收 `MRoundSystemTests`（7 例）。

**R-2P 双人对战**：引擎双向 TryHit + 受击机联调。headless `TwoPlayerCombatTests`（5 例）：P1 普攻命中 P2→掉血+进受击 5000→走完恢复、攻方无反伤、经回合系统驱动同样命中、确定性。表现层 `MugenVersusView`（双 SpriteRenderer + 回合系统，P1 键盘/P2 木桩）+ 菜单「创建对战场景 (kfm vs kfm)」。Unity 编译 0 error；**MCP Play+execute_code 注入一拳实测：P2 life 1000→877、PendingState=5000、MoveType=H、ghv.shake=4**。

**10 角色受击 TDD（用户核心验收）**：`AllCharactersGetHitTests`（10 角色 ×2 例 = **20/20 全绿**）。按各角色 .def 解析文件 + 标准 common1（模拟引擎内建公共态）装配，逐个：加载 OK + 公共态含 5000/5001 + 站立被普攻命中→进受击 5000 区→扣 100 血→走完周期脱离受击恢复。Ananzi/Animus/Final/Gustavo/Hashi/Janos/Maxine/Noroko/Peketo/Shar-Makai 全过——受击机对各家真实数据（var 偏移/动画表/常量）皆成立，非只 KFM。

**结果**：dotnet test **333/333**（0 跳过）。「两个角色能真打」从 ≈0% 跨到可玩雏形，功能加权完整度 ≈22-25%。
**诚实边界**：DefenceMulSet/HitFallDamage/PosFreeze 控制器（Codex 岛①）仍 no-op，不挡核心循环；HitDef 完整字段(fall.*/air.type 解析)归 R-HITDEF；ChangeAnim 切同号动画的"冻结复位"未做（不影响离散状态/动画号，归 R-FIX-2）；MugenVersusView 为 KFM 专用演示，任意角色装配入口归 R-ARENA。与 Codex 共享工作树，commit 须只 stage 我方文件。

---

## 2026-06-04 续16：恢复上轮成果（Tier A 落盘）+ R-HITDEF chunk-1（dotnet test 348/348）

接手背景：上轮（续15）Tier A 代码全绿但**因限额未提交**，git HEAD 仍停 35361f6(M9.2)，工作区滞留 R-GHV/R-ROUND/R-2P/R-TRG-SAE/R-CTRL-hit 全部改动 + 残留 .git/index.lock。桌面《评估报告.txt》(288/288) 即据该未提交 checkout 写就，故低估进度（仍把受击机列为最大卡点）。

**先落盘**：清 index.lock → 一个里程碑提交 56cbaca 收容 Tier A（交叉改 Char.cs/MStateMachine.cs 无法干净拆分；整体 333/333）。

**R-HITDEF chunk-1**（HitDef 完整字段第一刀，离散对账 Ikemen）：
- **kill/guard.kill/fall.kill**：`MHitDef` 新增（默认 1）；`MHitSystem.ComputeDamage` 移植 `char.go:8433`——kill=0 且伤害足以致死时保底剩 1 血（命中 + 守招 chip 各一条路径）。
- **能量 getpower/givepower/guard.\***：`MHitDef` 4 字段；命中→攻方 +HitGetPower、守方 +HitGivePower，被防→guard 版；`AddPower` 夹 [0,PowerMax]。CNS 默认值移植 `char.go:931-961`：default.attack.lifetopowermul=0.7、gethit=0.6、guard=命中×0.5、**超必杀(attr 含 H\*)攻方 0**；显式 getpower/givepower="hit[,guard]" 覆盖。
- **forcestand**：`MHitDef.ForceStand` + `MGetHitVar.ForceStand`；蹲被击时翻 statetype→S 并路由 5000（`char.go:12241`）；默认 = ground.velocity.Y!=0（`char.go:911`）。
- **air.\*/fall.\* 解析**：air.type(默认随 ground.type)/air.animtype(默认随 animtype)/fall.animtype(Up/否则 Back)/yaccel/fall.xvelocity/fall.yvelocity/fall.recover/fall.recovertime 全接入 `BuildHitDef`。
- **顺修 CopyInto 遗漏 bug**：`HitDefController.CopyInto` 之前漏拷 air/fall 等字段（Template 填了却进不了 live HitDef）；本轮补全所有字段（含新增 kill/power/forcestand）。`MGetHitVar` 增 ForceStand/Kill 入 Clone/Hash。
- 验收 `Tests/.../Mugen/Hit/HitDefFieldsTests.cs`（15 例：kill 限血/KO、命中+守招能量、PowerMax 夹取、forcestand 5000 vs 5010、power 默认 NonHyper/Hyper/显式覆盖、air·fall 解析、air.type 随 ground.type、forcestand 默认随 ground.vel.Y、CopyInto 完整性）。**dotnet test 348/348（+15）**。

**诚实边界**：R-HITDEF chunk-2 未做——p2getp1state(投技，依赖 custom state ownership)、spark/sound(依赖 R-CTRL-fx/R-SND)、numhits/hitonce 多段、envshake、cornerpush、down.*、fall.damage 落地伤害(依赖 HitFallDamage)、juggle(归 R-JUGGLE)、sprpriority。攻防倍率仍未接，ComputeDamage 暂不含 atkmul/defmul。

---

## 2026-06-05 续17：R-HITDEF chunk-2 攻防倍率伤害公式 + AttackMulSet/DefenceMulSet（dotnet test 361/361）

跟着 Ikemen 结构继续把伤害链路补完整（chunk-1 留的最大债=ComputeDamage 不含攻防倍率）。

- **MChar 运行态倍率字段**（移植 char.go attackMul[0]/customDefense/superDefenseMul/fallDefenseMul/finalDefense）：`AttackMul/CustomDefense/SuperDefenseMul/FallDefenseMul`(FFloat=1) + `DefenseMulDelay`(bool)，全入 Clone/WriteHash。AttackBase/DefenceBase 不另设字段，直接取 `Constants.Attack/Defence`（[Data] attack/defence，默认 100）。
- **两个计算方法**：`ComputeFinalDefense()`=(DefenceBase×customDef×superDef×fallDef)/100（customDef 在 DefenseMulDelay 且非受击态时按 1，char.go:12081-12085）；`AttackDamageMul()`=AttackMul×AttackBase/100。
- **ComputeDamage 重写为定点公式**（char.go:8440-8460）：damage ×= atkMul/finalDefense → 至少 1（极高防仍≥1）→ 不超剩血 → kill=0 保底剩 1 → `(x+0.5).ToInt()` 四舍五入。默认 atkMul=finalDefense=1 时退化为整数，与既有完全一致。两处调用（命中/守招）都传 `attacker.AttackDamageMul()` + `defender.ComputeFinalDefense()`。
- **新控制器**（新建 `StateCtrl/AttackMulControllers.cs`，避开 Codex 的 HitControllers.cs）：`AttackMulSetController`(value/damage→AttackMul，仅伤害分量) + `DefenceMulSetController`(mulType≠0→customDefense=value，=0→1/value；onHit→DefenseMulDelay；**MUGEN 默认 onHit=true·mulType=0**，bytecode.go:10762)。解析器 BuildController 加 attackmulset/defencemulset(含 defensemulset 拼写容错) case。
- **受击当帧先置 movetype H**：ApplyHit 把 `defender.MoveType=2` 提到伤害计算前，使 DefenceMulSet 的 onHit(延迟)防御对本次命中即时生效（对齐 Ikemen finalDefense 在 MT_H 下取 customDefense）。
- 验收 `Tests/.../Mugen/Hit/DamageMultiplierTests.cs`（13 例：默认不变/AttackMul/attackBase/defenceBase/最低1伤/四舍五入/两控制器/mulType/onHit 延迟语义/CNS 解析）。**dotnet test 361/361（+13）**。
- **副作用修正**：`AllCharactersGetHitTests` 原硬编码扣 100，现 **Final 角色 defence=200 半伤**触发——改为按各角色 `Constants.Defence` 算期望伤害（10000/defence 四舍五入）。这反而验证了倍率系统对真实角色数据生效。
- **同时关掉 R-CTRL-hit 的两个 no-op**：AttackMulSet/DefenceMulSet 之前落 NullController，现真正实现。

**诚实边界**：SuperDefenseMul/FallDefenseMul 字段已铺但驱动它们的 SuperPause/浮空防御加成未接（恒 1）；AttackMul 仅伤害分量（redlife/dizzy/guardpoints 三分量未做）；chunk-3 = p2getp1state 投技/spark·sound/numhits 多段/envshake/cornerpush/down.*/fall.damage/juggle/sprpriority。

---

## 2026-06-05 续18：R-HITDEF chunk-3 多段命中/hitonce/fall.damage/down.*（逐 Part TDD）

chunk-3 可做范围 = 纯逻辑、无子系统依赖的离散字段；被卡住的（p2getp1state 投技=custom state ownership、spark/sound=R-CTRL-fx/R-SND、envshake=sys 层、cornerpush=舞台边界、juggle=R-JUGGLE、sprpriority=纯表现）诚实推迟并在路线图写明。

### Part A — numhits 连击计数（dotnet test 366/366，+5）
- `MChar` 加 `GuardCount`/`ReceivedHits`（入 Clone/Hash）。命中→`attacker.HitCount += numhits`（char.go:12189，改原 ++）+ `defender.ReceivedHits += numhits`（char.go:11170 combocount 源）；被防→`attacker.GuardCount += numhits`（char.go:12191）。`MStateMachine.UpdateGetHitTimers` 非受击分支清 ReceivedHits/GuardCount（char.go:11807-11809）。`BuildHitDef` 解析 numhits。
- 测试 `Tests/.../Mugen/Hit/NumHitsTests.cs`（5 例：计数/默认1/被防/脱离受击清零/CNS 解析）。

### Part B — hitonce（dotnet test 372/372，+6）
- `MHitDef.HitOnce`（默认 -1=未设；入 Hash/CopyInto）。`BuildHitDef` 解析 hitonce + 决议默认：未设时投技(attr 含 MAttackType.AT=NT|ST|HT)→1，否则 0（char.go:848）。命中/被防（接触）后若 HitOnce>0 则 `hd.Active=false`（char.go:11283/12172），只命中一个目标。
- 测试 `Tests/.../Mugen/Hit/HitOnceTests.cs`（6 例：命中后停用/非 hitonce 可多目标/被防接触也停用/投技默认1/普攻默认0/显式覆盖）。多目标用第二个 dummy 守方验证。

### Part C — fall.damage + HitFallDamage 控制器（dotnet test 378/378，+6）
- `MHitDef.FallDamage` + `MGetHitVar.FallDamage/FallKill`（入 Clone/Hash/CopyInto）。命中写 ghv.FallDamage/FallKill（char.go:10842）。
- `MHitSystem.ApplySelfDamage`（公开）：自伤按受方 finalDefense 缩放（无攻方倍率）+ kill 保底，移植 char.go:9111 lifeAdd(absolute=false)。
- 新建 `StateCtrl/HitFallDamageController`（我方文件，避开 Codex HitControllers.cs）：movetype H 下应用 ghv.FallDamage 后清零（char.go:9109）。解析器加 `hitfalldamage` case（原落 NullController 的 no-op，关掉）+ BuildHitDef 解析 fall.damage。**关掉 R-CTRL-hit 的 HitFallDamage no-op。**
- 测试 `Tests/.../Mugen/StateCtrl/HitFallDamageTests.cs`（6 例：受击态扣血/非受击 no-op/finalDefense 缩放/fall.kill 保底/命中写 ghv/CNS 解析+控制器实例化）。

### Part D — down.*（击打倒地对手，dotnet test 383/383，+5）
- `MHitDef` 加 `DownVelX/DownVelY/DownHitTime(默认20)/DownBounce`（入 Hash/CopyInto）。`BuildHitDef` 解析 down.velocity(默认随 air.velocity，char.go:892)/down.hittime/down.bounce。
- `ApplyHit` 加倒地(ST_L=8)分支（char.go:10859-10871）：贴地(pos.y=0)用 down.velocity + down.hittime 作 hittime/ctrltime；浮空倒地用 air.velocity；down.bounce=0 且 vy≠0 → fall.yvel 归零（落地不反弹）。
- 测试 `Tests/.../Mugen/Hit/DownHitTests.cs`（5 例：down 速度/硬直+路由5080、bounce=0 归零、bounce=1 保留、默认随 air.velocity、CNS 解析）。

### chunk-3 收尾：可做部分(A-D)全完成；诚实推迟的部分（写明依赖）
- **p2getp1state 投技** → 需 custom state ownership（受击方跑攻击方状态表），独立任务（R-THROW，未列）。
- **sparkno/sparkxy/hitsound/guardsound** → R-CTRL-fx（火花）/ R-SND（声音），表现/实体层未就绪。
- **envshake.\*** → 需 sys 级屏震容器，无系统层。
- **ground.cornerpush/air.cornerpush** → 需舞台边界/相机（R-LOCALCOORD/screenbound）。
- **juggle** → R-JUGGLE：MUGEN 角色 juggle 逻辑有状态（ghv.targetedBy per-id + air_juggle + 仅空中连段第2击起生效，char.go:13139-13145/11360-11376），且需空中连段机制被真正驱动才好对账，独立任务。
- **p1/p2sprpriority** → 纯表现层绘制顺序，逻辑层不涉。

---

## 2026-06-05 续18 收尾：暂停，明天继续 + 「距可玩」评估 + 优先级重排

**停在**：R-HITDEF chunk-1/2/3 全完成，dotnet test 383/383，工作区干净（HEAD b7958bf）。

**⚠️ 优先级重排（重要）**：用户近期目标 = **可玩（KFM 对战 Terrarian/其他角色，无音效无特效即可）**。
经核对 `MugenVersusView`：现为「同一个 KFM 克隆两份、P2 木桩无输入、单 SFF 源」；但引擎 `MBattleEngine.Add(char, data)`
**已支持每角色独立数据**——到「可玩」的瓶颈在**表现层装配 + 控制**，不在 R-ENT。
故**明天起改走 R-ARENA-minimal**（不是先做 R-ENT；R-ENT 仅 helper/projectile 角色才需，KFM 不需，Terrarian 高级招才需）。

**到「可玩」的关键路径（明天起按此做）**：
1. **R-ARENA-minimal**：通用「角色 A vs 角色 B」装配——各自 .def/.cns/.cmd/.air/.sff + 各自 common1 +
   **两套独立 SFF 源 + 两套 anim 表**渲染（现 View 写死单角色单源）。引擎侧已就绪，主要改 View/loader。【最大单项，中等】
2. **P2 输入**：第二套键盘（双人）或简易 AI。双人最省。【小】
3. **双方真实招式能命中验证**：KFM 一拳已实测；需 spot-check KFM 必杀 + Terrarian 普攻经命令系统→HitDef 在 live 引擎打出。【小-中，有未知】
可选（让对战「好打」但非最小可玩必需）：守招（按后格挡，守招态 120/inguarddist，中）、血条 HUD（小，现可从 OnGUI 调试文本读血量）。

**风险/未知**：Terrarian 复杂招可能用未实现的 controller/trigger→降级 NullController（不崩，但该招不生效，需修一轮）；
复杂搓招命令逐角色未差分验证；各角色共用标准 common1 的细微差异。

**明天第一步**：开 R-ARENA-minimal，先把 MugenVersusView 泛化成双角色双源（或新建 MArenaView），载 KFM vs Terrarian 跑起来看两个不同角色同场。

---

## 2026-06-05 续19：修 KFM 完全不出招（.cmd Statedef -1 未加载）+ 6 键映射（dotnet test 387/387）

**用户报告**：MugenLiveView 里 KFM「连出招都不行」。

**诊断（headless 逐步复现，详见过程）**：两个真 bug + 一个表现层映射问题。
1. **【主因】`.cmd` 的 `[Statedef -1]` 命令解释器从未加载**。`MCharLoader.Load` 只把 cmdText 交给
   `MugenCmdParser`（解析 [Command] 定义→data.Commands），但 .cmd 里那一大段 `[Statedef -1]`
   （一堆 `command="x" → ChangeState` 出招逻辑）需 `MugenCnsParser` 解析进 `data.States[-1]` 才会每帧跑。
   修复前 `States` 根本不含 -1 → 命令能匹配 active 但无人消费 → **完全不出招**。
   **修**：MCharLoader.Load 在解析命令后追加 `MergeStates(data.States, MugenCnsParser.Parse(cmdText))`
   （CnsParser 认 [Statedef]/[State]、忽略 [Command]）。对齐 MUGEN：.cmd 状态块与 .cns 状态同属角色状态表。
2. **【次因/边缘】第 0 帧 AnimTime 时序**。招式态如 `[State 230,3]` 用 `trigger1 = AnimTime = 0` 判动画播完回站立。
   状态机在 Tick 第 3 步、动画系统 MAnimSystem 在第 5 步——回合刚开始头一两帧 AnimTime 仍是默认 0
   → 招式同帧自弹回站立。建立动画后（如 -146）即正常。**人类操作不会在第 0 帧按键，实测无影响**，
   登记为 R-FIX-ANIMTIME0 待后续（faithful 修法=ChangeAnim 即时刷新 AnimTime，触及 anim 时序，低优先）。
3. **表现层按键映射**：MugenLiveView 只映射 Z/X/C→A/B/C 三键，KFM 拳是 `command="x"/"y"`（按钮 X/Y）没接上
   →所有拳招按不出。**修**：改 MUGEN 6 键布局——拳 x/y/z = A/S/D，脚 a/b/c = Z/X/C。

**验收**：新增 `Tests/.../Mugen/Battle/CommandMoveTests.cs`（4 例）：Statedef -1 加载、按 A→轻脚 230、
按 X→轻拳 200、按 Y→强拳 210（暖机 5 帧建立 AnimTime 后按下边沿，对齐人类操作）。**dotnet test 387/387**。
改动文件仅 MCharLoader.cs / MugenLiveView.cs / CommandMoveTests.cs（无 Codex 冲突）。

**对可玩的意义**：出招链路打通是关键路径③（双方招式命中验证）的前置。Live 引擎现在 KFM 能走/跳/蹲/出招（拳脚）。
明天起仍按续18：R-ARENA-minimal 双角色双源装配。

---

## 2026-06-05 续20：启动 Tier B/C 全量复刻——Tier C 触发器开篇（random + animelem，dotnet test 402/402）

**用户决策**：不做双角色装配，先让动作/战斗系统完整化到能吃任意 MUGEN 角色数据；Tier B+C 全做（含冷门），
1:1 对照 Ikemen；可让 Codex 接手一部分。**做了数据驱动差距分析**（扫全部 12 角色 .cns/.cmd 统计真实用量）：
- 控制器已实现 ~30/110；缺口按真实用量：实体依赖 R-ENT（helper 731/destroyself 254/target* 等 ≈1450 次）=最大功能坑；
  逻辑相关不需 R-ENT（pause/width/hitoverride/varrandom 等 ~250 次）；纯表现 safe no-op（playsnd 1817/explod 1256 等 ≈4500 次）。
- 触发器已实现 ~50/200；**实锤坏掉**：random（2411，恒返 0）、animelem（3553，恒假）；待补 name/roundstate/inguarddist/numhelper（需 R-ENT）等。

**分工**：Claude 做 Tier C 触发器 + R-ENT + 引擎核心 + 实体类控制器；Codex 做 Tier B 非实体控制器
（独占 StateCtrl/*.cs + MugenCnsParser.BuildController）。新增 `Docs/Codex_控制器分工与字段契约.md`
（文件所有权 + MChar 字段请求协议 + 施工波次 + no-op 判定标准）。

**本会话 Claude 落地（均 TDD，commit 独立）**：
1. **random 触发器**（commit b878f96，394/394）：新增 `Char/MRandom.cs` 1:1 移植 Ikemen common.go 的
   Park-Miller 最小标准 LCG（a=16807, m=2^31-1, Schrage 分解 q=127773 r=2836，纯整数确定性）；
   `random` trigger = Rand(0,999)（bytecode.go:2308）；MChar 加共享 Rng 引用，MBattleEngine 持单一全局种子
   （默认 1，联机同步）+ 入 ComputeHash。验收 RandomTriggerTests 7 例（MINSTD oracle 16807/282475249/1622650073、
   Rand 0/131/755、范围、确定性、集成）。
2. **animelem 触发器**（commit a84e87a，402/402）：编译器特判 animelem，新增 EmitAnimElemCompare（=/!= 走首帧比较，
   操作数 ParseAddSub 不吞外层逻辑；无比较运算符退化发当前元素号兼容 animelem>=N）；新增 OC_animelem
   （AnimElemNo==n && AnimElemTime==0 首帧触发，避免每帧重复触发 HitDef）+ 补 OC_animelemno。
   验收 AnimElemTriggerTests 8 例。

**▶ 下一步（Claude 续 Tier C）**：name/p2name 字符串比较、roundstate（需接回合系统）、inguarddist、
animelemtime(n)、p2bodydist、num*/proj*（部分需 R-ENT）、const/sysvar 全字段。之后 R-ENT。
**Codex 并行起 Tier B**：按契约文档 Wave 1（零字段 no-op + varrandom/varrangeset/movehitreset）先行。

---

## 2026-06-05 续21：Tier C 触发器续（Codex 并行 Tier B，避开其文件）——距离 + 名字

Codex 已在独立 worktree 做 Tier B 控制器（本树无 StateCtrl/MugenCnsParser 改动），Claude 继续 Tier C 触发器
（只动 MugenExprCompiler/OpCode/Char + Expr 测试，零冲突）。两块均 TDD：
- **p2dist/p2bodydist**（commit 0580ac5，409/409）：编译器轴 trigger 块加 p2dist/p2bodydist；OC_p2dist_x/y、
  OC_p2bodydist_x/y；Char 求值 p2dist X=facing 相对(前为正)/Y 不翻向，p2bodydist X=边到边减双方前缘半宽（MUGEN 形）。
  对齐 char.go:8743 distX/8859 rdDistX/8787 bodyDistX；定点精确省 float 噪声夹取。DistanceTriggerTests 7 例。
- **name/p1name/p2name/enemyname**（commit f46a16d，416/416）：编译器特判，name/p1name 自身比较、p2name/enemyname
  走 P2 redirect；OC_name（编码同 OC_command，与 Name 精确比较，大小写敏感）。NameTriggerTests 7 例。

**本会话 Tier C 已修 4 个触发器**：random(2411)、animelem(3553)、p2dist/p2bodydist(~1070)、name/p2name(~1170)——
覆盖约 8200 处真实触发器调用。**▶ 续做**：roundstate(929，需 MChar 加 RoundState 字段+引擎每帧写)、inguarddist、
animelemtime(n)、const/sysvar 全字段、num*/proj*(部分需 R-ENT)→ 之后 R-ENT 实体系统。

---

## 2026-06-05 续21B（Codex 并行 worktree）：Tier B 非实体控制器可解析子集 + MChar 字段缺口登记（commit 8ae6874）

**范围**：Codex 在独立 worktree `D:\Desktop\demo\LockstepActDemo-codex-tierb` 处理 Tier B 非实体控制器。核对 `Char.cs` 后确认
Pause/Width/ScreenBound/HitOverride/ReversalDef/绘制态/PalFX/EnvShake 等运行态字段尚未落在 `MChar`，因此未把路线图 Tier B 标完成。

**已落地**：
- 新增 `TierBNonEntityControllers.cs`：补齐本批控制器类型，让 CNS parser 不再降级为 `NullController`。
- 可真实执行的子集：`VarRandom`、`VarRangeSet`、`MoveHitReset`、`SuperPause.poweradd`。
- 声音与屏幕特效按当前逻辑层要求保持 no-op：`PlaySnd/StopSnd/SndPan`、`Explod/ModifyExplod/RemoveExplod/MakeDust/GameMakeAnim`、
  `EnvShake/FallEnvShake/ForceFeedback/DisplayToClipboard/VictoryQuote` 均可解析、运行无模拟副作用。
- 新增 `Docs/Codex_需要的MChar字段.md`：列出后续 Claude 需加入 `MChar` 并纳入 Clone/Hash 的字段。

**验收**：新增 `Tests/.../Mugen/StateCtrl/TierBNonEntityControllersTests.cs`（7 例），rebase 到最新 master 后 `dotnet test` 409/409。

---

## 2026-06-05 续22：MChar 字段批 + 引擎暂停消费 + Tier C 触发器三批（dotnet test 444/444）

用户令「全做完」。Claude 按序落地（每块 TDD + 独立 commit），与 Codex 并行（Codex 做 Tier B 控制器参数化 + Target*）：
1. **MChar 影响模拟字段批 + MHitOverride**（commit a19299c，428）：按 Codex 字段缺口文档加 sim-affecting 运行态字段
   （Pause/SuperPause 时序、PosFreeze、Width player/edge、PlayerPush、ScreenBound、MoveContactTime/CounterHit、
   HitOverrides[8]）+ MHitOverride 结构体，全入 Clone(值数组深拷)/WriteHash。纯表现态留待表现层。
2. **引擎消费 Pause/SuperPause + PosFreeze**（commit 9feb890，433）：MBattleEngine.Tick 算全局暂停（SuperPause 优先）
   + 每角色冻结位，冻结者跳过本帧输入/状态机/物理/动画，仅施暂停方 movetime 窗口可动，暂停期不结算命中，计时每帧递减；
   PosFreeze 跳物理后清零。无暂停时逐位等价无回归。移植 system.go 全局冻结。PauseFreezeTests 5 例（含确定性）。
3. **roundstate + inguarddist**（commit bfc57b3，439）：MChar.RoundState（默认 2=Fight）+ inguarddist（对手攻击态且
   水平距 <=160）。
4. **animelemtime(n)**（commit 7da5e26，444）：UnaryFuncs→OC_animelemtime，当前元素精确/其他按累积起始推算。

**本会话 Tier C 触发器累计修 7 个**：random/animelem/p2dist·p2bodydist/name·p2name/roundstate/inguarddist/animelemtime
≈覆盖万级真实触发器调用。字段批已解锁 Codex 下一轮把 Pause/Width/ScreenBound/HitOverride 等 parse-only 控制器接线。

**▶ 余下大头 = R-ENT 实体系统**（helper/projectile/explod 实体化 + 生命周期 + Clone/Hash/快照）→ 解锁 numhelper/
ishelper/numproj 等触发器 + helper/proj/explod 实体类控制器。这是独立多会话子系统，下一步专注做。
const/sysvar 全字段为较小余项，一并后续。
## 2026-06-05 组23：文档清单按当前进度校准（路线图 + 审计 + 评估）

**范围**：只更新 Docs，不改代码。读取 `Docs/对战完整化路线图.md`、`Docs/Ikemen对照审计.txt`、`Docs/评估报告.txt`、
`Docs/Codex_需要的MChar字段.md`、`Docs/Codex_控制器分工与字段契约.md` 后，按当前主仓库与并行 worktree 状态校准。

**更新点**：
- 路线图从 2026-06-04 快照推进到 2026-06-05：HitDef chunk-1/2/3、高频触发器 random/animelem/距离/name/
  roundstate/inguarddist/animelemtime 已完成；Tier B parse-only 子集已合入；Codex 第二轮 Target* 与全参数捕获已在
  `codex-tierb-nonentity` commit `1e4845e` 完成但尚未合入 master。
- 审计清单明确最近已提交绿基线为 `7da5e26` 的 `dotnet test 444/444`，并标注当前主工作区存在未提交 Pause 字段重构，
  导致 `dotnet test` 编译失败。
- 评估报告从旧的 288/288 与“受击机未完成”口径更新为：1v1 根角色战斗主干已可验证，真实角色泛用兼容仍主要卡在
  Pause 字段收口、Target 合入、控制器写态接线、R-ENT、表现事件、localcoord/palette 与回滚确定性。
- 字段契约标出部分 MChar 字段已可见，Pause/SuperPause 正迁移到 `MPauseState Pause` 聚合模型，需要先收口。

**验收**：本次运行 `cd Tests/Lockstep.Logic.Tests && dotnet test` 未通过编译；错误为当前主工作区已有代码引用
`MChar.PauseTime/SuperPauseTime/PauseMoveTime/SuperPauseMoveTime/SuperPauseUnhittable`，而 `Char.cs` 当前正在改为
`MPauseState Pause`。文档已如实登记该状态。（注：该 Pause 收口已于 commit 8562687 完成，dotnet test 444/444 绿。）

---

## 2026-06-05 组11C（Codex 并行 worktree）：Tier B 参数捕获补齐 + Target 控制器落地

**范围**：Codex 在独立 worktree `D:\Desktop\demo\LockstepActDemo-codex-tierb` 继续 Tier B，先 `git fetch` 并 rebase 到最新 `master`。本轮只改 `StateCtrl/*.cs`、`MugenCnsParser.BuildController`、`Tests/.../Mugen/StateCtrl/` 与路线图，未触碰 `Char.cs`、表达式编译器、战斗引擎或 Claude 所属测试目录。

**任务 A**：对照 Ikemen GO `src/bytecode.go` 补齐现有 parse-only Tier B 控制器参数捕获。`Explod/ModifyExplod`、`PalFX/AllPalFX/BGPalFX`、`AfterImage`、`Pause/SuperPause`、`HitOverride/ReversalDef`、`PlaySnd/StopSnd/SndPan`、`Text/ModifyText/RemoveText` 等已把已知参数解析到强类型字段或参数组，运行时仍按契约保持 no-op 或仅执行不依赖新增 MChar 字段的现有副作用。

**任务 B**：新增并接入 `TargetState`、`TargetLifeAdd`、`TargetPowerAdd`、`TargetVelSet`、`TargetVelAdd`、`TargetFacing`、`TargetDrop`。这些控制器只使用现有 `MChar.Targets` 与已有字段；`TargetBind` 因需要绑定运行态字段，按本轮约束暂不实现。

**任务 C**：补入冷门 parse-only 控制器 `AppendToClipboard`、`ClearClipboard`、`Text`、`ModifyText`、`RemoveText`、`TagIn`、`TagOut`、`ModifyStageVar` 的 parser 分支和参数容器，保证真实 CNS 中遇到这些类型不会退化成未知控制器。

**验收**：新增 `TargetControllersTests` 与 `TierBParameterCaptureTests`，覆盖 Target 行为与参数捕获。`cd Tests/Lockstep.Logic.Tests && dotnet test` 通过 439/439。

---

---

## 2026-06-05 续24：主线恢复绿 + Pause 1:1 重构 + 合 Codex Target/参数 + 控制器接线（dotnet test 463/463）

用户令按序做完且补未完成项。Claude 落地（每块 TDD+commit）：
1. **主线恢复编译绿 + Pause/SuperPause 按 Ikemen 1:1 重构**（commit 8562687，444）：新增 MPauseState（全局 sys.pause/super
   + buffer/playerno，引擎持单例、角色共享引用、入 Hash）；MChar 加 Pause 引用 + PauseMovetime/SuperMovetime/PauseBool/
   Acttmp/UnhittableTime + SetPause/SetSuperPause(char.go:8920/8941)/ComputePauseBool(char.go:11421+11524 movetime 递减)；
   MBattleEngine.Tick 步骤0 改 PauseState.Step+各角色 ComputePauseBool，buffer 一帧延迟生效。收口 Clone/Hash/测试。
   消掉审计 R2「per-char max 聚合」偏差。
2. **合入 Codex codex-tierb-nonentity 1e4845e**（merge 8308a5c，455）：Target* 控制器(TargetState/LifeAdd/PowerAdd/
   VelSet/VelAdd/Facing/Drop，作用 MChar.Targets)+Tier B 全参数捕获+冷门控制器(Text/Clipboard/StageVar/Tag)。
   代码零冲突（分岛有效），仅文档双 append 冲突已解。
3. **Tier B 写态控制器接线**（commit fa5691f，463）：Pause→SetPause、SuperPause→SetSuperPause、PosFreeze、Width、
   PlayerPush、ScreenBound、HitOverride→HitOverrides[slot]。Codex 已捕获参数，Claude 补 Run() 写字段。
   ControllerWiringTests 8 例。ReversalDef 因需 reversal 容器+命中检测单列推迟。

**▶ 步骤 4 = R-ENT 实体系统**（helper/projectile/explod/custom state）：真实角色泛用兼容的最大地基、范围B 高风险。
   是独立多会话子系统，需谨慎设计实体生命周期 + 动态实体图 Clone/Hash 重链 + 全实体命中（回滚确定性核心），
   下一步专注做。之后 R-DMG-PIPELINE(R3)/juggle(R5)/AnimTime 即刻刷新(R7)/R-ORACLE 差分 trace。

---

## 2026-06-05 续26：R-ENT 切片 1-2（helper 实体系统）+ 兼容清单（dotnet test 470/470）

按 /goal 开 R-ENT。两切片 TDD+commit：
- **切片1 实体世界+helper 生命周期**（commit 1b68684，468）：MEntityWorld(共享 spawn 队列+helper 列表+确定性 id，入 Hash)；
  MChar 加 World/IsHelper/HelperType/Destroyed/RequestHelper；引擎 DrainSpawns 造 helper(用 owner 数据接 Parent/Root/P2/
  Rng/Pause/World)+RemoveDestroyed，Tick 跑 helper prepare/状态机/物理/动画；ishelper/numhelper 触发器。REntFoundationTests 5 例。
- **切片2 Helper/DestroySelf 控制器**（commit 987bb1a，470）：StateCtrl/MEntityControllers.cs(Claude 专属)+parser 派发；
  端到端 CNS 测试(type=Helper 经状态机造实体、helper 内 DestroySelf 移除)。HelperControllerTests 2 例。

新增权威 `Docs/R-ENT进度与兼容清单.txt`：R-ENT 已完成(切片1-2)/余下切片(3 Projectile/4 Explod/5 custom state 投技/
6 helper 关联控制器/7 全实体命中/8 参数形触发器)+R-ENT 外项(R-DMG-PIPELINE/juggle/AnimTime/ReversalDef/触发器长尾/
localcoord/pal/snd/R-ORACLE)+「状态机兼容所有角色」判据与范围 A/B 现状+执行顺序。
**▶ 下一步：切片3 Projectile → 切片7 全实体命中 → 切片5 custom state(投技) → R3 两段式 …**

---

## 2026-06-05 续27：R-ENT 切片 3a Projectile 实体（dotnet test 474/474）

- **切片3a Projectile**（commit a7be935）：MProjectile 轻量弹幕实体(vel/accel/facing/removetime/lifecycle，Clone/Hash)；
  MEntityWorld 加 Projectiles+ProjSpawnQueue+CountProjectiles；MChar.RequestProjectile；引擎 DrainSpawns 造弹幕+
  StepProjectiles 推进移除+入 ComputeHash；ProjectileController+parser 派发；numproj 触发器。ProjectileControllerTests 4 例。
  命中(HitDef+Clsn vs 敌方)归切片 3b。
**本会话 R-ENT 累计：切片1(实体世界+helper 生命周期)/切片2(Helper/DestroySelf 控制器)/切片3a(Projectile motion)。**
**▶ 下一步：切片3b 弹幕命中 → 切片7 全实体命中 → 切片5 custom state(投技) → 切片4 Explod → R3 两段式 …**

---

## 2026-06-05 续28：R-ENT 切片 3b/6部分/7（弹幕命中+ParentVarSet+全实体命中，dotnet test 482/482）

继续 /goal 推进 R-ENT。四块 TDD+commit：
- **切片6部分 ParentVarSet/RootVarSet**（commit bc79a6c，476）：RelayVarSetController(值在 helper 上下文求值，写 Parent/Root
  IntVars/FloatVars，set/add)+parser 派发。RelayVarSetTests 2 例。helper 回传变量打通。
- **切片7 全实体跨队命中**（commit 92a1430，479）：命中阶段改全 MChar 实体(玩家+helper)跨队两两 TryHit(队伍=Root.Id)，
  复用 ApplyHit，2 玩家逐位等价无回归。HelperHitTests 3 例(helper 打敌队掉血/不打同队/确定性)。
- **切片3b 弹幕命中**（commit ae59d48，482）：MProjectile 加 HitDef/Clsn1/Owner；Projectile 控制器 BuildHitDef 模板每发克隆；
  引擎 DrainSpawns 设 HitDef+projanim 首帧 Clsn1，StepProjectiles 对敌队 TryProjectileHit(几何用弹幕、效果走 owner)单段命中即移除；
  MHitSystem.TryProjectileHit。ProjectileHitTests 3 例。弹幕角色可真正打人。

**R-ENT 核心至此基本完成**：helper(生命周期+自跑+命中+变量回传)、projectile(运动+命中)、全实体跨队命中。
**▶ 余下：切片5 custom state(投技 p2getp1state/状态归属)、切片4 Explod(numexplod 追踪)、切片8 参数形触发器(numhelper(id)/projcontact)、
  BindTo*/ChangeAnim2 → R-DMG-PIPELINE 两段式 → juggle → AnimTime 即刻刷新 → 触发器长尾 → R-ORACLE 差分 → 全角色导入冒烟矩阵。**

---

## 2026-06-05 续29：R-ENT 切片 5 custom state 投技（dotnet test 486/486）—— R-ENT 核心机制完成

- **切片5 custom state 投技 p2getp1state**（commit 759d948）：MChar.StateOwner（自定义状态归属，Clone/Hash via id）+
  MHitDef.P2GetP1State(默认 true)+解析+CopyInto/Hash；ApplyHit 当 p2stateno>=0 且 p2getp1state→守方 StateOwner=attacker
  跑攻方状态表；引擎 StateDataFor 用归属角色数据表跑状态机；ApplyTransition 在 SelfState 复位 StateOwner。
  CustomStateThrowTests 4 例(P2 进 P1 投技态 1100/P1 状态在 P2 上跑写 P2 var/SelfState 释放/确定性)。

**★ 审计 4 个条件高风险缺口已全部解决**：helper(切片1-2/6-7)、projectile 命中(3b)、全实体跨队命中(7)、投技 custom state(5)。
**R-ENT 核心实体机制完成**，真实角色战斗状态机主干(含 helper/弹幕/投技)可跑。
**▶ 余下较小项**：Explod(numexplod)、参数形触发器 numhelper(id)/projcontact、BindTo*/ChangeAnim2、R-DMG-PIPELINE 两段式、
  juggle、AnimTime 即刻刷新、触发器长尾、R-ORACLE 差分兜底、全角色导入冒烟矩阵。
## 2026-06-05 续30：R-ENT 切片8 参数形实体触发器（dotnet test 491/491）

在 Claude 已完成 R-ENT 核心 486/486 后继续补覆盖面：
- `numhelper(id)`、`ishelper(id)`、`numproj(id)` 改为编译器特判，统一有参弹 id、无参压 `-1`。
- `projcontacttime(id)`、`projhittime(id)`、`projguardedtime(id)`、`projcanceltime(id)` 接入 `MChar.ReadTrigger`。
- `projcontact(id)`、`projhit(id)`、`projguarded(id)` 作为布尔别名编译为对应 `Proj*Time(id) >= 0`。
- `MChar` 增 `ProjectileContactId/Type/Time`，对齐 Ikemen `pcid/pctype/pctime`，纳入 Clone/WriteHash；`MHitSystem.TryProjectileHit`
  在弹幕接触时写 owner 的 projectile contact 状态，`MBattleEngine.Tick` 每帧递增 time。
- 新增 `EntityParamTriggerTests` 覆盖 helper/proj 参数形、projectile contact time、Clone/Hash 与逐帧递增。

验收：`cd Tests/Lockstep.Logic.Tests && dotnet test` 通过 491/491。

---
## 2026-06-05 续31：清单校准——任意角色全招式闭环与 Ikemen 复刻顺序

**范围**：只更新 Docs，不改代码。按 `73350f3` 后的 `dotnet test 491/491` 基线，校准路线图、R-ENT 清单、评估报告与
`移植方案_Ikemen.md` 的总进度表。

**结论更新**：
- R-ENT 的硬骨头已完成：helper、projectile、全实体跨队命中、ParentVar/RootVar、投技 custom state、实体参数形触发器。
- “任意 MUGEN 角色导入后所有招式可用”拆成两档：招式状态机可跑约 65-70%；包含表现与 Ikemen trace 对账的导入即用整体约 50%。
- 后续复刻顺序从“搭框架”改成“补招式闭环”：Explod/numexplod → helper redirect/partner → BindTo*/TargetBind/ChangeAnim2 →
  R-DMG-PIPELINE → juggle/AnimTime → trigger 长尾 → localcoord/palette/sound → R-ORACLE → 全角色冒烟矩阵。

**验收**：文档更新，无代码变更；上一代码基线 `dotnet test` 为 491/491。

---
## 2026-06-05 续32：R-ENT 切片4 Explod 最小追踪（dotnet test 493/493）

完成“所有招式可用”闭环第 1 块：
- 新增逻辑层 `MExplod`，由 `MEntityWorld.Explods` 持有并纳入确定性 Hash。
- `ExplodController.Run()` 创建可计数记录，捕获 id、anim、pos、vel、accel、scale、facing、bindtime、removetime、sprpriority 等核心运行态。
- `ModifyExplodController` 按 id + index 修改匹配 explod；`RemoveExplodController` 按 id + index 立即裁剪。
- `numexplod`/`numexplod(id)` 编译为带 id 参数的 trigger，运行时按当前 owner 计数。
- 新增 `ExplodRuntimeTests` 覆盖创建、计数、修改、删除。

验收：`cd Tests/Lockstep.Logic.Tests && dotnet test` 通过 493/493。

---
## 2026-06-06 续33：R-ENT 切片8 helper 与 partner redirect（dotnet test 496/496）

完成“所有招式可用”闭环第 2 块：
- `MugenExprCompiler` 支持 `helper(id,index), expr`、`helper, expr`、`partner(index), expr`、`partner, expr` redirect 发码。
- `MChar.Redirect` 支持 `OC_helper` 和 `OC_partner`；helper 按 Root 归属过滤，id <= 0 匹配任意，index 在匹配集合内计数。
- `MChar` 增 `Partner` 结构性链接，Clone 保留引用，Hash 混入 partner id。
- 新增 `HelperRedirectTests` 覆盖 helper 默认参数、id+index 筛选和 partner 显式链路。

验收：`cd Tests/Lockstep.Logic.Tests && dotnet test` 通过 496/496。

注意：partner 目前依赖装配层显式设置 `MChar.Partner`；team mode 自动 partner 装配仍归后续组队系统。

---
