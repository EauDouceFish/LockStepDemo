# 执行日志（人类可读 ledger）

> 与 git commit 互补：git 记"改了哪些文件"，本文记"为什么/到哪了/下次继续"。
> 条目用绝对日期。详细架构见 `架构设计.md`，步骤见 `细化计划.md`。

---

## 2026-06-02 · MUGEN 化改造启动

**方向确立**：把项目从"6 态硬编码状态机"大改为 **MUGEN 化定点帧同步格斗引擎**。方案 =
Ikemen 引擎结构（只抄设计）+ MUGEN 导入管线 + 自有定点确定性。三大取舍已定：
① 战斗模拟 0 Lua（角色=数据，定点表达式 VM 解释）；② 保留 FFloat + hash 对账；
③ 回滚先不实现、框架留出（S13 / Stretch）。

**本次完成**：
- `git init`（LockstepActDemo 现在是 git 仓库）+ 基线提交 `7ca8032`（308 文件）。补 `.gitignore`（Unity + .NET + 仓库外素材）。
- 原始素材：`../MugenSource/Terrarian/`（完整角色，仓库外，已 gitignore）。
- 参考/Oracle：clone `Ikemen-GO` 到 `../MugenSource/_reference/`（只读，12 万行 Go；核心 `bytecode.go`/`char.go`/`compiler.go`/`anim.go`/`image.go`）。
- 文档：`Docs/架构设计.md`（基准）+ `Docs/细化计划.md`（21 天 / 8 阶段 / T* 任务 / TDD + 差分测试 + 多 agent 协议）。

**关键方法论**：以 Ikemen GO 为 **Oracle 做差分测试**（离散量全等 + 连续量容差）+ **确定性黄金哈希**，把"调试"变"对答案"，解决"单人无法肉眼调手感"。

**下次继续（Phase 0 / B）**：建 .NET 测试工程 → 重写 `CLAUDE.md` 为 agent 宪法 → 数据 schema 骨架 + `IExpressionVM` 接口与最小实现 + 新增 Component → 第一个黄金哈希测试。详见 `细化计划.md` Phase 0（T0.1–T0.6）。

---

## 2026-06-02（下午）· 战斗模式拆双支路 + Phase 0 开工

**决策**：拆成两个独立战斗模式，**v1 只做 MUGEN-2D**（纯 2D 平面对拳，仿 MUGEN），火影纵深 3D 列为 v2 支路。理由：纵深改变摇杆语义（2D 上=跳/下=蹲 vs 3D 上下=纵深走位）→ 与 MUGEN 角色资源天生不匹配 → 必须分模式而非可切换。模式专属逻辑隔离在 4 个 seam（Physics/Collision/输入映射/View）。v1 坐标改用 **MUGEN 原生**（X 横, Y 高度, 上为负），importer 坐标恒等映射，便于差分对齐 Ikemen。详见 `架构设计.md` §3b。

**本次完成**：架构文档 §3b 改写为"双模式 + 隔离设计"；细化计划加 §0.0 范围锁定 v1=2D；**Phase 0 T0.1 启动**（.NET 测试工程）。

---

## 2026-06-02（晚）· Phase 0 完成（T0.1–T0.6）

**"最大框架"骨架搭完，`dotnet test` 12/12 全绿。**

- **T0.1**：`.NET 测试工程` + 冒烟测试（FFloat 可逆 / Hash64 可复现）。修 `.gitignore` 误伤手写 csproj。
- **T0.2**：重写根 `CLAUDE.md` 为 **agent 宪法**（方向/三铁律/TDD+差分+黄金哈希/DoD 五条/提交规范/多 agent 协议/关键路径）。
- **T0.3**：数据 schema 骨架 `Assets/Logic/Game/Data/`：CombatEnums(StateType/MoveType/Physics/ControllerType...)、ClsnBox、AnimData/AnimFrame、CommandData、HitDef、StateController、StateDef、CharConstants、CharacterDef。**形状对齐 MUGEN，实现留空待填**。
- **T0.4**：`Assets/Logic/Game/Expr/`：IExpr/IExpressionVM/IEvalContext + `ExpressionVM`（真 lexer+递归下降 parser+AST+定点求值；支持常量/标识符/比较/四则/一元负/括号）。**不硬编码任何 trigger（避形状捷径）**。
- **T0.5**：新增组件 `AnimC/VarsC/CommandStateC/HitDefStateC/CharacterRefC`（Clone 深拷 + WriteHash）。
- **T0.6**：第一个**黄金哈希测试**（10 帧，golden=`15945274352023983870`）+ 表达式 VM 单测 + 快照深拷测试，共 12 测试。

**验收**：`cd Tests/Lockstep.Logic.Tests && dotnet test` → 12 通过 0 失败。证明 Logic 层（含全部新代码）零 Unity 依赖、可独立编译测试。

**下次继续（Phase 1）**：导入管线 —— `IAirImporter`(.air→AnimData) 与 `ISffImporter`(.sff→Sprite) 可并行（T1.1/T1.2），再接 `SpriteAnimator`(T1.3) 让 Terrarian/KFM 在 Unity 动起来。详见 `细化计划.md` Phase 1。

---

## 2026-06-02（深夜）· Phase 1 导入管线（T1.1–T1.4）

**目标达成：T1.1–T1.4 完成，`dotnet test` 25/25 全绿（含真实 Terrarian 数据验证）。**

策略：每个任务的**可测纯逻辑**放 Unity-free 层（dotnet 可测），Unity 表现层只做薄壳。

- **T1.1 AIR 解析**：`Assets/Logic/Import/Air/AirParser.cs`。处理 [Begin Action]、Clsn1/2(Default 持久/非 Default 单帧)、帧行(含 flip)、LoopStart、注释。验收：合成样例 + 真实 Terrarian.air（Action0 dur=120、Action20 走路 13 帧、每帧有受击框）。
- **T1.2 SFF v1 + PCX**：`Assets/Logic/Import/Sff/`(SffTypes/SffV1Reader/PcxDecoder)。解析头(签名+版本+组/图数+链表节点)，PCX RLE 解码+调色板。验收：真实 Terrarian.sff → 187 组/1935 图/1935 节点全在文件内 + 首个非空精灵 PCX 解码尺寸自洽。**踩坑**：尾节点 next 指针未归零→越界 EOF，改为以 NumImages 计数封顶 + 偏移边界检查。
- **T1.3 动画推进**：`Assets/Logic/Game/Anim/AnimAdvance.cs`（纯逻辑，表现层与未来 AnimSystem 共用）。验收：帧切换/循环/永久帧/LoopStart 非零。
- **T1.4 Clsn→世界 AABB**：`Assets/Logic/Game/Data/ClsnWorld.cs`（含 facing 镜像、重叠判定）。验收：朝向镜像/原点偏移/命中落空。
- **Unity 表现层薄壳（不参与 dotnet test，待编辑器内验证）**：`Assets/Editor/Import/MugenImportMenu.cs`(SFF→PNG 菜单 + AIR 日志)、`Assets/Scripts/Game/View/MugenSpriteAnimator.cs`、`ClsnGizmo.cs`。生成精灵输出到 `Assets/Mugen/`（已 gitignore）。

**验收**：`cd Tests/Lockstep.Logic.Tests && dotnet test` → 25 通过 0 失败。

**下次继续（Phase 2）**：表达式 VM 扩 trigger + 通用数据驱动 StateMachineSystem + CNS 导入 + Physics/Anim System，从 common1.cns 把站/走/蹲/跳作为数据跑起来。**注**：表现层 3 个 Unity 脚本需在编辑器打开工程确认编译 + 跑一次 SFF 导入看图。

---

## 2026-06-02（深夜·续）· 规范自查 + Phase 2 数据驱动引擎（T2.1–T2.6）

**先做 Tencent 规范自查 + 重构**（提交 `865b57c`）：无 var；修 AirParser 单行 if→Allman；
ClsnWorld/ExpressionVM/AirParser/PcxDecoder/SffV1Reader 单字母→完整词；ExpressionVM 抽 ExprNode 基类去重；补 XML summary。

**Phase 2 完成（dotnet test 25→41 全绿，TDD 每步提交）**：
- **T2.1**（`b7ceab6`+`c28894b`）：表达式 VM 加逻辑(`|| && !`)/函数(`abs/ifelse/cond/min/max`)/带参 trigger(`var(n)`)；
  `WorldEvalContext` 从 World+Entity 解析 Time/StateNo/Ctrl/Pos/Vel/Anim/Life/var 等；新增 `MugenStateC`(int 状态号)。
- **T2.4–T2.6**（`0bd2882`）：`MugenGameData` + `StateControllerExecutor`(12 类控制器) + `MugenStateMachineSystem`
  (控制器求值 + ChangeState 同帧重入有界) + `MugenPhysicsSystem`(速度积分+Air 重力+落地) + `MugenAnimSystem`。
  端到端测试：手搓 stand↔walk 角色，状态切换/位移/确定性全过。
- **T2.2**（`07d3f21`）：`CmdParser` 解析 .cmd [Command] → CommandData(方向大写/按钮小写区分)。
- **T2.3**（`c8d1de6`）：`CnsParser` 容错结构化解析 .cns → StateDef/Controller(triggerall&&(gN||..) 合成)；
  合成 CNS 端到端驱动引擎；真实 common1.cns 容错解析不崩。

**Phase 2 里程碑达成**：数据驱动引擎能跑「CNS 文本 → 角色数据 → 状态机 + 物理 + 动画」，用受支持的 trigger/controller 子集。

**诚实边界（T2.3b，后续）**：common1.cns / Terrarian.cns 完整「活着跑」还需：运行期未知 trigger 容错
（现在 `alive`/`ishelper` 等会在 eval 抛异常）、枚举 token 翻译(StateType=S)、字符串 command="..."、p2 重定向、更多 trigger/controller。

**Unity 可见性**：① 静态精灵=Unity 里跑 SFF 导入菜单（待你那边编译）；② 动画循环=需写 demo 加载器（未做）；
③ 数据驱动角色=Phase 2 引擎已就绪，但要在 Unity 里看需接输入/渲染（Phase 3+）。Unity MCP 仍未接入本会话。

**下次继续**：要么 T2.3b（让 common1.cns 真正跑起来），要么写 Unity demo 加载器（让你先看到动画），要么进 Phase 3（搓招+碰撞+命中）。

---

## 2026-06-02（深夜·续2）· Demo 加载器 + T2.3b 容错 + Unity MCP 诊断

- **T2.3b 运行期容错**（`提交`）：`WorldEvalContext` 加 alive/ishelper/roundstate + 未知 trigger/函数宽容降级为 0。
  测试：真实 common1.cns → 建角色 → 站立跑 30 帧不崩、稳定停 0（不自杀）。dotnet test 42/42。
- **Demo 加载器**（`提交`，表现层，未经编辑器验证）：`MugenCharacterView`（运行期解 AIR+SFF→Sprite→驱动 MugenSpriteAnimator，
  角色无关）+ `MugenDemoMenu`（菜单一键建 Terrarian/kfm demo 场景）。
- **Unity MCP 诊断**：`~/.claude.json` 配了 HTTP MCP `http://127.0.0.1:8080/mcp`，但探测返回 **503**
  （HTTP 进程在、Unity bridge 未就绪）；且 MCP 工具在会话启动时加载，本会话启动时未就绪 → 本会话无 Unity 工具。
  **解法**：① Unity 里把 MCP bridge/窗口启好（探测应返回 200）；② 重启 Claude Code 会话或 /mcp 重连 → 工具注册后我才能建场景/进 Play/截图。
- **KFM**：Ikemen 源码不含 kfm（在发行版里）；需用户把素材放 `../MugenSource/kfm/`，demo 加载器即可用。

**Unity 手动看效果（无需 MCP）**：打开工程→确认编译→菜单 `MUGEN/Demo/创建动画 Demo 场景 (Terrarian)`→Play。
改 `MugenCharacterView.AnimNo`：0=站立、20=走路。

---

## 2026-06-02（晚·续3）· Unity MCP 接通 + Terrarian demo 编辑器实测通过

- **Unity MCP 接通**：会话内可见 `mcp__UnityMCP__*` 工具，实例 `LockstepActDemo@31e0d1f6995b885f`（Unity 2022.3.55f1）。
- **修编译错误**（`fix(view)`）：`MugenCharacterView.cs:164` CS0120——`static BuildSprite` 引用实例字段 `PixelsPerUnit`。
  改为把 `PixelsPerUnit` 作参数传入 `BuildSprite`。`read_console` 确认 0 error（仅剩 KcpServerTransport 一个无害 CS0067 warning）。
- **Terrarian demo 编辑器实测**（开局检查②完成）：菜单建场景 `MugenDemo_Terrarian.unity` → Play →
  console 打出 `[MUGEN] Terrarian 动画 0：4 帧 / 3 张精灵 已加载` → 截图确认精灵渲染+播放。
  **整条导入管线在 Unity 内端到端跑通**：运行期解 AIR→读 SFFv1→PCX 解码→建 Sprite→MugenSpriteAnimator。
  表现层脚本"未经编辑器验证"的标记**清除**。
- **下一步**：开局检查 ③ 写 SFFv2 读取器（KFM）/ ④ Phase 3（命令系统+碰撞+命中）。

---

## 2026-06-02（晚·续4）· MUGEN 全动作展示工具 + 素材格式盘点

- **素材盘点**：`../MugenSource/` 只有 2 个角色——**Terrarian**(`Terrarian.sff`=**SFFv1**,57MB,复杂自定义角色)、**kfm**(`kfm.sff`=**SFFv2**,200KB,规整官方角色)。
  kfm 另有 intro/ending.sff(SFFv1,过场 CG,非角色)。`_reference` 是 Ikemen 源码。**故"全是 SFFv2"为否**：能跑的 Terrarian 是 v1，想要的 KFM 才是 v2 → 印证路③(SFFv2 读取器)必要性。
- **全动作展示工具**（`feat(view)`,commit `83b2b3a`）：
  - `MugenAnimShowcase`：按动画号顺序轮播角色全部动画（Terrarian **215 个**），每个 2s 自动切，←/→ 手动切、空格暂停，OnGUI 显进度。
  - 抽 `MugenSpriteLoader`(Open/BuildForAnim/Load，懒加载避免上千精灵一次性建) + `MugenAssetPaths`，`MugenCharacterView` 改为复用，消除重复。
  - 菜单加「创建全动作展示场景 (Terrarian/kfm)」；`SetupCamera` 提取共用 + 补 `clearFlags=SolidColor`（原纯色背景不生效）+ 正交拉近。
  - **修编辑器失焦冻结**：`runInBackground=false` 时编辑器无焦点 → Play 不 tick → 动画/轮播冻结（MCP 截图必现）。`execute_code` 实测 `frameCount` 卡在 1 证实；两个 demo view 的 Start 置 `runInBackground=true` 修复。
  - 验收：read_console 0 error；Play 后 execute_code 确认 frameCount 自增、runInBackground=True；截图确认渲染+轮播；**dotnet test 42/42 无回归**。

---

## 2026-06-02/03 · SFFv2 读取器(路③) + 多角色画廊 + 选错.sff 修复

- **素材盘点更新**：`../MugenSource/` 共 **12 个角色**——10 个新角色(Ananzi/Animus/Final/Gustavo/Hashi/Janos/Maxine/Noroko/Peketo/Shar-Makai)**全 SFFv1**、Terrarian(SFFv1,55MB)、kfm(SFFv2)。核实用户说法：KFM=1.0 单 st+storyboard、Terrarian=1.1 拆 st1-4+12 act，全库无 .zss(传统包)，均属实。
- **SFFv2 读取器**（`feat(import)`,commit `a4d4a01`）：`SffV2Reader` 解 v2 头(精灵/调色板头偏移+数量、ldata/tdata)、独立调色板库(RGBA→768RGB)、28 字节定长精灵头链、linked 复用；Lz5/Rle8/Rle5/raw 解码器移植自 Ikemen image.go，PNG(头像)逻辑层跳过。`SffTypes` 加 SffV2Sprite/SffV2File；`MugenSpriteLoader` 按版本字节自动分派 v1/v2 + 抽 BuildSpriteFromImage 共用。验收：**dotnet test 45/45**(+3)；Unity 内 KFM 站立(g0n0 47x106)**像素级完美**(白道服功夫男)。kfm 实况：239 Lz5+1 PNG+41 linked。
- **多角色分页画廊**（`feat(view)`,commit `52c8f54`）：`MugenGalleryShowcase` 扫全部 12 角色，一页 4 人并排站立循环、自动统一缩放同高、OnGUI 左右按钮+←/→ 翻页、懒加载。菜单「创建角色画廊场景(全部角色)」。3 页全部角色渲染正常，v1/v2 混排无误。
- **修隐藏 bug：选错 .sff**。kfm 文件夹有 ending/intro/kfm.sff 三个，原 `FirstFile` 按字母序错选 ending.sff(SFFv1 故事板)→拿 kfm.air 定义找精灵全错位成 240x120 乱图(那个"SUAVE DUDE"文字)。这也是之前单角色 kfm showcase 花屏的根因。新增 `MugenDef` 读 .def `[Files]` 段取 sprite=/anim=(主 def 优先 <文件夹名>.def 或含 cmd= 者)，三处 view 改用之。**修后 kfm renderer=47x106 g0n0 功夫男**。
- **已知遗留**：Janos(v1) 显示纯黑剪影，疑 SFFv1 外部 .act 调色板未应用，待查。
- **（2026-06-03 查实）Janos 黑剪影=非 bug**：g0n0 标准 8bit RLE PCX、形状正确，但源数据只用 3 索引(0/254/255)、122x173 仅 1407 字节(15:1 压缩)→源美术本就是扁平剪影(对比 Maxine 13/Ananzi 22 索引)。真实次要局限=未加载外部 .act 调色板(共享板取文件首个非空=g9000 头像板)；加载 janos1.act 也只把黑剪影变品红(255→234,0,255)、救不回细节，判定不值得做。完整方案=外部.act+samePalPrev 链，Phase 3 不依赖，日后再补。
- **下一步**：③→④ 完成，进 **Phase 3**（命令系统+Clsn碰撞+命中=第一次真打中人）。

---

## 2026-06-03 · Phase 3 启动：T3.1 搓招匹配（commit `fe74e11`）

- **接手导航**：重写 `CLAUDE.md §8` 为文档地图（规约/架构/计划/日志/记忆/测试入口各在哪）+ 已完成实况 + 当前任务，供其他 agent 接手（commit `5d6f353`）。
- **T3.1 完成**：`CommandInputC`(方向+按钮 60 帧环形缓冲) + `CommandMatcher`(纯逻辑：方向按朝向转相对 + 序列贪心匹配，末符号当前帧边沿触发) + `CommandSystem`(每帧推输入+逐条匹配→写 `CommandStateC.Active`)。**dotnet test 54/54**(+9)。
- **关键事实留痕**：`FrameInput` 方向在 MoveX/MoveY 不在 Buttons；`InputBufferC`(6帧仅按钮,cancel 用)不够搓招→另建 `CommandInputC`；朝向取 `TransformC.FacingX.Raw`。
- **T3.1 后续增量（未做）**：`command="name"` 字符串 trigger 接入表达式 VM（需 VM 支持字符串字面量，见 T2.3b 遗留）；MUGEN 引擎 live pipeline 尚未组装（系统现由 dotnet 测试驱动，与 Phase2 一致），组装时 CommandSystem 须排在 MugenStateMachineSystem 前。
- **下一步**：T3.3 CollisionSystem（Clsn1×Clsn2→HitEvent，ClsnWorld 已有）/ T3.2 HitDef controller。T3.4 依赖二者。

## 2026-06-03 · Phase 3 收官（T3.2/T3.3/T3.4，dotnet test 64/64）

- **T3.3 碰撞**（commit `63ef930`）：`ClsnWorld.AnyOverlap`(Clsn1×Clsn2 按 facing 镜像求重叠) + `PendingHitC`(受击方待结算命中) + `CollisionSystem`(攻击方 HitDefStateC.Active+当前帧 Clsn1 × 受击方 Clsn2→写 PendingHit + HitTargetsBits 防一招多次命中)。+5 测试。
- **T3.4 命中**（commit `53f5788`）：`HitSystem` 消费 PendingHitC——扣血(钳0)、受击方硬直 PauseTimeDefender、击退(GroundVelocity 按攻击方朝向镜像)、切 MoveType=BeingHit+PendingStateNo=5000、攻击方 hitstop。+3 测试。
- **T3.2 HitDef 控制器**（commit `5c97b0e`）：`StateControllerExecutor` 加 `case HitDef`(激活 HitDefStateC+清目标位)；`MugenStateMachineSystem.ApplyTransition` 切状态停用 HitDef。+2 测试。
- **Phase 3 完成**。系统执行顺序(组装 live pipeline 时)：InputBuffer/Command → StateMachine → Physics → Anim → Collision → Hit。
- **诚实边界/留待**：HitDef 完整 attr/hitflag/守招判定、击退/hittime 与 Ikemen 差分对齐、`command="name"` 字符串 trigger 接入 VM、MUGEN 引擎 live pipeline 组装（现仍测试驱动）——均 Phase4+ 或后续增量。
- **下一步**：Phase 4（连段/受击/击倒/格挡 + 更多 controller），见 `Docs/细化计划.md`。

---

## 2026-06-03 · 重大方向变更：转 Ikemen GO 1:1 移植（定点化）

- **决策**：clean-room 简化子集（12 controller/贪心搓招/基线命中）**无法兼容真实 MUGEN 角色**（蓄力/负边缘/防御/hitflag/受击状态机全缺）。改为**把 Ikemen 战斗系统逻辑 1:1 忠实移植**（float→定点 FFloat，挂回滚底座），目标=直接吃别人角色文件战斗+保回滚。
- **法律核实**：Ikemen GO 源码 **MIT 许可**（LICENCE.txt），移植合法，保留版权头即可。此前担心的 GPL 传染系误判。
- **规模实测**：核心战斗 ~5 万行 Go（char.go 14k/bytecode.go 15k/compiler 15k/input 3k）。128 controller、HitDef 130 字段、200+ trigger。→ 多会话多 agent 工程。
- **最高纲领文档**：新建 `Docs/移植方案_Ikemen.md`（架构/float→定点契约/Unity 目录/M0-M11 移植顺序+进度表/卡点登记/MIT 文件头模板）。`CLAUDE.md §0/§8` 已同步指向它；旧 `细化计划.md` 降为留史。
- **不可消除约束**：定点≠浮点 → **位级 1:1 不可能**；追求**逻辑 1:1**（离散量与 Ikemen 全等、连续量容差）。
- **M0 起步**（commit 见下）：新引擎根 `Assets/Logic/Mugen/`（镜像 Ikemen 模块）。移植 `BytecodeValue`(定点版)：int 用 long、float 用 FFloat 拆存；**ToI 实现向零截断**对齐 Ikemen `int32(float)`（实测 FFloat.ToInt 是 floor，负小数差 1，已记卡点）。dotnet test **70/70**(+6)。
- **下一步**：M1 表达式字节码 VM（BytecodeExp OpCode 执行器）。退役旧 clean-room 战斗码在移植件取代后进行。

## 2026-06-03 · M1 表达式字节码 VM（dotnet test 87/87）

- `Mugen/Expr/OpCode.cs`：155 个顶层 OpCode 枚举（照搬 Ikemen 顺序，字节值一致）。
- `Mugen/Expr/BytecodeOps.cs`：值运算移植（add/sub/mul/div/mod/pow/比较/位/逻辑/区间/三角/取整/neg/abs/not）。**类型规则照搬**：Min(a.type,b.type)==Float 走浮点否则整数；除零/模零/非法对数→Undefined。
- `Mugen/Expr/BytecodeExp.cs`：栈式字节码执行器。OC_float 编码携带 8 字节 FFloat raw（偏离 Ikemen float32，已记卡点，M2 须遵此）。trigger/redirect opcode 走 `IExprContext` 钩子（M3 接）。短路跳转未做（纯表达式两侧求值结果一致；ifelse 用 OC_ifelse 无需跳转）。
- `Framework/Math/FMath.cs` 补 Acos/Atan/Asin/Ln/Pow/Exp（Asin=π/2−Acos，Exp=Pow(e,x)，e 用 Q31.32 raw 常数）。
- 测试：BytecodeValue 6 + BytecodeOps 9 + BytecodeExp 8 = M0+M1 共 +23（总 87/87）。
- **下一步**：M2 编译器（compiler.go：CNS trigger 字符串 → BytecodeExp，按 trigger 增量移植）或 M3 Char（先有 Char 才能接 trigger opcode 钩子让表达式读实际值）。建议先 **M3 Char 骨架**再回 M2，使表达式能读 Time/StateNo/Pos 等跑通端到端。

## 2026-06-03 · M3 Char 骨架（dotnet test 95/95）

- `Mugen/Char/Char.cs`：`MChar`（避开 System.Char 命名）——移植 Ikemen Char+StateState 核心字段（定点）：Time/StateNo/PrevStateNo/StateType/MoveType/Physics/Ctrl/AnimNo/Life/Power/Juggle/Hitstop/Pos/OldPos/Vel/Facing + var(n)/fvar(n) 字典。
- **实现 `IExprContext`**：把常用 trigger opcode 接到 Char 取值——time/stateno/prevstateno/statetype/movetype/ctrl/anim/pos_x|y/vel_x|y|z/facing/life/lifemax/power/powermax/alive/var/fvar。**至此 M1 表达式 VM 能读真实 Char 数据**，`time >= 2` 端到端跑通。
- **Clone()+WriteHash()**：深拷字典、哈希按 key 升序，接现有回滚/Desync 底座。
- 未接 trigger（gametime/animtime/redirect/p2…）默认 Undefined，增量补。StateType/MoveType 暂用 int32 原始码（枚举映射归 M2）。
- **下一步**：M2 编译器（compiler.go → 把 CNS trigger 字符串编译成 BytecodeExp）。有了 M2，真实角色 CNS 的触发条件就能编译执行；之后 M4 状态机 + M5 controllers。

## 2026-06-03 · M2 表达式编译器核心（dotnet test 104/104）

- `Mugen/Expr/MugenExprCompiler.cs`：tokenizer + 递归下降，**优先级链照搬 Ikemen** compiler.go（expBoolOr→BoolXor→BoolAnd→Or→Xor→And→Eqne→Grls→AddSub→MulDiv→Pow→Unary→Value，低→高）。
- 支持：int/float 字面量、括号、一元(- ! ~)、算术(+ - * / % **)、比较(= != > >= < <=)、逻辑(&& || ^^)、位(& | ^)、无参 trigger(time/stateno/life/ctrl/alive/facing/anim…)、轴 trigger(pos x/vel y…)、函数(abs/floor/ceil/exp/ln/sin/cos/tan/acos/asin/atan/log/ifelse/cond)、var(n)/fvar(n)。
- **端到端**：`"life > 0 && ctrl"`、`"ifelse(time>2,100,200)"`、`"pos x"` 编译→字节码→对 MChar 求值，优先级正确。
- 修 bug：文本小数解析原"逐位×(1/10)"累加导致 0.5 带误差(1/10 定点不精确)→改"小数分子÷10^位数"一次除法(0.5/0.25 精确)。
- **待补**(记进度表)：statetype=S 字母枚举操作数、`=[a,b]` 区间语法、字符串 command=、redirect(p2/parent)、const(...)、更多 trigger。
- **下一步**：M4 状态机（StateBytecode + state runner，跑 StateDef 的控制器；依赖 M2 trigger 求值）+ 渐进补 M2 待补项。

## 2026-06-03 · M4 状态机 MVP（dotnet test 108/108）

- `Mugen/State/MStateDef.cs`：`MStateDef`（状态：头部 type/movetype/physics/ctrl/anim + 控制器列表）+ `MStateController` 基类（Trigger BytecodeExp + Run→是否 ChangeState）+ `ChangeStateController`。
- `Mugen/State/MStateMachine.cs`：`RunFrame`——hitstop>0 冻结；跑当前状态控制器（trigger 过则 Run）；ChangeState 设 PendingStateNo→ApplyTransition（设号/time=0/应用 statedef 头）→**同帧从头重入**（有界 16 次，对应 Ikemen changeStateEx 的 for c.stchtmp）；末 Time++。
- `MChar` 加 `PendingStateNo`（入 Clone/WriteHash）。
- **全链路集成验证**：用 M2 编译 `"time >= 2"` 等真实触发条件驱动状态机，stand↔walk 按时切换、statedef 头生效、hitstop 冻结——**M1+M2+M3+M4 串通**。
- **待补**：StateBlock 的 triggerall/trigger1..n 编译合并、loop/persistent/ignorehitpause、负状态(-1/-2/-3)、SelfState、common states 加载。
- **本会话移植里程碑**：M0✅ M1✅ M2🔄核心 M3🔄骨架 M4🔄MVP。**下一步 M5 controllers**（VelSet/PosAdd/ChangeAnim/CtrlSet/VarSet/HitDef… 逐个移植，挂到 MStateController）。

---

## ▶▶ 重启后开局指引（RESUME，2026-06-02 20:05）

**一句话状态**：Phase 0/1/2 全部完成，`dotnet test` **42/42 绿**，全部已提交。下面是重启新会话后要做的。

**怎么继续验证**：
```
cd D:/Desktop/demo/LockstepActDemo/Tests/Lockstep.Logic.Tests
dotnet test            # 应 42/42 通过
cd D:/Desktop/demo/LockstepActDemo && git log --oneline | head   # 看进度
```

**已完成（都在 git）**：
- Phase 0 地基：测试工程 + schema + 表达式 VM + 组件 + 黄金哈希。
- Phase 1 导入：AirParser / SffV1Reader+PcxDecoder(**仅 SFFv1**) / AnimAdvance / ClsnWorld。
- Phase 2 引擎：表达式 VM(逻辑/函数/带参) + WorldEvalContext + MugenStateC + StateControllerExecutor(12 控制器) + MugenStateMachineSystem + MugenPhysics/AnimSystem + CmdParser + CnsParser(容错)。数据驱动引擎跑通(CNS→数据→状态机+物理+动画)。
- T2.3b 部分：运行期未知 trigger 容错；common1.cns 站立可跑 30 帧不崩。
- Demo 加载器：MugenCharacterView + MugenDemoMenu（**Unity，未在编辑器验证编译**）。
- Tencent 规范自查重构完成。

**素材**：`../MugenSource/Terrarian`(SFFv1，能用)、`../MugenSource/kfm`(**SFFv2，当前读取器不支持**)。`../MugenSource/_reference/Ikemen-GO`(只读 Oracle)。

**Unity MCP**：已 register + server 跑起来(30 工具，HTTP `127.0.0.1:8080/mcp`)。重启会话后应能看到 `mcp__*unity*` 工具。

**下次开局 4 件事（按序）**：
1. 确认 Unity MCP 工具可用 → 实测 Terrarian demo（建场景/Play/截图）+ 确认表现层脚本编译。
2. 写 **SFFv2 读取器**让 KFM 能用（参考 Ikemen `src/image.go` 977/1176/1027 行；fmt 0/2/3/4/10-12；Rle/Lz5 纯 C# 可测，PNG 走 Unity）。
3. T2.3b 余：枚举 token 翻译、字符串 command、带点号 const、p2 重定向。
4. Phase 3：命令系统(输入→搓招匹配) + Clsn 碰撞 + 命中（第一次真正打中人）。

**当前测试数 42**；新引擎与旧 PlayerStates 栈并行，旧栈接场景时再切除。






---

## 2026-06-03 续：测试按模块分目录 + M1–M4 补全到预期级别（TDD）

**测试工程治理**：发现测试文件平铺（旧 clean-room 18 个在根、新 Mugen 件平铺在 `Mugen/`）。
按"每模块一子目录镜像源码树"重组：`Mugen/Expr` `Mugen/Char` `Mugen/State`（`git mv` 保历史），
并把该约定写入 `CLAUDE.md §2`（强制条款 + 旧平铺测试"删一退一"策略）。commit `e04b716`。

**M1–M4 逐模块 TDD 补全（每块先写失败测试→实现→全绿→commit）**：
- **M1**（`3ba75b4`）短路跳转：`BytecodeExp.Run` 实现 OC_jmp/jz/jnz + 8bit + jsf8，peek(不pop)语义、
  8bit(`==0→末尾`,否则`i+off+1`)/32bit(`i+len+4`)编码照搬 Ikemen。113/113。
- **M3**（`d91ab38`）redirect 机制 + Char 扩展：VM 引入 oc/cur 双上下文 + 每迭代重置 + OC_run/nordrun/rdreset，
  实现 root/parent/p2/target 重定向；MChar 加 Id/P2/Root/Parent/Targets（Clone 浅拷结构引用待 World 重链、
  Hash 不递归被引者）；CharSystemVar 常用字段+trigger；MGetHitVar 受击容器(Clone/Hash)。121/121。
- **M2**（`6a67038`）编译器：statetype/movetype 字母枚举比较(S/C/A/L,OR,!=,发 opcode+1字节掩码)、
  `=[a,b]`/`(a,b]` 区间语法、redirect 编译(p2/root/parent 用 OC_run 包裹)。127/127。
- **M4**（`9584849`）状态机：MTriggerSet(triggerall + trigger1..n, OR-of-ANDs)、persistent(1/0/N)、
  ignorehitpause、负状态 -3/-2/-1 每帧跑(移植 actionRun 顺序)、SelfState、common states 回退。135/135。

**当前 dotnet test 135/135 绿**。M0–M4 全部 ✅（见移植方案 §4）。M5(StateControllers，依赖 M2+M4) 已解锁。

**诚实边界（已在 §4 标注待补）**：command=(M6)、const()/角色常量(M9)、gethitvar opcode 与 HitDef 130 字段(M7)、
嵌套 StateBlock if/else 树·loop·for、p2statetype/prevstatetype/target 编译、persistent=N 精确节奏。

**待 Codex 复核后开 M5。**

---

## 2026-06-03 续2：治理修复(Codex 复核阻塞项) + 开 M5 第一批

**Codex 复核发现阻塞项(已全修, commit 见下)**：
- 旧平铺测试删除当时只 stage 了新分目录路径(`git reset`+`add 新路径`漏了旧路径删除)→ HEAD 双份同名测试类，
  干净检出会编译失败。已 `git add -A` 真正提交 6 个旧文件删除；新建 worktree 干净检出验证：构建通过、0 失败、无重复。
- `GetHitVar.cs.meta` 漏跟踪 → 已提交。`BytecodeExp.cs` 顶部"M1 未实现短路跳转"过期注释 → 已修。commit 治理项一条。
- 经验教训：`git mv` 后**不要** `git reset` 再只 `add` 新路径；要么直接提交 mv 的暂存，要么 `git add -A` 把删除一起带上。

**M5 第一批 基础控制器(commit `a06ebd7`, dotnet test 146/146)**：
`Mugen/StateCtrl/BasicControllers` = Null/VelSet/VelAdd/PosSet/PosAdd/ChangeAnim/CtrlSet/VarSet/VarAdd/StateTypeSet。
朝向语义逐一对照 Ikemen char.go：PosAdd X 乘 facing(addX)、PosSet 绝对(setPosX)、VelSet 存原值(facing 积分时应用)。
VarSet/VarAdd 与表达式 VM 闭环。控制器无内部可变状态(参数=配置)。测试落 `Mugen/StateCtrl/`。

**M5 待续**：其余战斗控制器(HitBy/AssertSpecial/Width/Turn/...)、把控制器接入 CnsParser(`[State N] type=` 实例化)、HitDef 归 M7。

---

## 2026-06-03 续3：Codex 复核 M5 + 记录当前项目状态

**复核结论**：上次指出的测试治理阻塞项已修复。`dfe7f81` 真正删除旧平铺测试、补 `GetHitVar.cs.meta`、修 `BytecodeExp.cs` 过期注释；`git ls-files Tests/Lockstep.Logic.Tests/Mugen` 只剩按模块分目录的测试文件，不再有干净检出双份同名类风险。

**M5 验收**：M5 可按"核心 StateControllers + CNS 接入"验收通过，但不能表述为 ~90 个控制器全量完成。
- `a06ebd7`：基础控制器 Null/VelSet/VelAdd/PosSet/PosAdd/ChangeAnim/CtrlSet/VarSet/VarAdd/StateTypeSet。
- `431edc3`：LifeAdd/LifeSet/PowerAdd/PowerSet/Turn/AssertSpecial，并补 `MAssertFlag` 与帧首清空断言标志。
- `c736b13`：`MugenCnsParser` 将 `[State N] type=` 实例化到上述控制器，CNS 文本可端到端驱动 `MStateMachine`。
- 覆盖测试：`Mugen/StateCtrl/BasicControllersTests.cs`、`Mugen/StateCtrl/StatControllersTests.cs`、`Mugen/Parse/MugenCnsParserTests.cs`。

**验收命令**：`cd Tests/Lockstep.Logic.Tests && dotnet test`。普通权限仍会遇到 NuGet 临时锁权限问题；提升权限重跑通过：**167/167 passed**。

**当前项目状态**：
- M0-M4：完成并通过复核。
- M5：核心完成，已解锁后续；边界仍是 HitDef→M7，PlaySnd/Explod/AfterImage/HitBy/Width 等长尾控制器待后续增量。
- M6：已出现核心提交 `a8ace2c`（输入缓冲 + CMD 键解析 + 匹配测试），但当前工作树另有 `Assets/Logic/Mugen/Command/MCommandList.cs`、`Assets/Logic/Mugen/Parse/MugenCmdParser.cs` 未跟踪；本地 167/167 已包含它们，提交前不得视为完整 ledger。
- 当前工作树仍不干净：`Docs/移植方案_Ikemen.md` 有本次状态更新；另有上述 M6 未跟踪文件。

---

## 2026-06-03 续3：M5-M7 完成（TDD，dotnet test 180/180）

承 /goal「制作到 M5-M7 都完成并验收通过」，按相同标准逐模块 TDD + Ikemen 对照 + 每单元 commit。

**M5 StateControllers（核心完成）**
- b1 `a06ebd7`：BasicControllers = Null/VelSet/VelAdd/PosSet/PosAdd/ChangeAnim/CtrlSet/VarSet/VarAdd/StateTypeSet。朝向语义照搬 Ikemen char.go（PosAdd X 乘 facing、PosSet 绝对、VelSet 存原值）。146/146。
- b2 `431edc3`：StatControllers = LifeAdd(kill/clamp)/LifeSet/PowerAdd/PowerSet/Turn/AssertSpecial(MAssertFlag 每帧清)。153/153。
- b3 `c736b13`：MugenCnsParser —— [Statedef]/[State] → MStateDef + 实例化 18 类控制器 + 编译 triggerall/triggerN，未知控制器降级 Null。CNS 文本端到端驱动状态机。159/159。

**M6 命令系统（完成）**
- 核心 `a8ace2c`：MCommandModel(输入位+环形缓冲 Clone/Hash) + MCommandParser(~释放/数字蓄力//按住/$4way/+AND/>严格；方向大写 B/F/U/D+对角，按钮小写) + MCommandMatcher(末步当前帧边沿触发 + 向前贪心 + 朝向相对 B/F + 蓄力连按 N 帧 + 释放边沿 + $子集)。167/167。
- 集成 `ecb2b13`：MugenCmdParser(.cmd [Command] 段) + MCommandList(每帧 Update/IsActive/Clone/Hash) + 编译器字符串字面量词法 + command="name"/!= → OC_command+[长][ASCII名] + MChar 持 CommandList 求值。173/173。

**M7 命中系统（核心完成 `6c6e593`）**
- MHitDef(核心字段) + MClsn(Clsn1×Clsn2 AABB 朝向镜像重叠) + HitDefController(激活+清 targets+movetype=A) + MHitSystem.TryHit(hitflag 校验受击姿态 + 重叠检测 + 结算：扣血/守方转向/击退速度/双方 hitstop/GetHitVar 填值/守方进 5000+movetype H+失控/攻方 movehit·hitcount·target 登记/同招对同目标只命中一次) + MugenCnsParser 接入 type=HitDef。
- MChar 加 HitDef/Clsn1/Clsn2(帧派生浅拷) + Clone/Hash。180/180。

**M0-M7 全部 ✅（见移植方案 §4）。当前 dotnet test 180/180 绿。**
**诚实边界**：M5 更多控制器(HitBy/Width/Explod/PlaySnd…)、M6 '|'OR 逻辑、M7 完整 130 字段/守招格挡/projectile/多段命中/gethit 5000-5150 数据态、gethitvar(...) opcode 接入，均列为后续。下一步建议 M8(Anim/SFF 对接) → M9(角色加载+差分对账 Ikemen)。

## 2026-06-03 续5：修复诚实边界遗留（修复1-4，dotnet test 200/200）

承 /goal「修复到修复5为止 + 大厂面试官标准自审 + 全测试绿」，逐项补齐 M5-M7 列为后续的边界。

- 修复1 gethitvar `6aa6fec`：接入 gethitvar(...) 触发器，编译 OC_ex_ + 字段 id，MChar 从 Ghv 读。184 绿。
- 修复2 命令 OR `c4cb4bc`：命令支持 '|' 逻辑（步内任一键满足即过）。186 绿。
- 修复3 p2statetype `c10b859`：编译 p2statetype/prevstatetype 字母枚举比较。189 绿。
- 修复4 守招 + HitBy/NotHitBy（本条）：
  - Char 加 Guarding/HitByAttr/HitByTime/HitByIsNot（含 Clone/WriteHash）。
  - MHitDef 加 MAttackType 攻击类别位标志枚举 + Attr + guardflag(GuardHigh/Low/Air) + guard.hittime/ctrltime/velocity（含 Hash）。
  - MHitSystem：HitBy/NotHitBy 属性免疫过滤（HitByTime>0 时按 Attr bitmask 与 IsNot 决定放行）+ guardflag 守招结算 ApplyGuard（chip 伤害 / 守方击退用 guard.velocity / ghv.guarded / 攻方 moveguarded，不进受击 5000、不失 movetype）。
  - MStateMachine.RunFrame：HitByTime 每帧递减（hitpause 冻结，不降为负），递减在控制器执行之前。
  - StateCtrl：HitByController（HitBy/NotHitBy 共用，IsNot 区分）。HitDefController.CopyInto 补拷新字段（Attr/Guard*），否则解析值传不到运行期。
  - MugenCnsParser：BuildHitDef 解析 attr/guardflag/guard.*；BuildController 加 hitby/nothitby 分支；ParseAttr/AttackTypeCode 解析 `<statetype>, <攻击类别>` 串为 MAttackType bitmask。
  - 测试 `Mugen/Hit/GuardAndHitByTests.cs`：守招 chip/击退/不进 5000、guardflag 不匹配回落整命中、HitBy/NotHitBy 放行与拦截、HitByTime 递减与 hitpause 冻结、CNS 端到端（guardflag/guard 字段、HitBy、NotHitBy）。

**dotnet test 200/200 绿**（黄金哈希通过，MChar 新增字段已纳入 WriteHash 未破回归）。
**修复5 未开工**：const(...) 角色常量、enemy redirect、persistent=N 节奏核对（对照 Ikemen 倒计时）。

---

## 2026-06-03 续6：修复5 完成（const / enemy redirect / persistent 帧冷却，dotnet test 215/215）

承 /goal「完成修复5为止 + 大厂面试官标准自审 + 全测试绿」，把修复3-5 的诚实边界三项补齐。

- **修复5-1 const(...) 角色常量**（`06cf718`）：新增 `MConstId`(常量字段 id 枚举) + `MConstants`(MChar 不可变常量集，默认值对齐 Ikemen init) + `MugenConstParser`(解析 [Data]/[Size]/[Velocity]/[Movement] 段，jump.neu.y→velocity.jump.y 等键映射对齐 Ikemen)。编译器 `const(field)` 发 `OC_const_`+id 字节，`MChar.ReadTrigger` 从 Constants 读（无常量集容错返 0）。Constants 引用 Clone 浅拷、WriteHash 不混入（不可变配置）。
- **修复5-2 enemy/enemynear redirect**（`c5ea674`）：`MChar.Redirect` 接 `OC_enemy/OC_enemynear`，1v1 中索引 0→P2 其余→null；编译器 `EmitEnemyRedirect` 解析可选 (index) + 子值（OC_run 在敌人上下文求值），须在函数调用判断前处理以免 enemy(0) 被当函数。
- **修复5-3 persistent 改 Ikemen 帧冷却模型**（`5f03b16` + 续 `375736b`）：重写 `MStateMachine` persistent gate——每帧到达控制器先递减计数器，>0 跳过（不求值 trigger），<=0 时 trigger 过才执行并重置为 persistent 值（对齐 Ikemen StateBlock.Run + ctrlsps 倒计时）。归一对齐编译期：1 或 >128→1；<=0→锁定哨兵（进状态仅一次）；其余=N。计数器改 (状态号,控制器序号) 复合键消除负状态/当前/重入串扰，进状态只清该状态范围。续提交进一步修正：控制器触发 ChangeState 时**不**重置旧状态计数器（Ikemen 仅在控制器返回 false 时于 StateBlock.Run 末尾重置）。

**dotnet test 215/215 绿**（黄金哈希通过；新增 ConstTests/enemy/persistent 共约 15 例）。
**M0-M7 + 修复1-5 全部 ✅。诚实边界（后续）**：const 仅覆盖真实角色常用子集（非 200+ 全量）；enemy redirect 仅 1v1（多人/helper 未做）；负状态的 persistent 重置时序与当前状态略有差异（负状态在切状态前即 ResetPersist），真实角色负状态少用 persistent+ChangeState，列为待核。下一步建议 **M8（Anim/SFF 对接）→ M9（角色加载 KFM + 差分对账 Ikemen）**——把已成型的 Expr/State/Ctrl/Command/Hit 子系统组装成 live pipeline，第一个可玩里程碑。

---

## 2026-06-04 续7：M8 动画推进系统完成（移植 Ikemen Animation.Action，dotnet test 229/229）

承 /goal「先简单重构 → 推进 M8（TDD）→ 通过后汇报讨论 M9」。先做零风险微重构再推进 M8。

**前置微重构（commit 5b12904，215 绿）**：抽 `Mugen/Parse/MugenCodes` 集中 statetype/movetype/physics 字母码、攻击类别二字母码、attr 合成、hitflag/guardflag 标志解析；MugenCnsParser 删 6 个私有码表方法改委托，guardflag/hitflag 两段重复解析合并为 MugenCodes.HitFlags（501→约430 行）。纯内部、行为不变。

**M8（commit 7f80357，229 绿）**：
- `Mugen/Anim/MAnim`：引擎自有动画数据 MAnimFrame/MAnimData，Clsn 用 MClsnBox（与命中系统一致，免运行期转换）；`ComputePacing` 预算 TotalTime/LoopTime/PreLoopTime（移植 anim.go 行 337-363）。
- `Mugen/Anim/MAnimSystem`：`Action` 逐 tick 推进，**literal 移植 Ikemen Action() 的 next 闭包**——跳零时长帧、永久帧停末元素、末尾回绕 loopstart、curtime 到 totaltime 时回绕到 totaltime-looptime。派生 trigger 量 AnimElemNo(1-based) 与 AnimTime(=curtime-totaltime，惯例≤0)，每 tick 填当前帧 Clsn1/Clsn2 供命中系统。动画号变化（ChangeAnim/statedef anim=）由系统自检 AnimNo!=AnimRunningNo 重置，不需控制器介入。无静态可变态、运行态全写回 MChar 接回滚。`Play` 复位不推进。
- `Mugen/Anim/MAnimImport`：AIR(Game.Data.AnimData，AirParser 输出)→MAnimData 桥接，新引擎对共享导入层的**唯一耦合点**（单向、加载期，便于日后随 AnimData 上移）。
- MChar 增 AnimElem/AnimElemTime/AnimCurTime/AnimLoopEnd/AnimRunningNo 五个运行态字段，全部纳入 Clone/WriteHash。
- TDD：`Tests/.../Mugen/Anim/MAnimSystemTests` 14 例（节拍量/单双帧推进/循环回绕/永久帧停留/跳零时长帧/切动画重置/Clsn 填充/Play/null 容错/AIR 导入/克隆哈希独立）。先红（一处测试算错 totaltime）后绿。

**dotnet test 229/229**。**M0-M8 + 修复1-5 全部 ✅。**
**诚实边界（后续）**：AnimElemTime(n)/AnimElemNo(time) 复杂查询未做（当前只给简单情形的 AnimElemNo=curelem+1）；interpolate/scale/blend/翻转为纯表现（M11）；SFF 像素加载属表现层（M11）；本里程碑只做逻辑层动画推进与 Clsn 派生。
**下一步**：按计划暂停，向用户汇报 M8 + 讨论 M9（角色全链路加载 KFM + 黄金哈希迁到 MChar + 旧 clean-room 引擎退役）。

---

## 2026-06-04 续8：M9.1 live pipeline 装配 + 角色加载，真实 KFM 跑通（dotnet test 235/235）

承用户选择「先接 Unity 渲染看角色动起来 + M9 内做真差分对账」。但要"看到动"或"对账"，前提是先把散装子系统组装成能跑的引擎并能吃真实角色文件——故 M9 从此起步（commit 9c14d6f）。

- `Mugen/Parse/MDefParser`：解析 .def [Files] 段（cmd/cns/st*/stcommon/sprite/anim），逻辑层不做 IO。
- `Mugen/Battle/MCharData`：角色静态配置（状态/公共状态/动画表/命令/常量），加载后只读。
- `Mugen/Battle/MCharLoader`：从 CNS/CMD/AIR 文本装配 MCharData（复用既有解析器 + MAnimImport）；`SpawnChar` 建挂好配置的运行期 MChar。
- `Mugen/Battle/MBattleEngine`：每帧 pipeline = 命令→状态机→物理→动画(M8)→命中，顺序对齐 CLAUDE §8.3；ComputeHash 覆盖全角色接快照/回滚。**物理目前仅最小速度积分（pos+=vel，X 乘朝向）**，重力/摩擦/地面夹取列为 M9 物理完善 TODO。
- 命名避坑：模块命名空间用 `Lockstep.Mugen.Battle`（不能叫 System，否则遮蔽 BCL System 致 [System.Flags] 编译失败）。
- 测试基建修复：`TestAssets` 改为锚定自身文件目录（DemoDir），不再依赖 [CallerFilePath] 调用方深度——之前嵌套子目录的新测试会解析错素材路径而误跳过真实素材测试。
- TDD：`Tests/.../Mugen/Battle/MBattleEngineTests` 6 例（.def 解析/多状态文件/装配/单角色推进+物理位移/逐帧确定性哈希/**真实 KFM 加载并连跑 60 帧不崩、动画推进、未自杀**）。

**dotnet test 235/235（0 跳过）。M0-M8 + 修复1-5 + M9.1 ✅。**
**诚实边界/下一步**：
1. **M9 物理完善**：当前最小积分，缺重力/摩擦/地面 y=0 夹取/PlayerPush——"走路/跳跃手感"要靠这步。
2. **公共状态**：KFM 测试只载 kfm.cns（招式态），未载 common1.cns（站立/行走在公共态里，KFM 目录无 common1，需从引擎 data 供给）。差分对账前要把 common1 接上。
3. **M9.2 Unity 渲染**：把 MBattleEngine → MugenSpriteAnimator/View，编辑器里看 KFM 动（依赖用户启动 Unity MCP 桥接 8080 + 重启会话）。
4. **M9.3 真差分对账**：需在 Ikemen 跑出 KFM 逐帧状态/动画序列存 Golden，新引擎离散全等——须先确认 Ikemen 可 headless 抽 trace（Go 构建可行性待查）。

---

## 2026-06-04 续9：M9 物理(摩擦/重力) + 接入 common1 公共状态（dotnet test 240/240）

承用户「把物理完善 + 接 common1 两块纯逻辑做掉，然后写文档，明天重启做剩下的」。参考 Ikemen char.go（非 MCP）。

- **物理（commit 97904e3，移植 char.go posUpdate/gravity）**：新增 `Mugen/Battle/MPhysics`——位置积分（pos+=vel*facing X / vel Y）+ 按状态 physics 类型：S/C 地面摩擦（vel.x*=friction，绝对值低于阈值归零）、A 重力（vel.y+=yaccel）、N 不施加。**落地/地面夹取不在物理里**，MUGEN 由公共状态 trigger 检测，忠实照搬。`MConstants` 增 Stand/CrouchFrictionThreshold + 解析 [Movement] 对应键（MUGEN 1.0）。`MBattleEngine` 第 3 步改用 MPhysics.Step。
- **公共状态（同 commit）**：`MCharLoader.SpawnChar` 入场应用初始 [Statedef] 头部（type/movetype/physics/ctrl/anim），对齐 MUGEN 入场 changeState——否则直接置 StateNo 不经转换，physics=S 等头部不生效。公共状态加载链路打通（Load 的 commonText→CommonStates→引擎传状态机回退）。**KFM 目录无 common1.cns**（MUGEN 引擎自带；Ikemen 用 .zss 脚本格式，我们吃 CNS），借 Terrarian 的标准公共状态。
- **TDD**：`MPhysicsTests` 4 例（空中重力累加/站立摩擦衰减并归零/N 不变/朝向积分）；`MBattleEngineTests` 加真实 KFM+common1（公共态含 Statedef 0/20、入场 physics=S/type=S、stand.friction 载入、连跑 60 帧不崩）。

**dotnet test 240/240（0 跳过）。M0-M8 + 修复1-5 + M9.1 + M9 物理/公共状态 ✅。**
**文档**：新增 `Docs/M9_进度与续作.md`（M9 状态快照 + 明日续作导航：M9.2 Unity 渲染、物理/状态补全、M9.3 Ikemen 差分对账可行性、黄金哈希迁移/旧引擎退役）。
**明日重启续作**：见 `Docs/M9_进度与续作.md §2`。第一步建议 = 用户启 Unity MCP 桥接 8080 + 重启会话 → 做 M9.2 渲染（先看 KFM 站立/行走），同时调研 Ikemen headless 抽 trace 可行性（go version）。

---

## 2026-06-04 续10：M9.2 接 Unity 渲染——新引擎首次在编辑器里跑出真实 KFM

承用户「连 Unity MCP 继续昨天未完成工作」。Unity MCP 桥接已起（LockstepActDemo@31e0d1f…，2022.3.55f1），M9.2 前置满足。

- **表现层 live 入口（`Assets/Scripts/Game/View/MugenLiveView.cs`，Assembly-CSharp，非逻辑层）**：MonoBehaviour 把 `MBattleEngine` 接进 Unity。Start 做文件 IO（读 kfm.cns/.cmd/.air + 借 Terrarian/common1.cns）→ `MCharLoader.Load`/`SpawnChar` 装配纯定点引擎；`MugenSpriteLoader.Open` 开 SFF、按动画号懒构建精灵。Update 按 60 tick/s 定步累加：采键盘 → `MInput` → `engine.Tick` → 读 `MChar.AnimNo` + 当前帧 `MAnimFrame(SpriteGroup,SpriteImage)` 取精灵摆位。**定点→float 仅在此边界转换**（pos/PPU，MUGEN Y 向下取负）。OnGUI 叠 StateNo/AnimNo/Elem/Pos/Vel 调试。
- **编辑器菜单**：`MUGEN/Demo/创建 Live 引擎场景 (kfm)` 一键建场景（正交相机 + MugenLiveView）。
- **实测验证（MCP，进 Play 截图 + execute_code 反射读引擎态）**：KFM 站立动画在新引擎驱动下渲染；引擎确在逐帧推进——`frame=2140 stateNo=0 animNo=0 animElem=3(循环中) animCurTime=26 physics=1(S) alive=True`，无运行时错误。加载报告：状态 58 / 动画 117 / 命令 37。**这是 MUGEN 化新引擎第一次"能在 Unity 里看到真实角色动起来"。**

**诚实边界 / 下一步（走跳补全，原续作 §2.2）**：
- 现 `ctrl=False`：直接 spawn 进 state 0，未经 MUGEN 回合开始赋 ctrl 的流程 → 走路/出招被挡（攻击 trigger 要 `ctrl`）。
- **关键诊断**：站立→走(20)/蹲(10/11)/跳(40+) **不是 .cns 里的 ChangeState**——MUGEN 引擎硬编码了"stand+ctrl+holdfwd → 自动进保留状态 20"这套内置移动转移（Ikemen 在系统代码 actionRun/changeStateEx 实现，不在角色数据里）。kfm.cmd 的 Statedef -1 只有招式/跑/跳攻击，common1 Statedef 0 也无走路 ChangeState。
- 故"方向键能走"= 移植 Ikemen 内置移动转移（stand↔walk↔crouch↔jump 的自动进入）+ 回合开始 ctrl 初始化，是独立的逻辑层 TDD 单元（下一块）。Unity 渲染/输入/精灵链路本条已全部就绪，走跳一旦接上即可见。

**未回归**：本条仅新增表现层文件（Assembly-CSharp），逻辑层与 dotnet test 240/240 不变。

---

## 2026-06-04 续11：动作战斗系统 Ikemen 1:1 复刻（站/走/蹲/跳/落地，dotnet test 271/271）

承用户「别挤牙膏，对标 Ikemen 列 1:1 复刻清单一口气做完，每模块 TDD，沉淀文档」。先深读 Ikemen 源码定位：站立→走/蹲/跳、空中→落地的状态转移**不在角色 .cns 数据里**，是引擎硬编码（char.go actionPrepare/actionRun）。common1 公共状态只管进入保留状态后的行为。这才是"走跳缺失"的真因——整块引擎动作系统未移植。

复刻清单（6 模块，均 MIT 保留版权头）：
- **A 输入边沿缓冲**（input.go InputBuffer→`Command/MInputBuffer`）：有符号帧计数 Fb/Bb/Ub/Db，按住+N 松开-N；B/F 按朝向推导；SOCD 对消。Clone/Hash 接回滚。测 7。
- **B 动作禁制标志**（ASF_no{jump,crouch,stand,airjump,brake,hardcodedkeys,postroundinput}→`MAssertFlag`+解析）。测 3。
- **C 硬编码基础动作**（char.go actionPrepare 11435-11481→`Battle/MActionSystem.Prepare`）：jump40/airjump45/crouch10/stand12/walk20/brake(20→0)，gate=keyctrl&&!nohardcodedkeys&&ctrl，按 Ikemen else-if 顺序读边沿；刹车不需 ctrl；落地清空跳计数。
- **C2 空中落地**（actionRun 11717-11723，posUpdate 之后→`MActionSystem.LandCheck`）：physics=A&&vel.y>0&&pos.y>=0&&ss.no!=105→state52。**这条之前漏了，导致跳起后无限下落（实测 y 冲到 170 万）**——地面夹取在 MUGEN 同样是引擎硬编码转移，非 .cns。C+C2 测 16。
- **D ctrl/keyctrl**（char.go ctrl() 5255→`MChar.Control()/KeyCtrl`+`MBattleEngine.StartRound`）：回合开始授玩家 ctrl（faithful shim，无回合状态机）。否则 spawn 进 state0 ctrl=false，硬编码动作全被挡=之前"走不动"根因。
- **E 管线整合**（`MBattleEngine.Tick` 重排对齐 char.go）：输入缓冲→actionPrepare→状态机→物理+LandCheck→动画→命中。AssertFlags 时序：actionPrepare 读上帧、RunFrame 起始清零。真实 KFM 端到端测 5（走前移/走后/跳升空/完整弧线落回地面/刹车/确定性）。

验收：**dotnet test 271/271（240→271，+31，0 跳过）**。Unity MCP 实测（execute_code 注入合成输入）：walk dx=48；完整跳跃 apex=-84.4、landed=True、finalState=52、finalY=0.0（精确落地，无限穿地已修）。MugenLiveView 加 StartRound 授 ctrl。

诚实边界：守招120（需 inguarddist+敌方攻击，单角色无法触发，未入链）、inguarddist 未接（走路用无敌人 XOR 解，持后即后退）、回合状态机未做、跑100/105（属角色 cmd Statedef-1 数据，不涉）、down/getup 5110/state140 两条硬编码转移（受击系统时补）。详见新增 `Docs/动作系统_Ikemen移植.md`。
