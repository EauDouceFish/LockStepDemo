# 对战完整化路线图（M9.4 → 可玩 → 导入即用）

> **本文是「让两个角色真正能打 + 任意 MUGEN 角色导入即用」的权威任务队列。**
> 上游纲领 = `Docs/移植方案_Ikemen.md`（M0–M11 总表 + float→定点契约 + 文件头模板）。
> 本文把该表 §4 里 M9 之后那条**长尾**拆成「一个 agent 一次能独立做完并验收」的任务单元。
> 接手先读：本文 §0（验收 harness）+ §1（重构结论）+ 认领 §3 的某个 `R*` 任务。
> 创建于 2026-06-04。状态：⬜未开始 / 🔄进行中 / ✅完成（带 commit）。

---

## 0. TDD 封闭验收 harness（所有任务的硬门，缺一不算完）

每个任务单元（`R*`）必须**先写会失败的测试 → 实现 → 全绿**，然后在本文把该任务标 ✅ 并写 commit。
具体门（= `CLAUDE.md §3` DoD 的本工程落地）：

1. **单元测试**：被移植的 controller / trigger / 子系统，每个独立可测，放
   `Tests/Lockstep.Logic.Tests/Mugen/<模块>/`（**镜像源码树**，禁止平铺到工程根）。
2. **差分对照用例**：凡有 Ikemen 对应行为的，测试里写明**离散量期望值**（状态号 / 动画号 /
   命中与否 / KO / ctrl）。离散量**必须全等**；连续量（pos/vel/三角）**容差**（给 epsilon）。
   离散期望值来源：① M9.7 真 trace（最佳）；② 暂时用手工核对 Ikemen 源码逻辑推出的关键帧值（标 `// oracle: 手算`）。
3. **不回归**：整套 `cd Tests/Lockstep.Logic.Tests && dotnet test` 全绿。
4. **黄金哈希**：固定输入跑 N 帧断言 `MBattleEngine.ComputeHash()` 不变；改了行为就回填新值并在 commit 说明。
   （注：黄金哈希迁移见 `R-DET-1`，迁移前先锚旧 ECS，迁移后所有新字段必须纳入 `MChar.WriteHash`。）
5. **腾讯 C# 规范**：Allman `{}`、禁 `var`、禁单字母命名、补注释、文件头带 Ikemen MIT 版权块
   （`移植方案_Ikemen.md §6` 模板，注明对应 `src/<file>.go` 行段）。
6. **登记**：本文对应任务标 ✅+commit；`Docs/执行日志.md` 追一条（绝对日期）。

> **每个 `R*` 任务卡片格式**：`依赖` / `Ikemen 源` / `验收（要写哪些测试）` / `产出文件`。
> 完成后把标题前的 ⬜ 改 ✅ 并补 commit 短号——这就是「做完即沉淀到文档标注已完成」。

---

## 1. 重构评估结论（用户问：是否要照 Ikemen 那样重构现有代码？）

**结论：不做结构性重构，保持现有 C# 风格；只做「定点化保真修补」。** 依据：

| 维度 | Ikemen（Go） | 我们（C#） | 是否需对齐 |
|---|---|---|---|
| 控制器派发 | bytecode 编译 + `bytecode.go` 巨型 type-switch | 一控制器一 C# 类 + 虚 `Run(MChar)` | **否**。`移植方案 §2.8` 已授权此适配；行为等价（差分测试只校**可观测离散量**，bytecode 编码是内部细节）；C# 多态更可读可调试 |
| Char | 巨 struct | `MChar` 大类（镜像字段）| 否，已对齐 |
| 状态机 | 嵌套 `StateBlock` 树 | 扁平控制器列表 + persistent/ignorehitpause 行为移植 | 否，行为已移植（`MStateDef.cs` 头注已说明刻意不抄嵌套树）|
| 表达式 | bytecode VM | bytecode VM（已 1:1）| 已对齐 |

**为什么不重构**：把 5389 行改成 bytecode-controller 模型 = 巨大 churn、破全部测试、**零可观测收益**。
Ikemen 上 bytecode 是为「序列化 + 上千控制器性能」，我们不需要。

**但以下「保真修补」要随手做（不是重构，是改对）**——它们是真实的 Ikemen 逻辑偏离：

- ✅ **R-FIX-1 ChangeAnim 缺 `a==nil` 守卫**（2026-06-04 已修，commit 待填）：切到不存在动画曾冻死动画运行态 +
  表现层位置（KFM 跳起浮空根因）。已对齐 `char.go:4489 changeAnimEx`，加 `MChar.CanChangeAnimTo` 守卫
  到 ChangeAnim / `MStateDef.RunInit` / ChangeState·SelfState 内联 anim；`MugenLiveView.Render` 改为位置永不冻结。
- ⬜ **R-FIX-2 动画赋值集中化**：现 ChangeAnim / RunInit / ChangeState / `MAnimSystem.Play` 各自写 AnimNo。
  抽一个 `MChar.SetAnim(animNo)`（含存在性守卫 + prevAnimNo），所有处委托之，杜绝再漏守卫。低优先、零行为变化。
- ⬜ **R-FIX-3 借 Terrarian common1 → 换标准 common1**：现 KFM 借 `Terrarian/common1.cns`（它改过、
  State 50,3 切 anim44 无 `SelfAnimExist` 守卫）。应换**标准 MUGEN common1.cns**（带守卫）或转换 Ikemen
  `data/common1.cns.zss`。依赖 `R-TRG-SAE`（SelfAnimExist trigger）。

---

## 2. 任务分解（按「离可玩有多近」排序；同层可多 agent 并行）

### Tier A — 让两个角色真正能打（最高优先，离「可玩」最近）

#### ✅ R-GHV — 受击状态机 5000–5160（2026-06-04，dotnet test 301/301）
已落地（引擎侧，岛②③）：`MHitSystem.ApplyHit` 忠实填 GetHitVar 全字段 + 按 statetype 路由 5000/5010/5020
（trip→5070、down→5080），对齐 `char.go:10790-10924` / `:12195-12259`；守方不进 hitpause（getter.hitPauseTime=0），
受击冻结由 `ghv.HitShakeTime` 驱动；`MStateMachine.UpdateGetHitTimers` 逐帧递减 hitshaketime→hittime、
fallTime++（`char.go:11775-11834`）；新增触发器 `HitShakeOver/HitOver/HitFall/CanRecover`（`char.go:5165/5338/5342`）
+ GetHitVar 字段 groundtype/airtype/yaccel/fall.yvel；`MStateDef` movetype 默认 I（`bytecode.go:15139`）。
**用真实标准 common1 验收：命中→5000→hitshake→5001 滑行→恢复站立 0 + 重获 ctrl + 扣血**。
测试 `Tests/.../Mugen/Hit/GetHitStateMachineTests.cs`（13 例）。
边界：DefenceMulSet/HitFallDamage/PosFreeze 控制器（Codex 岛①）暂 no-op，不挡核心循环；fall.* 字段解析归 R-HITDEF；
ChangeAnim 切同号动画的"冻结复位"未做（不影响离散状态/动画号，归 R-FIX-2）。

#### ✅（已被上面替代，保留原描述）原 R-GHV 卡片
- 依赖：Tier-B 的若干受击控制器（见下）先到位；M7 命中（已有核心）。
- Ikemen 源：`char.go`（gethit 处理 / getHitVarSet）+ 标准 `common1.cns` Statedef 5000–5160。
- 内容：被击后整套数据态——5000 立受击 / 5010 蹲受击 / 5020 空中受击 / 5030 击退 /
  5040–5050 击飞·落地 / 5070 倒地 / 5080 起身 / 5100+ 空中被击 / 5150 KO。
  这些是**数据（common1）**，但依赖一批控制器与 trigger（HitVelSet/VelMul/DefenceMulSet/HitOver/HitShakeOver…）。
- 验收：构造「A 普攻命中 B」→ B 进 5000 → 走完 hitshake→slide→recover→回 0，
  **离散对照**各阶段状态号/动画号；`GetHitVar(*)` 全字段被正确读取。

#### ✅ R-ROUND — 回合状态机（2026-06-04）
新增 `Battle/MRoundSystem`（移植 `system.go` roundState/stepRoundState 骨架）：RoundState 离散值
Intro(1)→Fight(2，授 ctrl)→PreOver(3，KO/超时缓冲)→Over(4，记分)→下一回合/整场结算；KO 与超时(血高者胜)
判胜负、双 KO 平局、三局两胜、回合间满血复位。计时在进入 Fight 起算。
验收 `Tests/.../Mugen/Battle/MRoundSystemTests.cs`（7 例：状态序列/KO/超时/双KO/整场/复位/确定性）。

#### ✅ R-2P — 双人对战接通（2026-06-04）
引擎双向 TryHit + R-GHV 联调跑通：headless 测试 `Tests/.../Mugen/Battle/TwoPlayerCombatTests.cs`（5 例）——
P1 普攻命中 P2 → P2 掉血 + 进受击 5000 → 走完恢复站立；攻方无反伤；经 `MRoundSystem` 驱动同样命中；确定性。
表现层 `MugenVersusView`（双 SpriteRenderer + 回合系统，P1 键盘/P2 木桩）+ 菜单「创建对战场景 (kfm vs kfm)」。
**Unity 实测（MCP execute_code 注入一拳）：P2 life 1000→877、PendingState=5000、MoveType=H、ghv.shake=4。**

### Tier B — 控制器补全（最适合多 agent 并行；每人认领几个）

> 现 21/~110。每个控制器 = 一个 `R-CTRL-<名>` 微任务，独立单测。按「受击/对战必需」优先。
> Ikemen 源统一在 `bytecode.go`（StateController 子类型）。

- 🔧 **R-CTRL-hit**（受击必需，优先，Codex 2026-06-04 部分落地）：已接 `HitVelSet` `VelMul`
  `HitAdd` `HitFallSet` `HitFallVel` `Gravity`；**`AttackMulSet` `DefenceMulSet` 已于 R-HITDEF chunk-2
  落地（攻防倍率运行态 + 伤害公式，2026-06-05）**。剩余 `HitFallDamage` `HitOverride` `PosFreeze`
  需要受击 fall damage/hitoverride/posfreeze 运行态容器后再补。
- ⬜ **R-CTRL-fx**（打击感）：`Explod` `ModifyExplod` `RemoveExplod` `AfterImage` `EnvShake`
  `EnvColor` `PalFX` `AllPalFX` `BGPalFX` `Trans` `SprPriority` `Offset` `AngleDraw/Set/Add/Mul` `Width`。
- ⬜ **R-CTRL-snd**：`PlaySnd` `StopSnd` `SndPan`（依赖 R-SND 声音加载）。
- ⬜ **R-CTRL-pause**：`Pause` `SuperPause`（超必杀冻结对方，很多招依赖）。
- ⬜ **R-CTRL-helper**（大）：`Helper` `DestroySelf` `ParentVarSet/Add` `RootVarSet`
  `BindToParent` `BindToRoot` `BindToTarget` `ChangeAnim2`（依赖 R-ENT 实体系统）。
- ⬜ **R-CTRL-proj**：`Projectile`（依赖 R-ENT）。
- ⬜ **R-CTRL-target**（投技/连段）：`TargetState` `TargetBind` `TargetLifeAdd` `TargetPowerAdd`
  `TargetVelSet` `TargetVelAdd` `TargetDrop` `TargetFacing`。
- ⬜ **R-CTRL-var**：`VarRangeSet` `VarRandom` `RemapPal`。
- ⬜ **R-CTRL-misc**：`ScreenBound` `MakeDust` `ReversalDef`。

### Tier C — 命中与触发完整化

- 🔧 **R-HITDEF** HitDef 完整 130 字段（chunk-1 完成，2026-06-04，dotnet test 348/348）：
  本轮落地 **kill/guard.kill/fall.kill**（computeDamage 限血，char.go:8433）、**getpower/givepower/
  guard.getpower/guard.givepower**（命中/被防加能量 + lifetopowermul 默认 0.7/0.6/×0.5 + 超必杀 0 分支，
  char.go:931-961）、**forcestand**（蹲被击改判站立 5000 + statetype 翻 S，char.go:12241；默认 = ground.vel.Y!=0）、
  **air.type/air.animtype/fall.animtype/yaccel/fall.xvelocity/fall.yvelocity/fall.recover/fall.recovertime**
  解析与默认（air.type 随 ground.type、air.animtype 随 animtype、fall.animtype Up/Back）；
  顺修 **HitDefController.CopyInto 遗漏 bug**（air/fall 字段曾不拷到 live HitDef）。
  测试 `Tests/.../Mugen/Hit/HitDefFieldsTests.cs`（15 例）。
  **chunk-2 完成（2026-06-05，dotnet test 361/361）**：攻防倍率伤害公式——`MChar` 增运行态
  `AttackMul/CustomDefense/SuperDefenseMul/FallDefenseMul/DefenseMulDelay`（入 Clone/Hash）+
  `ComputeFinalDefense()`(char.go:12081-12085) + `AttackDamageMul()`；`MHitSystem.ComputeDamage` 重写为
  定点公式 `damage ×= (AttackMul×attackBase/100)/finalDefense → 至少1 → 不超剩血 → kill保底 → 四舍五入`
  (char.go:8440-8460)，attackBase/defenceBase 取自 `Constants.Attack/Defence`；新增控制器
  `AttackMulSetController`+`DefenceMulSetController`（mulType/onHit，MUGEN 默认 onHit=true·mulType=0，
  bytecode.go:10714-10789）接入解析器；受击当帧先置 movetype H 使 DefenceMulSet onHit 即时生效。
  测试 `Tests/.../Mugen/Hit/DamageMultiplierTests.cs`（13 例）。**副作用：AllCharactersGetHitTests 改为按
  各角色 defence 常量算期望伤害（Final defence=200 半伤，验证倍率对真实角色生效）。**
  **chunk-3 待办**：`p2getp1state`（投技，依赖 custom state ownership）`sparkno/sparkxy/hitsound/guardsound`
  （依赖 R-CTRL-fx/R-SND）`numhits/hitonce` 多段去重、`envshake.*`、`ground.cornerpush/air.cornerpush`、
  `down.*`、`fall.damage` 落地伤害（依赖 HitFallDamage 控制器）、`juggle`（归 R-JUGGLE）、`p1/p2sprpriority`。
- ✅ **R-TRG-SAE** `SelfAnimExist`/`AnimExist` trigger（2026-06-04，commit 待填）：编译器
  `UnaryFuncs` 发 `OC_animexist/OC_selfanimexist`（求值参数 n 后发码）+ `MChar.ReadTrigger` 弹参数查
  `AnimExists(n)`（undefined 透传）。对齐 Ikemen `char.go:5088/5102`、`compiler.go:1824/3588`。
  v1 单角色无 helper/共享动画，animexist 与 selfanimexist 同归本角色 AnimTable（分歧待 R-ENT）。
  测试 `Tests/.../Mugen/Expr/AnimExistTriggerTests.cs`（6 例）。**已解锁 R-FIX-3 换标准 common1。**
- ⬜ **R-TRG-batch** 其余 trigger（现 ~50/200+）：`animelemtime(n)` `animelemno(time)` `gethitvar` 全字段
  `proj*`（projcontact/projhit/projguarded）`num*`（numexplod/numhelper/numproj）`p2bodydist/p2dist`
  `screenpos/camerapos` `const()` 全字段 `sysvar/sysfvar`。按角色实际用到的优先；每个一单测。
- ⬜ **R-JUGGLE** juggle 系统真正生效（防无限连）。

### Tier D — 导入即用所需的子系统

- ⬜ **R-ENT** 实体系统：helper / projectile / explod 作为引擎内独立实体（带各自 MChar 状态 + 生命周期 +
  纳入 Clone/Hash/快照）。这是 helper/projectile/explod 一切的地基。
- ⬜ **R-DMG-PIPELINE**（依赖 R-ENT）伤害两段式应用：现 `MHitSystem.ApplyHit/ApplyGuard` 命中当场扣血
  （单帧单次命中与 Ikemen 逐位等价）。Ikemen 是命中阶段累加 `ghv.damage`（char.go:11063/11078）→ 防守方
  自身 tick 一次性 `lifeAdd(-ghv.damage, absolute=true)`（char.go:11743）+ 中间 KO 排序（char.go:11098-11140）。
  **仅当同帧多次命中（弹幕/helper）或同帧读 life 的触发器才分歧**，故等 R-ENT 有多实体时连 KO 排序一并改。
  验收：构造同帧两次命中，离散 life 对账 Ikemen trace（R-ORACLE）。
- ⬜ **R-SND** 声音：SND 文件加载（逻辑层产数据）+ 表现层播放（PlaySnd 触发）。
- ⬜ **R-PAL** `.act` 调色板加载 + palette 选择 + PalFX 着色（现 SFFv1 共享板有局限，已记）。
- ⬜ **R-LOCALCOORD** localcoord 缩放（不同坐标系角色，clsn/pos 缩放）。

### Tier E — 确定性 / 回滚（课题核心论据，M10）

- ⬜ **R-DET-1** 黄金哈希从旧 ECS 迁 `MBattleEngine.ComputeHash()`；审计全新增字段进 `MChar.WriteHash`。
- ⬜ **R-DET-2** 旧 clean-room 引擎（`Game/Expr|Systems|Command|States`）+ 平铺测试**成对退役**（删码退测，需用户拍板降测试数）。
- ⬜ **R-ROLLBACK** `RollbackPredictor` + 逐帧 desync hash 对账 + KCP 输入预测/确认/重模拟（参考 `rollback.go`）。

### Tier F — 差分对账基建（贯穿，越早越好）

- ⬜ **R-ORACLE** Ikemen headless trace：调研能否改 `char.go` 加帧日志 `go build` 出带 trace 版本
  （MIT 可改），或 Lua dump 每帧 KFM 的 stateno/animelem/pos/vel → 存 `Tests/.../Golden/kfm_trace.json`。
  先查 `go version`。拿到后把 Tier A–C 的「手算 oracle」逐步替换为真 trace 断言。

### Tier G — 决斗场 demo 场景（**最后做**，列入计划）

- ⬜ **R-ARENA** 决斗场 demo：地面/边界/相机跟随 + HUD（血条/能量/计时/连段/胜负）+ 2P/简易 AI +
  「导入任意已转换角色 → 丢进场 → 开打」的装配入口。依赖 R-2P / R-ROUND / R-ENT / R-SND。
  **目标达成判据 = 随意导入一个外部 MUGEN 角色（转换器已就绪）即可直接在决斗场测试。**

---

## 3. 多 agent 协议 + 认领

- 一个 agent 认领一个 `R*`，在 **git worktree 隔离**中干（`CLAUDE.md §5`）。
- **认领前先看依赖**：Tier A 的 R-GHV 依赖 Tier B 的 R-CTRL-hit；R-2P 依赖 R-GHV；R-FIX-3 依赖 R-TRG-SAE；
  helper/proj 全依赖 R-ENT。**无依赖的 R-CTRL-* / R-TRG-batch / R-HITDEF / R-ORACLE 可立即并行多开。**
- 冲突由黄金哈希测试兜底回归（`R-DET-1` 落地后更稳）。
- 完成即按 §0 六门验收 → 本文标 ✅ → commit + 执行日志。

### 认领状态表（动态更新）
| 任务 | 负责人/agent | 状态 |
|---|---|---|
| R-FIX-1 ChangeAnim 守卫 | （本会话） | ✅ 2026-06-04 |
| R-CTRL-hit | Codex（本会话） | 🔧 2026-06-04 部分完成：HitVelSet/VelMul/HitAdd/HitFallSet/HitFallVel/Gravity |
| R-GHV 受击状态机 | Claude（本会话） | ✅ 2026-06-04 |
| R-ROUND 回合机 | Claude（本会话） | ✅ 2026-06-04 |
| R-2P 双人对战 | Claude（本会话） | ✅ 2026-06-04（headless+Unity 实测） |
| 10 角色受击 TDD 验收 | Claude（本会话） | ✅ 2026-06-04（20/20 全绿） |
| R-TRG-SAE SelfAnimExist | Claude（本会话） | ✅ 2026-06-04 |
| R-HITDEF chunk-1（kill/power/forcestand/air·fall 解析+CopyInto 修复） | Claude | ✅ 2026-06-04（348/348） |
| R-HITDEF chunk-2（攻防倍率公式 + AttackMulSet/DefenceMulSet） | Claude | 🔧 2026-06-05（361/361；chunk-3 见 §2 Tier C） |
| R-ORACLE 差分 trace | — | ⬜ |
| …（其余见 §2） | — | ⬜ |

---

## 4. 当前完整度快照（对照 Ikemen，2026-06-04）

移植 5389 行 C# / ≈51000 行战斗相关 Go ≈ **10–11%（行）**；按功能加权 **≈15%**，且集中在「单角色站走蹲跳」。
命令系统 ~80%（最扎实）。

**2026-06-04 更新：Tier A 全部完成（R-GHV/R-ROUND/R-2P），dotnet test 333/333。**
「两个角色能真打」从 ≈0% 跨到**可玩雏形**：P1 普攻命中 P2 → P2 进受击机走完反应→恢复；回合 intro→fight→KO→结算可走完。
10 个真实角色（Ananzi/Animus/Final/Gustavo/Hashi/Janos/Maxine/Noroko/Peketo/Shar-Makai）逐个跑受击 TDD **20/20 全绿**。
功能加权完整度升至 **≈22-25%**。下一坎 = R-HITDEF（HitDef 完整字段，让真实招式的击退/击飞/投技精确）+ Tier B 剩余控制器（Codex）
+ Tier D 子系统（helper/proj/声音）。最终目标：导入任意外部 MUGEN 角色（转换器已就绪）→ 决斗场（R-ARENA）即测。
